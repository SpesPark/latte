# Latte — Project Roadmap

> Multi-session execution plan for Latte (working folder: `Latte/` — renamed from `Caffeinated-Clone/` in S8).
>
> **Current phase**: v1.9 line (S20 ship) → i18n (S31–S36) → iCloud dark pre-build (S37–S43) → reliability + store prep (S44–S47) → iCloud design audit + doc fixes (S48–S49) → **S50 whole-app audit (Opus 4.8) + 5 fixes; 691/691 tests passing**
> **Current session**: S50 (complete) — 4 parallel review agents over ~11k LOC (0 CRITICAL) + 5 fixes T1–T5 (onboarding lockout, os_log PII, post-stop spurious vote, settings encode guard, Wi-Fi When-In-Use). 681 → 691 tests, both builds 0 warnings, all gates clean. Autonomous backlog exhausted again; next track = owner-driven App Store upload
> **Next session entry point**: see [docs/SESSION_HANDOFF.md](docs/SESSION_HANDOFF.md) — owner-driven App Store upload chain (merge PR #1 · Team ID → `DEVELOPMENT_TEAM` · screenshots 2880×1800 hard-blocker · ASC · signing) + Wi-Fi When-In-Use device-verify; iCloud Phase 2/3 still S8.5-gated

---

## Vision

Latte is a macOS menu bar utility that keeps a Mac awake **automatically based on context** (calendar, apps, Wi-Fi, Focus) — solving the "Mac slept mid-Zoom-call" pain that incumbents (Amphetamine, Caffeinated) leave unaddressed.

**Decisions locked** (see `docs/design/01-PRD.md` §12):
- Name: **Latte**
- Min OS: **macOS 13 Ventura**
- Pricing: **$2.99 one-time** (App Store, Small Business 15%)
- Source: **private repo**
- Phase scope: **A (automation) + C (design)** at launch; B (iOS) deferred to Phase 2

---

## Multi-session plan

Each row is one focused session. Sessions are sequential — earlier ones produce inputs for later ones.

| # | Session theme | Goals | Deliverables | Status |
|---|---|---|---|---|
| 1 | **Foundation: PRD + Architecture** | Lock product scope, lock module/layer structure | `01-PRD.md`, `02-architecture.md`, `ROADMAP.md`, `SESSION_HANDOFF.md` | 🟢 Done |
| 2 | **Design close-out** | Resolve all Open Questions before code | `03-state-machine.md`, `04-data-model.md`, sign-off on 01+02 | 🟢 Done |
| 3 | **Phase 1.0 implementation** | Rewrite Sources to match architecture; implement MVP features | All `Sources/**/*.swift`, replaces current skeleton | 🟢 Done |
| 4 | **Phase 1.A — Triggers** | Calendar + App + Wi-Fi + Focus triggers fully implemented | `Sources/Triggers/*.swift`, integration tests | 🟢 Done |
| 5 | **Phase 1.C — Design polish** | Coffee cup animation, Liquid Glass + fallback, app icon spec | UI files, asset catalog updates | 🟢 Done |
| 6 | **Build verification** | First successful build, fix compile errors, run tests | Working `.app` bundle, all tests passing | 🟢 Done |
| 7 | **Test coverage + QA** | ≥80% coverage, manual smoke test on macOS 13/14/15 | Test files, QA log | 🟢 Done |
| 8a | **Code & metadata prep** *(no Dev Program needed)* | Bundle ID + version 1.0.0, marketing site HTML, App Store metadata text, Privacy Policy, screenshot guide, v2 backlog | `docs/site/`, `docs/store/`, `docs/v2-backlog.md`, project.yml @ 1.0.0 | 🟢 Done |
| 8a-research | **S8b research preamble** *(doc-only)* | 4 parallel agents, 45-min market scan: wedge/pricing/triggers/ASO. Outcome-first marketing pivot, $2.99 retained, v2-backlog reshuffled. | `docs/SESSION_HANDOFF.md` preamble, memory `project_latte_session8b_research.md` | 🟢 Done |
| 8a-expand | **v1.0 scope expansion** *(doc + code)* | V2-01 menu-bar awake viz, V2-04 EKCalendar picker, V2-10 cleanup, NEW Launch at Login, NEW first-run onboarding wizard. 263 → 284 tests. | Commits `05f8c2d`, `6109859`, `7decb84`, `2da1f3d` | 🟢 Done |
| 8b | **Owner pre-flight smoke (round 1)** | Owner ran first new build through smoke; 4 rounds of fix-first surfaced and resolved P1-P4 across icon styles, trigger restart, onboarding-only first launch, WiFi/Calendar immediate-reflect, and Focus deferral (V2-03b). Tail commit `31389cd`. | 288/288 tests pass; `aa42e90`+`45e73fc`+`acea09a`+`31389cd` series of fix commits. | 🟢 Done |
| 8c | **Smoke harness V2-30 + Latte URL scheme + automated marketing capture** *(no Dev Program needed)* | Five S8c sub-passes landed: (1) V2-30 cross-project harness (`~/dev/smoke-harness/`) with 15 lib helpers + run.sh + JSONL reporter + xcuitest-target template; (2) Latte's deferred S8b D/E/F smoke automated as 8 functional scenarios; (3) `latte://settings/<tab>` + `latte://demo/cup` URL schemes added to Latte production code (12 unit tests, 297 → 309 total); (4) window-id capture (`screencapture -l <wid>`) makes capture Claude/Bartender-immune; (5) `deploy_pages.sh` + `tcc_bootstrap.sh` shrink owner-side ops. **4/5 marketing shots fully automated** (Shot 4 = ~30s manual hover). gh-pages branch pre-staged at `../latte-gh-pages-staging/`. | 12 scenarios PASS in ~2:30; 17 PNG artifacts; tail commit `ce69864`; v2-backlog V2-30 = Done | 🟢 Done |
| 8d | **GitHub Pages live + marketing capture finalize** *(owner-side, ~5 min)* | **Pages live portion CLOSED in S29 (row 28)** — SpesPark/latte PUBLIC repo, both branches (main + gh-pages) pushed, https://spespark.github.io/latte/ + /privacy.html validated 200 + text/html. Marketing capture portion (screenshots picked + renamed into `docs/store/screenshots/`) moved into the S9 ASC submission flow because the URLs are now live and the screenshot work is logically part of App Store submission, not Pages deployment. Original plan retained for historical reference: Owner runs `~/dev/smoke-harness/run.sh --project .` (12 scenarios → 17 PNGs in `.smoke/artifacts/`), picks {01-onboarding, 09-general, 10-triggers, 11-about, 12-cup}, captures Shot 4 manually (~30s). | Live: https://spespark.github.io/latte/ + /privacy.html; deferred: `docs/store/screenshots/01..05.png` (now part of S9 ASC flow) | 🟢 Done (Pages portion via S29) + 🟡 Deferred (screenshots → S9 ASC) |
| 8.5 | **Apple Developer Program enrollment** *(owner-only HARD GATE)* | Pay $99/yr, wait 1-2 days for approval | Team ID visible in Xcode → Settings → Accounts; App Store Connect access | 🔴 Blocked (owner) |
| 9 | **v1.1 features pass 1 — V2-05 Schedule trigger** *(in-flight while owner blocks 8d/8.5)* | Recurring time-of-day windows (weekday × time range × multiple entries), midnight-crossing semantics, 30s polling, full UI + onboarding wizard wiring. 309 → 337 tests. | `Sources/Triggers/ScheduleTrigger.swift`, `Tests/ScheduleTriggerTests.swift` (28 tests), `TriggersTab` ScheduleTriggerConfigForm, AppEnvironment registration | 🟢 Done |
| 9b | **v1.1 features pass 2 — Battery-aware (C-1) + Pause-all (C-9)** | C-1: `PowerSource` protocol (`IOPowerSource` real + `MockPowerSource`) + `AwakeManager.requireACForAwake` + Settings → General → "Sleep when on battery". C-9: `AwakeManager.triggersPaused` + popover top-row toggle. Both gates at manager input boundary; constraint flips auto-release when active. 337 → 360 tests. | `Sources/Core/PowerSource.swift`, AwakeManager constraint logic, GeneralTab + MenuBarRoot UI, `Tests/PowerSourceAndConstraintsTests.swift` (23 new) | 🟢 Done |
| 9c | **v1.1 polish — About tab assertion display (A-1) + V2-02 surface parity** | About tab gains live State/Mode/Reason/Power status card backed by pure `AssertionStatusFormatter` helpers. CalendarTrigger / WiFiTrigger / ScheduleTrigger gain `reevaluateWatched()` matching AppTrigger's S7.9 contract; forms switched to unified API. 360 → 380 tests. | `Sources/UI/Settings/{AboutTab,AssertionStatusFormatter}.swift`, reevaluateWatched on Calendar/WiFi/Schedule, `Tests/{AssertionStatusFormatterTests,ReevaluateWatchedTests}.swift` (20 new) | 🟢 Done |
| 9d | **v1.1 features pass 3 — B1 keyboard shortcut (⌘⇧L) + smoke harness coverage + latent debt** | Carbon `RegisterEventHotKey` toggles `manager.toggle()` via `KeyboardShortcutCoordinator` + `HotKeyRegistrar` protocol + `MockHotKeyRegistrar`; off by default, opt-in via Settings → General. 4 new smoke scenarios (15 pause-all / 16 battery-aware / 17 about-status / 18 schedule) cover the surfaces shipped in S9 family — full batch passes 18/18. Existing scenarios 08 / 13 patched for `pipefail` + `grep -Eq` SIGPIPE false-negative; pmset grep broadened to match by owning-pid line (catches both `NoDisplaySleepAssertion` and `NoIdleSleepAssertion` regardless of `allowDisplaySleep`). 380 → 388 tests. | `Sources/Core/KeyboardShortcutCoordinator.swift`, AppEnvironment + GeneralTab toggle, `.smoke/scenarios/{15,16,17,18}*.sh`, scenarios 08/13 patches, `Tests/KeyboardShortcutCoordinatorTests.swift` (8 new) | 🟢 Done |
| 9e | **S10 follow-through — simplify-pass + Settings `.resizable` + AppTrigger friendly Reason + smoke 18 comment + Activate at launch** *(2026-04-30)* | (1) Code-reviewer simplify-pass on S9 family — APPROVE verdict, 4 findings (2 MED + 2 LOW) all resolved: `LatteLog` consolidation, `ScheduleEntryRow` label commit on `.onChange`, `CarbonHotKeyRegistrar` doc note, `reevaluateWatched()` comment trims. (2) Settings window resizable — `.resizable` styleMask + `setContentSize` + `minWidth/minHeight`; opens at default 460×360 but user can drag to grow. (3) AppTrigger `emitOn` resolves bundle ids via `WorkspaceSource.displayInfo` so vote reason reads "App: Zoom" instead of "App: us.zoom.xos" — 3 regression tests for curated/fallback/explicit-override branches. (4) Smoke 18-schedule comment hygiene (was "non-resizable" → now "resizable but harness lacks AX permission"). (5) Activate at launch — closes S2-vintage `SettingsKey.activateOnLaunch` deferred debt: `AppEnvironment.activateOnLaunch` published + `applyActivateOnLaunchIfEnabled()` (3 gates: flag/onboarding/asleep), `LatteAppDelegate` calls post-`bootTriggers`, `AwakeManager.activate(reason:)` honours non-`.user` reason via post-FSM override (full plumb-through deferred — 8 source + 7 test sites tracked in handoff). 388 → 398 tests; smoke 18 → 19 scenarios (new `19-activate-on-launch.sh` covers ON/OFF/onboarding-incomplete). | `Sources/{App/AppEnvironment,App/LatteApp,Core/AwakeManager,Core/Logging,Core/PowerSource,Core/KeyboardShortcutCoordinator,Triggers/AppTrigger,Triggers/{Calendar,WiFi,Schedule}Trigger,UI/Settings/{GeneralTab,SettingsRoot,SettingsWindowController,TriggersTab}}.swift`, `Tests/{AppEnvironmentTests,AppTriggerTests,TriggerCoordinatorTests}.swift`, `.smoke/scenarios/{18-schedule,19-activate-on-launch}.sh` | 🟢 Done |
| 28 | **S29 / S8d Pages unlock + V2-22 GitHub remote — no Latte source touched** *(2026-05-15)* | Same-day decision sequence from owner closes the S28-deferred Step C (S8d). Three owner decisions resolved in-session: **(Q1) username `SpesPark`** — initial pick `Spes` and script default `bj-park` both 200 (taken) per `curl -s -o /dev/null -w "%{http_code}" https://github.com/<name>` availability probe; 13 verified-available variants offered; owner picked `SpesPark`. **(Q2) `gh` CLI** (`brew install gh` → 2.92.0; owner ran `gh auth login` with GitHub.com + HTTPS + browser OAuth via Google SSO — 8-char device code paste at github.com/login/device; token scopes `gist`+`read:org`+`repo`+`workflow` stored in macOS keyring). **(Q3) SSH key not registered** — HTTPS + token path adopted; `gh auth setup-git` wrote `[credential "https://github.com"] helper = !/opt/homebrew/bin/gh auth git-credential` to `~/.gitconfig` (the in-flow "Authenticate Git? Yes" prompt did NOT write the helper — explicit `setup-git` step is required). Then: `gh repo create SpesPark/latte --public --source=. --remote=origin --push` from main repo creates PUBLIC repo + adds origin + pushes main in one shot (V2-22 closed as side effect); `cd ../latte-gh-pages-staging && git push -u origin gh-pages` pushes the S8c-staged Pages content (worktree shares `.git`, origin propagated — no `git remote add` needed); explicit `POST /repos/SpesPark/latte/pages` returned `409 already enabled` (GitHub auto-provisions Pages when gh-pages branch lands on a new repo — S28's predicted ~5 min web-UI clicking was actually 0 min); polled `gh api .../pages --jq '.status'` until `built` (~36s); `scripts/validate_pages.sh https://spespark.github.io/latte/` returned `[validate] / OK (200, text/html; charset=utf-8)` + `/privacy.html OK` + `all checks passed — Pages site is App Store submission ready`. **Security**: pre-push `git grep` for secret patterns (`api[_-]?key`/`secret[_-]?key`/`password\s*=`/`TOKEN\s*=`/`sk-[A-Za-z0-9]{20,}`/`ghp_...`/`AKIA...`/`PRIVATE KEY`) + tracked-file scan (`.env`/`.pem`/`.p8`/credentials) both clean. **Patterns NEW (5)**: (a) **Availability probe before commitment** — `curl -s -o /dev/null -w "%{http_code}"` for 200/404 username status; batch 15 variants in a `for` loop; owners pick from verified-available menu instead of hitting collision at `gh repo create`. (b) **`gh auth setup-git` is not implied by `gh auth login`** — even with "Authenticate Git? Yes" in the OAuth flow, `~/.gitconfig` was empty post-login; explicit `gh auth setup-git` call after login removes the conditional behavior. (c) **GitHub Pages auto-enables on first `gh-pages` branch push** — `POST /repos/<owner>/<repo>/pages` returned 409 immediately after `git push -u origin gh-pages`; no Settings → Pages click needed; validate path is `gh api .../pages` polling + `scripts/validate_pages.sh`. (d) **`gh repo create --source=. --push` auto-completes V2-22-style "remote add" + "first push"** — single command for the post-decision unlock; V2-22 closed as side effect, not separately tracked. (e) **Worktrees share `.git`, so `origin` propagates** — adding origin to main repo at `~/Documents/Claude/Projects/Latte` made it visible from staging worktree at `~/Documents/Claude/Projects/latte-gh-pages-staging` without re-running `git remote add`; cleanest pattern for multi-branch deployment (main + gh-pages in separate worktrees). **Tests unchanged**: 587/587 PASS (no Latte source touched). **Smoke unchanged**: 22/22 (no scenarios touched). **V2-22 closed** (GitHub remote was the only V2-22 requirement). | No Latte source files touched. Infrastructure: `gh 2.92.0` installed via Homebrew (system-wide, not in repo); `~/.gitconfig` (owner-local, +credential.https://github.com helper); SpesPark GitHub account (new, Google SSO); https://github.com/SpesPark/latte (PUBLIC, both branches pushed); https://spespark.github.io/latte/ + /privacy.html (live, 200). Docs commits: SESSION_HANDOFF.md (full overwrite for S29), ROADMAP.md (header + row 8d status flip + this row 28), `docs/v2-backlog.md` (V2-22 row → ✅ Done) | 🟢 Done |
| 27 | **S28 / Smoke harness rebuild gate + feature-presence assertion** *(2026-05-05)* | Closes the S27 stale-binary infrastructure discovery without touching Latte source. **Step A — rebuild gate (commit `beae984`)**: cross-project change to `~/dev/smoke-harness/run.sh` (owner-local tree, not in this repo). Adds 3 optional `build_*` keys to `.smoke/config.yml` (`build_command`, `build_source_dir`, `build_binary_path`). Before running scenarios, harness compares newest source mtime under `build_source_dir` to the binary mtime; if any source is newer (or binary is missing) it runs `build_command` from the project root and fails fast on rebuild error (exit 6). Empty `build_command` skips the gate (preserves non-Xcode projects). New `--no-build` flag forces skip (useful when iterating on harness itself). Verified with 3-case test: up-to-date binary → "rebuild = up to date" no build runs; touched source → "STEP rebuild (source newer than binary)" trigger + mtime updated to post-build; `--no-build` with stale source → "skipped (--no-build)" + binary unchanged. README updated with required vs optional key tables + `--no-build` flag docs. Latte's config.yml gained: `build_command: xcodebuild -scheme Latte -configuration Release build -quiet` / `build_source_dir: Sources` / `build_binary_path: …/Latte.app/Contents/MacOS/Latte`. **Step B — feature-presence assertion (commit `7d10ef4`)**: cross-project lib helper `~/dev/smoke-harness/lib/assert_binary_type.sh` (owner-local) + per-scenario assertion calls in 4 scenarios (in this repo). Strategy: use `nm | xcrun swift-demangle | grep -E '<pattern>'` against the Mach-O binary to assert a Swift type symbol is present. Beats osascript / NSAccessibility (no permission grant needed) and OSLog signposts (no source instrumentation needed); deterministic and cross-cutting (every Swift macOS app's binary table demangles the same way). Per-scenario calls: `18-schedule.sh` → `\bLatte\.ScheduleTrigger\b`; `20-external-display.sh` → `\bLatte\.ExternalDisplayTrigger\b`; `21-shortcut-recorder.sh` → `\bLatte\.KeyboardShortcutCoordinator\b`; `22-activity-log.sh` → `\bLatte\.ActivityTab\b`. Each runs at scenario top (after `set -euo pipefail` + log source) so a stale binary FAILs the scenario with a clear message ("stale or feature-stripped binary") before any UI work happens. Verified: 22/22 PASS on fresh binary; negative test (bogus pattern `\bLatte\.NoSuchTabZZZ\b`) makes scenario 22 FAIL deterministically with rc=1. **Step C — S8d deferred**: Owner-blocked items (GitHub username decision / `gh` install vs web UI / SSH key registration) — owner has no current decision; carried forward to next session. **Patterns NEW (4)**: (a) **`xcodebuild test` rebuilds the test bundle but not the standalone Release `.app`** — confirmed in S27 discovery, codified in S28 rebuild gate. CI / smoke harnesses must run `xcodebuild build -configuration Release` explicitly when scenarios target the Release `.app`. (b) **mtime-based rebuild gate over unconditional rebuild** — `find <source_dir> -type f -newer <binary>` runs in milliseconds for typical Swift projects (~50-500 files), keeps "no source change" runs at full speed (no ~30s rebuild penalty), and the explicit "newer than binary" semantic is more defensible than wall-clock thresholds. (c) **`nm | xcrun swift-demangle | grep` is the cheapest "is feature X compiled in?" check** — works without running the app, without NSAccessibility, without source-side instrumentation. The trade-off: only catches "type was removed entirely" not "type's body was emptied or render no-op'd." That's an acceptable floor for closing the URL-routing-fallback false-positive class. (d) **Cross-project tooling commits split: harness changes go to owner-local tree, project-specific config goes in repo** — Latte's commit only changes `.smoke/config.yml` + 4 scenario files (35 lines added across them). Harness `run.sh`/`assert_binary_type.sh`/`README.md` changes are NOT in this repo. Future repo readers see only the per-project knobs; the cross-project plumbing lives where it can serve other projects. **Tests unchanged**: 587/587 PASS in 9.08s — no Latte source touched in S28. | `~/dev/smoke-harness/run.sh` (owner-local, +rebuild gate + --no-build flag), `~/dev/smoke-harness/lib/assert_binary_type.sh` (owner-local, NEW helper), `~/dev/smoke-harness/README.md` (owner-local, +rebuild-gate docs), `.smoke/config.yml` (+9 lines build_*), `.smoke/scenarios/{18-schedule,20-external-display,21-shortcut-recorder,22-activity-log}.sh` (+8-9 lines each, assertion call) | 🟢 Done (Step A+B) + 🔴 Blocked (Step C / S8d / owner) |
| 26 | **S27 / Owner manual-smoke fix-first — drop ⌘, popover binding + ⌘⇧L conflict resolution** *(2026-05-05)* | Owner ran S26 Phase 1 manual smoke (popover ⌘, / ⌘Q): ⌘Q worked, ⌘, silently failed. Root cause: Korean IME consumes `,` as punctuation passthrough while letters (q) bypass IME under ⌘ modifier — `event.charactersIgnoringModifiers` returns IME-transformed character, not US-layout `,`. Considered `event.keyCode == kVK_ANSI_Comma (43)` fallback but owner judged ⌘, popover binding low-value: popover already requires mouse for trigger selection (durations, Wi-Fi, schedule preset, recurring preset), so a 1-step ⌘, → Settings shortcut to a context the user is already mouse-bound to is marginal. **(1) Drop ⌘, popover binding** *(`6d4c95a`)*: `PopoverKeyAction` enum simplified — `.openSettings` case removed, only `.quit` + `.passthrough` remain. `PopoverKeyHandler.decide` Q-only switch. `MenuBarRoot.installKeyMonitor` `.openSettings` arm removed. ⌘, now passes through so any other app that consumes ⌘, while a Latte popover happens to be visible keeps working. Tests 586 → 587 (+1 net): removed `testCommandCommaOpensSettings`, `testCommandOptionCommaPassesThrough`, `testNumPadModifierIgnored`; added `testCommandCommaPassesThrough` (regression guard for the S27 removal), `testCommandOptionQPassesThrough`, `testNumPadModifierIgnoredForQuit`, `testCapsLockIgnoredForQuit`. **(2) ⌘⇧L Claude-Code conflict resolution** *(owner-side, no commit)*: owner reported ⌘⇧L 동작 안 됨 in both ON and OFF Latte states. Diagnosed as Claude Code (or other LSUIElement app) holding the global chord first — Carbon `RegisterEventHotKey` is OS-level, second registrant gets `kEventHotKeyExistsErr`. Initial misdescription corrected mid-session: original ⌘⇧L behaviour (caffeinate-OFF → `.userActivate(.indefinite)`; caffeinate-ON → `.constraintDeactivate` + `disableAll()` via `postUserExplicitDeactivateIfTransitioned`) already matches owner's proposed redesign 100% — `AwakeManager.toggle()` at line 683 unchanged from v1.1. Recommendation: keep code unchanged; owner uses chord recorder (shipped v1.2 / S11) to rebind to `⌘⌃L` (control + command + L — control rarely participates in third-party global chords, lowest collision risk). Owner reported successful rebind + verified popover toggle / awake on/off via the new chord. **(3) Step 3 dropped — smoke 23 not viable**: planned smoke regression for "popover ⌘, → Settings does not open" rejected during scoping. Smoke harness scenario 21 (shortcut-recorder) explicitly notes "Pure SwiftUI key capture isn't scriptable from a smoke harness without Accessibility permission" — same constraint applies to NSEvent injection into popover. Owner manual smoke territory; the unit-test layer (`testCommandCommaPassesThrough`) provides permanent regression guard at the value-API level where it actually fires deterministically. **Patterns NEW (3)**: (a) **Korean IME consumes punctuation under ⌘ but lets letters pass** — macOS Hangul IME forwards letter-keys (a-z) to apps untransformed when ⌘ modifier is held, so ⌘Q matches `"q"` reliably; punctuation (`,`, `.`, `;`, etc.) goes through IME's character-mapping path, so ⌘, may yield IME-transformed character (or be consumed entirely) depending on input mode. Don't rely on `charactersIgnoringModifiers` alone for punctuation chords — either drop the chord (simplest) or add `event.keyCode` fallback (`kVK_ANSI_Comma == 43`). (b) **Manual smoke is the regression test for owner-environment-specific bugs** — Korean IME consuming `,` only manifests when Korean input mode is active, which CI test machines and unit tests can't reproduce; the only place this surfaces is owner manual smoke. The S27 fix follows scenario 21's precedent: capture the constraint in a unit test where it CAN be deterministic (`testCommandCommaPassesThrough` ensures ⌘, never reactivates as a popover binding). (c) **"Default chord choice in shipped product" decision pattern**: when a user reports the default global chord (⌘⇧L) collides with their environment, prefer the chord-recorder rebind over changing the default. Rationale: any new default risks colliding with a different inevitable third-party app for a different user. Latte's chord recorder ships v1.2; the per-user fix is 5 seconds. The default stays for new users until evidence shows a systematic collision (e.g., colliding with macOS itself). **Step 4 — STALE-BINARY DISCOVERY (post-wrap)**: while concluding the session, owner reported "preset 기능 / activity tab 사라지고 / popover layout 원래대로 돌아갔다." Source intact (Activity tab, Recurring presets, all v1.x features present in `Sources/`). Build artefact mtime check: Release `Build/Products/Release/Latte.app/Contents/MacOS/Latte` = **Apr 30 10:32** (= v1.2 ship day). Test bundle = May 5 (today). Source = today. Conclusion: `xcodebuild test` rebuilds the test bundle (`LatteTests.xctest` inside Debug Latte.app's PlugIns) but does NOT update the standalone Release `.app`. Smoke harness (`~/dev/smoke-harness/run.sh`) uses Release `.app` at fixed config path and has no `xcodebuild build` step. Scenario 22 (activity-log) PASSED on stale binary because it only sends `latte://settings/activity` URL + captures window — doesn't verify Activity tab is in the binary's UI. **5+ days of owner manual smoke (S22~S27) ran against a v1.2-era binary**. Resolution this session: ran `xcodebuild -scheme Latte -configuration Release build` mid-session, fresh binary now at May 5 16:53. **Patterns NEW (additional 2)**: (d) **`xcodebuild test` ≠ `xcodebuild build` for standalone .app** — test scheme bakes xctest plug-in into Debug `.app`, leaves Release `.app` untouched. CI / harness must explicitly run `xcodebuild build -configuration Release`. (e) **Smoke scenarios that don't verify feature presence are false positives** — "Window appeared after URL routing" is not feature verification; need `osascript` System Events on tab labels or feature-specific plist read. **Carry-forward to next session (entry points #0/#1/#2 in HANDOFF)**: owner must verify fresh binary shows 7 features; smoke harness needs rebuild-before-run gap closed; S22~S27 owner-confirmed P-issues need re-verification against fresh binary because owner was running v1.2 the entire time. | `Sources/UI/MenuBar/{MenuBarRoot,PopoverKeyHandler}.swift`, `Tests/PopoverKeyHandlerTests.swift` | 🟢 Done (code) + 🟡 Discovery (carry-forward) |
| 25 | **S26 / Autonomous v1.x backlog drain — popover key handler (B6) + S8d Pages scripts + V2-22 prep + 14th simplify-pass** *(2026-05-05)* | First session entered with the v1.x P-issue queue empty (S25 closed it). Owner-approved "进行 in priority order until context fills" pacing. Four-commit chain. **(1) B6 — popover ⌘, / ⌘Q via NSEvent.addLocalMonitor** *(`cb1c054`)*: closes S22 P-issue-4 deferred. SwiftUI `.keyboardShortcut(...)` doesn't fire inside `MenuBarExtra(.window)` popovers (LSUIElement / .accessory NSStatusItem context — popover window doesn't enter the SwiftUI keyEquivalent dispatch chain). Fix structure: pure-logic `PopoverKeyHandler.decide(modifiers:character:) -> PopoverKeyAction` enum (`.openSettings` / `.quit` / `.passthrough`). Strict matching: only `.command` participates in the chord mask check (.shift / .option / .control admixtures fall through). `.capsLock` / `.numericPad` / `.function` / `.help` are OS state bits ignored — numpad-comma + capslock-on still works. NSEvent monitor installed in `MenuBarRoot.onAppear` (popover open) and torn down in `.onDisappear`; while open, ⌘, → openSettings() + consume, ⌘Q → NSApp.terminate(nil) + consume; ⌘⇧Q logout / ⌘⇧L global awake-toggle / any other modifier combination passes through so system + Carbon-registered chords stay authoritative. PopoverKeyHandler is a value-API layer (no NSEvent in the API) — 12 new XCTest cases cover all branches + edge cases (numpad, capslock, ⌘L letter passthrough, nil/empty character). NSEvent.addLocalMonitor wiring itself is owner-side manual smoke (Step 6 region 7 deferred). 574 → 586 tests (+12). Smoke 22/22 unchanged. **(2) S8d Pages deploy scripts** *(`efb04d5`)*: S8d remains owner-blocked for the GitHub remote URL + auth bits, but the cp/commit/push flow itself can be automated so the owner-side step shrinks to a single `scripts/deploy_pages.sh` invocation after one-time `git remote add origin ...`. New `scripts/deploy_pages.sh`: diffs `docs/site/{index,privacy}.html` against the pre-staged `../latte-gh-pages-staging/` worktree, `--dry-run` mode prints the diff without writing, commits only changed files, pushes to origin/gh-pages if a remote is configured (otherwise stops at local commit and prints one-time setup steps), guesses the live URL from origin and prints the validate command. Exit codes: 0 success/no-op, 1 precondition, 2 git failure. New `scripts/validate_pages.sh`: curl-based App-Store-readiness check on landing + privacy.html, verifies HTTP 2xx + content-type=text/html + non-empty `<title>`, auto-derives base URL from gh-pages origin if not passed. `docs/site/README.md` updated to point at the new scripts as the preferred path; manual cp+commit retained as fallback. **(3) 14th simplify-pass on B6** *(`4527ed0`)*: `PopoverKeyHandler.decide` redundant-guard cleanup. Dropped the `!character.isEmpty` clause (empty string falls through `default: .passthrough` because no string literal case matches ""). Collapsed `let pressed = ...; guard pressed == .command` into a single guard with the intersection inline. 22-line body → 14-line body, 12/12 tests still GREEN. **(4) V2-22 prep — README refresh + .github/ templates** *(`5cd884a`)*: V2-22 (GitHub repo push + Pages enable) is owner-blocked for the auth/URL bits, but repo metadata can land ahead of time so the public landing is clean from day one. README.md status: v1.0 / 309 tests → v1.9 / 586 tests + 22/22 smoke. Trigger list expanded to reflect ExternalDisplay + Schedule + custom recurring presets + Activity log + custom global hotkey shipped in v1.1~v1.9. Tech stack: Swift 5.10 → Swift 6 + Xcode 26+. Smoke harness: 12 / ~2:30 → 22 / ~6:14. Directory layout reflects Sources/Core/ActivityLog + scripts/ + UI/Settings/Activity tab + MenuBar key handler. New `.github/ISSUE_TEMPLATE/{bug_report,feature_request}.md` + `config.yml` (blank issues disabled, App Store reviews + privacy URL as contact links). New `.github/PULL_REQUEST_TEMPLATE.md` with conventional-commit type hint + test-plan checklist + risk/blast-radius section. **Patterns NEW (3)**: (a) **`MenuBarExtra(.window)` popover blocks SwiftUI keyEquivalent dispatch** — confirmed empirically over S22→S26: `.keyboardShortcut(...)` modifiers attached to a Button inside the popover's body do not fire while the popover is the visible/key window in an LSUIElement / .accessory app. The structural fix is to install an `NSEvent.addLocalMonitorForEvents(.keyDown)` in the popover root's `.onAppear` and tear it down in `.onDisappear` so the keys map only while the popover is on-screen. (b) **NSEvent.ModifierFlags `.deviceIndependentFlagsMask` ≠ "user-pressed chord modifiers"** — the device-independent mask includes `.capsLock` / `.numericPad` / `.function` / `.help`, which are OS state bits set independently of user intent. Match against an explicit `[.command, .shift, .option, .control]` mask instead so numpad-comma / capslock-on still trigger ⌘, correctly. (c) **Pure-logic value-API for NSEvent-driven handlers** — extract the modifier+character → action decision into a value-API function (`decide(modifiers: NSEvent.ModifierFlags, character: String?) -> Action` enum). The NSEvent.addLocalMonitor wiring becomes a thin forwarder; all branch coverage is unit-testable without spinning up real key events. | `Sources/UI/MenuBar/{MenuBarRoot,PopoverKeyHandler}.swift`, `Tests/PopoverKeyHandlerTests.swift`, `scripts/{deploy_pages,validate_pages}.sh`, `docs/site/README.md`, `README.md`, `.github/{ISSUE_TEMPLATE/{bug_report,feature_request}.md,ISSUE_TEMPLATE/config.yml,PULL_REQUEST_TEMPLATE.md}` | 🟢 Done |
| 24 | **S25 / Cold-start Activity-tab placeholder flicker fix — pre-load via AppEnvironment hoist** *(2026-05-05)* | S24 deferred P-issue-4 closeout. Owner-driven manual smoke confirmed at session start that the flicker symptom owner first reported in S24 (entries-empty placeholder briefly visible on first-ever Activity tab entry per app launch) was still present even after this session's first attempted in-View `hasFirstSnapshot` two-flag gate. Diagnostic re-walk through the load sequence pinpointed the actor's lazy first-load (`Data(contentsOf:)` + `JSONDecoder.decode` synchronous within the actor hop) as the load-blocker the in-View gate couldn't bypass — between Task suspension and snapshot resumption, SwiftUI rendered at least one frame with `entries=[]`. Owner approved Option A (AppEnvironment hoist + boot-time pre-load) over the alternative skeleton-frame UX path, reasoning the snapshot pre-load also pays off in zero-actor-hop tab open and gives a single source of truth for any future menu-bar activity-summary surface. Implementation: `@Published var activityEntries: [ActivityLogEntry]?` on `AppEnvironment` with three-state semantics (`nil` = boot snapshot still resolving — only possible if user opens Settings within the first tens of ms of launch; `[]` = loaded but no entries; non-empty = loaded with data). New `loadActivityEntriesEagerly()` (idempotent, guards on nil) + `refreshActivityEntries()` (always-fresh fetch, used by retention change + notification reload). `LatteAppDelegate.applicationDidFinishLaunching` kicks off the eager pre-load in a parallel Task to `bootTriggers()`. ActivityTab refactored to a dumb view: removed `@State entries`, `hasLoaded`, `hasFirstSnapshot`, and the local `reload()` function; reads `environment.activityEntries` directly with an `if let entries = ...` 3-state branch. Retained `liveReloadTask` debouncer (now calls `environment.refreshActivityEntries()`). API cleanup: dropped `store: ActivityLogStore?` parameter from ActivityTab init; SettingsRoot and SettingsWindowController follow (transitive). **12th simplify-pass within S25**: `loadActivityEntriesEagerly` collapsed to a guard + delegate to `refreshActivityEntries` (eliminating duplicated cutoff math); ActivityTab body's three-state branch tightened from `let entriesOrNil = ...; let entries = entriesOrNil ?? []` + nil-check + isEmpty-check to a single `if let entries = environment.activityEntries` with nested isEmpty branch. Lazy-load contract preserved: `snapshot()` reads the file but never creates it (smoke 22 "file absent on fresh launch" still PASSES). 574 tests unchanged (refactor only — pure UI / state-source migration with no new behavior to test that wasn't already covered). Owner-confirmed flicker eliminated post-fix. **Patterns NEW (3)**: (a) **actor lazy-init blocks first-frame rendering** — when an actor hop also triggers expensive synchronous work (disk read + JSON decode) on its first call, in-View async-gating flags can't hide the latency from SwiftUI; the only fix is to pre-load the actor's first call off the rendering path (eager init at app boot). (b) **single-source-of-truth on @Published Optional[T] with three-state semantics** — `nil = loading`, `[] = loaded empty`, non-empty = loaded with data — collapses three concerns (in-flight, empty result, data ready) into one Optional read with `if let ... else if .isEmpty ... else ...`. Cleaner than separate `hasLoaded` + `entries` flags because state transitions are atomic at the @Published assignment. (c) **Hoist async-loaded view state to a parent ObservableObject when load timing matters** — when a child view's @State is sourced from an async fetch and the fetch's first call has perceptible latency (actor lazy-init, network call, etc.), hoist to a parent that can pre-load before the child mounts. Child becomes a dumb view; the @State → @Published migration also frees the data for cross-view reuse. | `Sources/App/{AppEnvironment,LatteApp}.swift`, `Sources/UI/Settings/{ActivityTab,SettingsRoot,SettingsWindowController}.swift` | 🟢 Done |
| 23 | **S24 / Step 8 owner-driven Activity-tab manual smoke — Export removal + spinner / re-entry flicker fix** *(2026-05-05)* | Step 8 entry per S23 handoff. Owner-driven manual smoke on Activity tab surfaced **3 P-issues**, all in `Sources/UI/Settings/ActivityTab.swift`. Single fix commit `0e13546` + this docs commit. Tests **581 → 574** (-7, all from removed `ActivityLogExporterTests.swift`). Smoke unchanged at 22 (export feature was never owner-side covered by harness). **(1) P-issue-1 — Activity tab Export CSV/JSON removed** — buttons remained non-functional through three fix attempts: S23 P-issue-5 (inline-as-Section-rows + `.buttonStyle(.bordered)`); S24 attempt 1 (move buttons OUT of Form into a footer `HStack` to bypass Form Section row tap-handling entirely); S24 attempt 2 (add `NSApp.activate(ignoringOtherApps: true)` + `panel.level = .modalPanel` to handle menu-bar-app modal restoration). NSSavePanel never appeared on owner's machine and `[activity] export tap` info-level log never emitted on `subsystem == "com.parkbyeongjun.latte"` — strong evidence the Button action closure itself never fired in any of the three configurations. Owner called the feature: cost > value. Removed `Sources/Core/ActivityLogExporter.swift` (only consumer was ActivityTab), `Tests/ActivityLogExporterTests.swift` (-7 tests), Export buttons / footer / `export(_:as:)` / VStack wrapper / `import AppKit` + `import UniformTypeIdentifiers` from ActivityTab.swift. **Diagnosis lessons new this session**: (a) `os.Logger.info` logs are filtered out by `log stream` defaults — `--info` flag (and `--debug`) is required to surface anything below `default`-level; without it a working tap closure looks identical to a non-firing one; (b) when the logging subsystem doesn't even capture an info-level diagnostic at the action-closure entry boundary, the closure isn't running — escalate to feature-removal rather than chasing the modal-display path. **(2) P-issue-2 — spinner-only frame on cold load** — `@State private var isLoading = true` initial value caused a 1-2 frame "ProgressView only" render at the top of an otherwise mostly-empty Form before charts were inserted on `await store.snapshot(...)` return. Owner perceived this as Activity-tab flicker on first entry. Removed `isLoading`, the `if isLoading { ProgressView() }` branch, and the `showSpinner` parameter on `reload(...)`. Layout now transitions empty-state → charts directly without the dramatic spinner-only intermediate frame. **(3) P-issue-3 — scroll-position re-entry flicker** — SwiftUI `.task` cancels on view disappear and re-fires on every re-appear, so every Activity-tab re-entry re-ran `reload()` even when the snapshot was identical. The redundant `entries = await store.snapshot(...)` triggered a Swift Charts re-render of the bar / heatmap / daily-totals views, and the underlying NSScrollView under `.formStyle(.grouped)` briefly resettled — owner reported "the previous scroll position flashes for a few frames before the live render takes over" on every tab re-entry (showed bottom area when previously scrolled to bottom; showed retention region when previously at top). Gate first-time load with `@State private var hasLoaded = false`; subsequent re-entries skip reload. Incremental updates flow exclusively through the `.onReceive(.activityLogDidAppend)` notification listener and the retention `.onChange`. Owner-confirmed flicker eliminated post-fix. **Patterns NEW (4)**: (a) **`os.Logger.info` requires `log stream --info` to surface** — info-level logs are filtered by default; without the flag a working closure looks indistinguishable from a non-firing one. **First diagnostic step when an action closure logs nothing: re-run the stream with `--info --debug`** before suspecting the closure itself. (b) **subsystem case sensitivity in `log stream` predicates** — `com.parkbyeongjun.Latte` (capital L) and `com.parkbyeongjun.latte` (lowercase l) are different subsystems for filtering purposes; verify the actual subsystem string from `LatteLog.subsystem` against the filter. (c) **`@State` initial values that drive a layout branch produce a guaranteed flicker frame on every view (re-)creation** — `isLoading = true` initial render → reload await → false render is two distinct visual states even when the await is sub-frame-fast, because SwiftUI commits at least one frame between state mutations. If the initial state branch's layout differs dramatically from the post-load layout, owner perceives flicker. Remove the loading-only branch and render the entries-driven layout from frame 0; live updates flow through notifications. (d) **`.task { await reload() }` re-fires on every tab re-entry** — TabView (macOS) cancels `.task` on disappear and restarts on appear; if reload writes to `@State` even when data is unchanged, every re-entry triggers a SwiftUI body re-evaluation that can resettle NSScrollView under `.formStyle(.grouped)`. Gate first-time load with a `hasLoaded` flag; rely on `.onReceive` for live updates. **Deferred to next session (P-issue-4)**: cold-start flicker — first-ever Activity tab entry after launching the app briefly shows the entries-empty chart-placeholder state ("Trigger fires will appear here") for 1-2 frames before the snapshot resolves and charts populate. Likely fix path: hoist `entries` to a parent-owned cache (AppEnvironment or a SettingsRoot @State) so the snapshot is preloaded before ActivityTab is first rendered. Out of scope for S24 — owner classified as next-session work. | `Sources/Core/ActivityLogExporter.swift` (DELETED), `Sources/UI/Settings/ActivityTab.swift`, `Tests/ActivityLogExporterTests.swift` (DELETED) | 🟢 Done |
| 22 | **S23 / Continued owner-driven manual smoke — Step 6 region 6 (Recurring presets) + Step 7 (Settings 4-tab light + dark)** *(2026-05-03 → 2026-05-04)* | Picked up A1 (Recurring Quick presets) → A3 (Settings 4-tab) per S22 handoff. **Five P-issues** total, **3 commits** (3 fix). Tests 570 → 581 (+11). Smoke 22/22. **(1) P-issue-1** *(`a82fd86`)* — `RecurringQuickPreset.minutes(from:)` `.rounded()` → `.rounded(.up)` so `endsAt` never lands in the previous minute (owner-reported: "Until 0:00 AM" preset showed "11:59 PM" caption). Same change to dead-but-tested `QuickPreset.minutes` for parallelism. +1 regression. **(2) P-issue-2** *(`d4a9647` batched)* — Recurring preset clicks routed checkmark to Custom row. New `@Published activeRecurringPresetID: UUID?` on `AwakeManager` + `activate(for:reason:fromRecurringPreset:)` overload (default-nil = clear-marker semantic). `MenuBarRoot` ANDs `activeRecurringPresetID == nil` into Custom row's `isActive`. +5 lifecycle tests. **(3) P-issue-3** *(same commit)* — Owner-reported via screencap: stripe `Rectangle().fill(...)` HStack-child rendered as 0px on `DurationPickerRow` + `CustomDurationRow` despite source-equivalent code rendering correctly on `RecurringQuickPresetRow`. Refactored stripe to `.overlay(alignment: .leading) { Capsule().fill(accent).frame(width: 3) }` across all 3 row types — drawn outside HStack layout. Pure UI fix, no unit-testable surface. **(4) P-issue-4** *(`3c61efc` batched)* — Retention `Stepper` UX disorienting (every +/- click flipped `isLoading=true`, collapsing chart sections, form re-laid out twice). Replaced with `.menu` `Picker` (6 preset windows: 1d / 1w / 2w / 1mo / 2mo / 3mo, all within `retentionDayRange = 1...90`); Picker `Binding(get: nearestPreset(to:), ...)` maps legacy stored values. Plus `reload(showSpinner: Bool = true)` opt-out so retention-change re-fetch keeps charts rendered while snapshot swaps atomically. +5 helper tests. **(5) P-issue-5** *(same commit)* — Activity Export CSV/JSON buttons unresponsive. Root cause: `ExportButtons` wrapper rendered both inside an `HStack` as a single Form Section row; SwiftUI Form Section row tap-handling can swallow nested Button events. Inlined as 2 separate Section rows + `.buttonStyle(.bordered)` + diagnostic `LatteLog.activity.info` at click/panel/file-write boundaries. **Patterns NEW (10)**: round-up-for-minute-boundary-targets / marker-indirection-via-published-state / default-nil-param-as-clear-semantic / SwiftUI-HStack-child-Rectangle-rendering-unreliable / source-equivalent-≠-render-equivalent (supersedes S22's `manual-smoke-falsifies-source-inspection` for SwiftUI specifically) / isLoading-not-for-warm-refresh / preset-Picker-over-Stepper-for-period-ranges / Form-Section-row-swallows-HStack-Button-events / diagnostic-logging-at-3-boundaries / xcodegen-drift-on-new-test-files. | `Sources/Core/{AwakeManager,RecurringQuickPreset,QuickPreset}.swift`, `Sources/UI/MenuBar/{MenuBarRoot,DurationPickerRow,CustomDurationRow,RecurringQuickPresetRow}.swift`, `Sources/UI/Settings/ActivityTab.swift`, `Tests/{AwakeManagerTests,RecurringQuickPresetTests,RetentionPickerTests}.swift` | 🟢 Done |
| 21 | **S22 / Resumed owner-driven manual smoke — Step 6 cup/stroke/noir + popover deferral + pause/Turn-off semantics rework + observer-sync + snooze-bypass + cross-trigger reemit parity** *(2026-05-02 → 2026-05-03)* | Step 6 re-checked after S20 P1+P2. **Ten P-issues** total — three light-mode visual + popover keybinding (deferred) + pause-all snooze caption + P5b reemit + P6 UX redesign + P6b observer-sync + P6c snooze-bypass + **P6d cross-trigger parity** (P5b auto-recovery only worked for AppTrigger; Calendar/WiFi/Schedule/ExternalDisplay inherited the empty `reemitCurrentVote()` default — pause OFF wouldn't wake them even with steady-state condition still holding). Twenty-two-commit chain. **(1) P-issue-1 — light-mode cup body re-fix** *(`ebbd04a`)*: cup `(0.42, 0.32, 0.20)` had near-identical luminance to default Espresso liquid `(0.42, 0.24, 0.08)`, gap=0.067 — cup and liquid blended into one brown mass with the 1px foam line reading as coffee-on-coffee boundary. Mental model was wrong: a mug is a **light vessel holding dark coffee**, body must sit **between** liquid and popover background brightness-wise. Fix: cup light → `(0.86, 0.80, 0.72)` warm tan. Replaced obsolete single-sided `cup avg ≤ 0.55` ceiling test with two inter-layer gap tests: `testCupLightModeBrighterThanDefaultLiquidByMargin` (cup ≥ liquid + 0.30) + `testCupLightModeDarkerThanPopoverBackground` (cup ≤ 0.85). 554 → 555 tests. **(2) P-issue-2 — light-mode stroke for handle visibility** *(`69d8927`)*: after P-issue-1 fix, owner reported outline visible but **handle barely visible** in light mode. Both used `labelColor.alpha(0.55)` but closed body outline reads from 4-way edge cues while open handle curve floating in popover background perceptually washes out at alpha 0.55. Fix: `Theme.Colors.cupStroke` becomes a dynamic NSColor — light mode resolves to solid `(0.36, 0.20, 0.09)` coffee brown alpha 1.0 (strong warm dark line ties visually with brand `coffee`), dark mode keeps `labelColor.alpha(0.55)` (preserves v1.x soft creamy outline). Two regression-pin tests: `testCupStrokeLightModeIsOpaqueAndDark` (alpha 1.0 + avg ≤ 0.30) + `testCupStrokeDarkModeMatchesLegacySoftAlpha` (alpha 0.55). 555 → 557 tests. **(3) P-issue-3 — noir accent light-mode visibility** *(`e4b340e`)*: owner reported Noir liquid rendered as solid black against the new warm tan cup body. Previous `(0.20, 0.20, 0.20)` was darker than every other accent (espresso=0.247, mocha=0.36, etc.) and indistinguishable from pure #000. Fix: noir light → `(0.32, 0.32, 0.32)` clear charcoal — still the darkest accent but visibly grey not black; dark `(0.55, 0.55, 0.55)` unchanged. New regression test `testNoirAccentLightModeIsCharcoalNotBlack` (avg ≥ 0.28). 557 → 558 tests. Smoke 22/22 PASS. **(4) P-issue-4 — popover ⌘, / ⌘Q (attempted, owner-confirmed non-functional, reverted)** *(`272970b` → `378b614`)*: self-discovered during the manual-smoke checklist preparation by source-grep: the popover footer "Settings…" and "Quit" buttons were plain `Button`s without `.keyboardShortcut(...)` modifiers, and Latte's `LSUIElement` + `.accessory` activation policy provides no standard application menu fallback. Initial fix added `.keyboardShortcut(",", modifiers: .command)` + `.keyboardShortcut("q", modifiers: .command)`. **Owner manual verification revealed the modifiers don't actually fire** in the NSStatusItem popover context — even when the popover is the key window, its NSWindow doesn't appear to enter the SwiftUI responder chain for keyEquivalent dispatch. Owner classified as non-critical (mouse-click still works); reverted to keep the source matching reality. Doc-comment in `MenuBarRoot.swift` records the attempt + the deferral rationale + future revisit options (`NSEvent.addLocalMonitor` on popover-show, or a future macOS API change). **(5) P-issue-5 — pause-all from `.awakeTriggered` painted "Until X" caption + didn't auto-recover** *(`895947a`)*: while `AppTrigger` was awake (Notion running), toggling Pause triggers ON emptied the cup but left a misleading "Until 12:27 AM" caption visible; toggling OFF didn't auto-recover. Two root causes — (a) `enforceConstraintsIfNeeded` routed pause-all through `process(.userDeactivate)`, and `(.awakeTriggered, .userDeactivate) → enterSnoozed` set `endsAt = now+5min` and `activeReason = .user`, which `HeaderView`'s `case .user` rendered as "Until X"; (b) post-pause `.snoozed` lockout held for 5 min, and `AppTrigger` doesn't naturally re-fire for steady-state conditions ("Notion still running"). Two-part fix — (a) new `AwakeInput.constraintDeactivate` + helper `constraintDeactivateResult(leavingState:)` that goes directly to `.asleep` clearing `pendingVotes` (constraint forbids replay), with `enforceConstraintsIfNeeded` dispatching it instead of `.userDeactivate`; (b) `triggersPaused` `didSet` posts `.latteTriggerPauseDidLift` on true→false transition, `TriggerCoordinator` subscribes and calls `reevaluateAll()` which invokes new `Trigger.reevaluateWatched()` (default empty impl, all 5 concrete triggers already implement it from S7.9 / S9c). 3 new tests pin: caption-clear-on-pause, lift-notification-on-unpause, no-lift-on-pause-ON. **Patterns established**: (a) **inter-layer contrast tests** — when a colour exists primarily to contrast against another rendered element, contract test must encode the **luminance gap between the two**, not a single-sided ceiling on either; (b) **mug-and-coffee mental model** — vessel body sits between contents and background brightness-wise; (c) **closed-shape vs open-curve perceptual asymmetry** — same alpha-blended stroke reads stronger on a closed bounded shape than on a free-floating curve, so an alpha that works for outline can hide a handle/curve; light-mode strokes default to solid alpha 1.0; (d) **floor-test against pure black** for accent palettes — perceptually any RGB with avg < 0.25 reads as solid black against most cup bodies; (e) **LSUIElement popover keybinding completeness ≠ keybinding functionality** — source-inspection during popover UX checklist prep can flag missing `.keyboardShortcut(...)` modifiers, but only owner manual smoke can confirm they actually fire in the NSStatusItem popover context (P-issue-4 found and reverted because they don't); (f) **constraint-driven deactivate ≠ user-driven deactivate** — system-level forces (pause-all, AC unplug) must NOT inherit the user-deactivate transition table, because the user-flavoured `.snoozed` path paints a misleading "Until X" caption; new `AwakeInput.constraintDeactivate` exists for this; (g) **constraint-lift hooks for stateless re-evaluation** — when a constraint that suppressed triggers is lifted, the coordinator must explicitly ask each trigger to re-emit; steady-state conditions (Notion still running) don't naturally re-fire. **(6) P-issue-5b — pause-lift auto-recovery still failing + Turn off caption** *(`c4703db`)*: after the P-issue-5 ship, owner manual-tested again. Pause OFF still didn't auto-recover the cup; additionally, Turn off pressed while AppTrigger was voting ON painted the same misleading "Until X" caption. Two distinct root causes. (a) `TriggerCoordinator.reevaluateAll()` called `trigger.reevaluateWatched()`, but `AppTrigger.reevaluateWatched()` short-circuits on watched-set no-diff (its design intent is the Settings UI list-edit path, idempotent on no-change). Pause-lift doesn't change the watched set, so the trigger silently exited without emitting. Fix: new `Trigger.reemitCurrentVote()` protocol method with default empty impl; `AppTrigger.reemitCurrentVote()` refreshes `matchingRunning` from the system and `emitOn()` when non-empty (no emission when condition not met — manager is asleep, redundant OFF would be noise); `TriggerCoordinator.reevaluateAll()` switched from `reevaluateWatched()` to `reemitCurrentVote()`. (b) `HeaderView.statusSubtitle` `case .user` returned "Until X" whenever `endsAt != nil` regardless of `isAwake`. The `.snoozed` state (entered via `.userDeactivate` from `.awakeTriggered`, e.g. Turn off while a trigger is voting ON) carries `activeReason=.user + endsAt=now+5min` even though `isAwake=false` — caption painted "Until 12:27 AM" against an empty cup. Fix: gate every "Until …" branch on `isAwake == true` first; show "Tap the cup to wake your Mac" otherwise. Same gate keeps us correct even if a future caller re-introduces a similar `.userDeactivate → .snoozed` path. 561 → 563 tests (+2 AppTrigger reemit-current-vote contract tests; HeaderView change is UI-only). Smoke 22/22 PASS. **Pattern added**: (h) **two-layer dedup for trigger re-evaluation hooks** — the same surface needs two separate methods. `reevaluateWatched()` is idempotent on watched-set no-diff (Settings UI path). `reemitCurrentVote()` bypasses dedup and emits current state (constraint-lift / coordinator-driven path). Conflating the two breaks one of the two callers. (i) **HeaderView captions must gate `isAwake` before reading `endsAt`** — endsAt is published for both awake-deadline (.awakeUserTimed) AND not-awake-deadline (.snoozed, .coolingDown) states. The "Until X" phrasing is only correct for the awake-deadline subset. Other concrete triggers (Calendar, WiFi, Schedule, ExternalDisplay) inherit the empty `reemitCurrentVote()` default for now — owner only reported the AppTrigger symptom; they can be implemented case-by-case if reports surface. **(7) P-issue-6 — Turn off becomes "big red button" (UX redesign)** *(`d78fca3`)*: owner-requested redesign after manual smoke surfaced that even with the P5b caption fix, the underlying behaviour was still confusing — Turn off pressed while AppTrigger was voting ON left the trigger enabled and 5 min later (after snooze expired) the cup auto-recovered. Owner's mental model: pause-all is for temporary suspension (triggers stay enabled, P5+P5b auto-recovery applies), Turn off should be the explicit termination — kill the cup AND disable all triggers; user re-enables in Settings to resume. Implementation: new `Notification.Name.latteUserExplicitDeactivate` posted by `AwakeManager.deactivate()` / `toggle()` on awake→not-awake transitions only (no post from asleep, no post on toggle-asleep→awake). `TriggerCoordinator` subscribes (mirror of `.latteTriggerPauseDidLift` pattern) and calls new `disableAll()` which flips `trigger.isEnabled = false` (setter persists to UserDefaults so Settings UI reflects state) + calls `stop(_:)` (halts emissions + emits OFF vote clearing AwakeManager's shadow set). Pause-all explicitly NOT affected — its `didSet` continues to dispatch `.constraintDeactivate` (P5) without posting the new notification, preserving the temporary-suspend semantic with reemitCurrentVote auto-recovery (P5b). 563 → 566 tests (+3): testTurnOffDisablesEnabledTriggers (golden path), testPauseAllDoesNotDisableTriggers (regression guard), testDeactivateFromAsleepIsNoOpForTriggerEnableState (no-op when not awake). UX cost accepted: re-enabling triggers requires one Settings click — owner traded the friction for the clarity. **Patterns added**: (j) **two distinct off-paths for distinct mental models** — temporary (pause, no disable, auto-recovery) vs explicit (Turn off, disable, manual re-enable). Conflating them into a single FSM transition forces a confused middle ground (the original `.userDeactivate → .snoozed` semantics). Each path needs its own notification + observer wiring; `triggersPaused.didSet` and `deactivate()` / `toggle()` produce different signals downstream. (k) **`isEnabled` setter as the persistence boundary for trigger state** — flipping `trigger.isEnabled = false` programmatically is equivalent to the user toggling it OFF in Settings: same UserDefaults key, same UI reflection, same observer chain. New code that needs to "kill" a trigger should use this rather than introducing a parallel disable mechanism. **(8) P-issue-6b — observer sync + Settings UI refresh** *(`55c2c78`)*: owner reported during retest that even with P6 shipped, the trigger toggle in Settings → Triggers still showed ON after Turn off. Two defects. (a) `TriggerCoordinator`'s notification observers were registered with `queue: .main`, which dispatches the block to OperationQueue.main and runs it on the next run-loop iteration. Test environment spins the run loop between assertions (so unit tests passed); the live app's popover-dismiss + Settings-show transition can defer the queued block past the moment Settings reads `trigger.isEnabled` for the first time. Switched to `queue: nil` so the block runs synchronously on the posting thread (`AwakeManager.deactivate()` and `triggersPaused.didSet` are both @MainActor, so `MainActor.assumeIsolated` continues to hold). (b) `TriggerSection`'s `@State private var isOn` is initialized once from `trigger.isEnabled` at view creation; when `disableAll()` flips UserDefaults programmatically, the local @State stays at the old value until the view is re-created. If Settings is open at the moment of Turn off, the toggle visibly lies. Fix: `.onReceive(NotificationCenter.default.publisher(for: .latteUserExplicitDeactivate))` re-reads `trigger.isEnabled` into `isOn`. 566 → 567 tests (+1 new integration test using a real `AppTrigger` to verify the protocol-level `trigger.isEnabled = false` write reaches the underlying `SettingsStore` — `MockTrigger` has its own non-persisted isEnabled storage and wouldn't have caught a write regression). **Patterns added**: (l) **NotificationCenter `queue: .main` is a deferral** — even from the main thread, scheduling on OperationQueue.main runs the block on the next run-loop turn; UI navigation between post and observer can leak past it. Use `queue: nil` for "must run before any subsequent UI read" semantics, with `MainActor.assumeIsolated` for actor safety when posters are @MainActor. (m) **`@State` initialized from a non-observable model is a stale-cache hazard** — when programmatic writes to the underlying model bypass the SwiftUI binding (e.g. `trigger.isEnabled = false` from a coordinator), the @State retains the previous value until view re-creation. Add an `.onReceive` for the relevant change notification to re-sync. (n) **Mock-backed tests can hide model-vs-store regressions** — `MockTrigger` stores `isEnabled` as an instance property; `AppTrigger.isEnabled` setter persists through `SettingsStore`. Tests using only mocks pass even if the protocol-level write doesn't reach the store. Add at least one integration test using the production trigger type whenever the contract involves persistence. **(9) P-issue-6c — snooze bypass for user-explicit deactivate** *(`bc13880`)*: owner reported 5.5 — after Turn off (P6 working: trigger toggle correctly OFF in Settings) + re-enable trigger in Settings + matching app still running → **cup stayed asleep**. Root cause: `AwakeManager.deactivate()` dispatched `.userDeactivate`, which from `.awakeTriggered` ran `enterSnoozed(...)` and locked the manager in `.snoozed` for 5 min. The subsequent `(.snoozed, .triggerVoteOn)` transition records the vote into the shadow set without changing state — vote ON had no effect. Snooze was originally a re-fire suppressor for "user said no, don't immediately reawake from same trigger"; with P6's `disableAll()` already disabling every enabled trigger on the same transition, the snooze became moot but still suppressed legitimate re-enable wakes. Fix: `deactivate()` and `toggle()`-when-awake now dispatch `.constraintDeactivate` instead. The helper from P5 goes directly to `.asleep`, clears pendingVotes, releases assertion, cancels timers. Two existing FSM tests (`test83_userSnoozesDuringCall_shadowSetPreserved`, `test85_userForceOffDuringCooling_goesSnoozed`) encoded the v1 snooze contract; revised to match the post-P6c expectation. New regression test `testReenableTriggerAfterTurnOffWakesImmediatelyWhenConditionHolds` covers the owner-reported 5.1 → 5.5 flow with a real `AppTrigger`. 567 → 568 tests. **Pattern added**: (o) **dead invariants from earlier-design fix-paths** — when a redesign (P6 disableAll) covers the same scenario as an earlier safeguard (snooze-on-userDeactivate), the safeguard becomes dead weight that can actively block the new design. Audit related FSM transitions for invariants the new design has subsumed and remove them. **(10) P-issue-6d — cross-trigger reemit parity** *(`0bf0b12`)*: owner-asked verification surfaced that P-issue-5b's pause-lift auto-recovery only worked for AppTrigger; Calendar/WiFi/Schedule/ExternalDisplay all inherited the protocol's empty `reemitCurrentVote()` default, so pause OFF wouldn't wake the cup even with steady-state condition still holding. Fix: implemented `reemitCurrentVote()` on each — same dedup-bypass pattern as AppTrigger, just clearing each trigger's per-state-machine dedup variable (Calendar `activeEventIDs = []`, WiFi `lastVote = nil`, Schedule `activeEntryID = nil`, ExternalDisplay `lastVote = nil`) before re-running the existing evaluate/poll function. Contract tests added on Calendar + WiFi; Schedule + ExternalDisplay rely on the identical pattern + smoke harness coverage. 568 → 570 tests. **Pattern added**: (p) **protocol default ≠ trigger-agnostic feature** — extending a protocol with a default empty impl makes the *call* trigger-agnostic but the *behaviour* still has to be implemented per type. When shipping a feature whose contract requires non-default behaviour from every concrete impl, audit the inheritance map at fix time and either implement everywhere or document the partial coverage. | `Sources/UI/Theme/{Theme,CoffeeAccent}.swift`, `Sources/UI/MenuBar/{MenuBarRoot,HeaderView}.swift`, `Sources/UI/Settings/TriggersTab.swift`, `Sources/Core/AwakeManager.swift`, `Sources/Triggers/{Trigger,AppTrigger,CalendarTrigger,WiFiTrigger,ScheduleTrigger,ExternalDisplayTrigger,TriggerCoordinator}.swift`, `Tests/{ThemeAppearanceTests,PowerSourceAndConstraintsTests,AppTriggerTests,CalendarTriggerTests,WiFiTriggerTests,TriggerCoordinatorTests,AwakeManagerTests}.swift` | 🟢 Done |
| 20 | **S21 / Autonomous post-S20 polish — 15th simplify-pass (NSAppearance.isDarkAqua extraction)** *(2026-05-02)* | Single-commit autonomous polish session. After the S20 ship cleared the queue (Apple Dev Program awaiting approval, owner manual smoke not yet resumed), a fresh code-reviewer pass over the post-S20 surface area found 1 MED + 3 LOW. Only the MED was worth shipping: the `appearance.bestMatch(from: [.aqua, .darkAqua]) == .darkAqua` predicate appeared verbatim in three places — `Theme.foam` line 31, `Theme.cup` line 44, `CoffeeAccent.color` line 93. Three-copy DRY violation. Added `extension NSAppearance { var isDarkAqua: Bool { bestMatch(...) == .darkAqua } }` at the top of `Theme.swift`; all three providers now read `appearance.isDarkAqua` directly. Future appearance-name set changes (e.g. accessibilityHighContrast variants) become a one-line edit. Pure refactor — no behavior change, 0 test Δ. **Audits run alongside but no actions taken**: dead-code grep confirmed `QuickPreset` enum + `QuickPresetRow` view remain intentionally retained per their S19 doc-comments (`QuickPreset` for `QuickPresetTests` regression coverage, `QuickPresetRow` for "API stability until v2.0"); test-fixture duplication noted (4 files each carry their own UTC `Calendar` fixture) but extraction marginal; ROADMAP/v2-backlog/MEMORY.md staleness check confirmed all S20 ship state captured correctly. Smoke 22/22 verified post-refactor in ~6:30 — no behavior regression. **Patterns reinforced**: standing simplify-pass ritual now 15 passes deep (S10 → S21); `NSAppearance.isDarkAqua` is the canonical isDark probe going forward — use it in any new dynamic-NSColor providers from S22 onward. | `Sources/UI/Theme/{Theme,CoffeeAccent}.swift` (refactor only) | 🟢 Done |
| 19 | **S20 / Second owner-driven manual smoke — About-tab tab-bar overflow + light-mode coffee cup visibility (14th simplify-pass)** *(2026-05-02)* | Same-day continuation of S19. Three-commit chain. (1) **P1 fix — Settings window `minHeight` 360 → 420** *(`97e6a1f`)* — owner reported during step 7 manual smoke that the tab bar disappeared and other tabs became unreachable when entering the About tab on a stock 460×360 Settings window. About content (hero card ~196pt + statusCard ~112pt + spacers + footer + padding) totalled ~386pt minimum vs ~332pt available content area (after ~28pt tab bar), so the greedy `Spacer()` compressed to negative and content rendered over the tab bar. Extracted `SettingsRoot.minWindowWidth` (460) + `minWindowHeight` (420) as static `CGFloat` constants referenced by both `SettingsRoot.body` `.frame(...)` and `SettingsWindowController.setContentSize`. Window remains user-resizable above the floor. NEW `Tests/SettingsRootLayoutTests.swift` (2 tests): `XCTAssertEqual` two-way contract pin on both constants. 548 → 550 tests (+2). (2) **P2 fix — dynamic `Theme.Colors.cup` + `foam` for light-mode visibility** *(`654c847`)* — owner reported during step 6 popover smoke that both the cup outline AND the rising steam particles were nearly invisible in light mode. Root cause: `Theme.Colors.cup` (`(0.95, 0.95, 0.97)`) and `foam` (`(0.96, 0.93, 0.85)`) were hard-coded near-white creamy values tuned for dark popover/Settings backdrops. Converted both to dynamic `NSColor` resolving via `appearance.bestMatch(from: [.aqua, .darkAqua])` — pattern matches the existing `CoffeeAccent.color` provider. Dark mode preserves legacy creamy palette (no regression). Light mode uses deeper cappuccino tones — cup `(0.42, 0.32, 0.20)` (rich brown body), foam `(0.78, 0.68, 0.50)` (warm tan steam, lighter than cup so foam stroke still reads on liquid layer). NEW `Tests/ThemeAppearanceTests.swift` (initially 6, simplified to 4): legacy dark-mode pinning + light-mode luminance ceilings (cup avg ≤ 0.55, foam avg ≤ 0.7). 550 → 556 tests (+6). (3) **14th simplify-pass** *(`fd5a5bc`)* on the 2 fix commits returned APPROVE-WITH-NITS — 0 CRIT/HIGH/1 MED/2 LOW, all addressed: MED-1 `XCTAssertGreaterThanOrEqual` → `XCTAssertEqual` for two-way height contract (matches the existing width test); LOW-1 `foam` + `cup` providers extracted `let isDark = ...` to mirror `CoffeeAccent.color` exactly; LOW-2 deleted two redundant `XCTAssertNotEqual` tests strictly subsumed by the legacy-pinning + luminance-ceiling pair. 556 → **554 tests** (-2). **Patterns established**: SwiftUI greedy-Spacer overflow risk in fixed-height windows (extract minWindowHeight as named constant + assert with equality test); dual-mode dynamic `NSColor(name:)` pattern for hard-coded brand colors that need dark/light variants (preserve legacy values in dark, derive contrasting deeper tones for light); luminance-average proxy as a contrast smoke test for hard-coded colors. | `Sources/UI/Settings/{SettingsRoot,SettingsWindowController}.swift`, `Sources/UI/Theme/Theme.swift`, `Tests/{SettingsRootLayoutTests,ThemeAppearanceTests}.swift` | 🟢 Done |
| 18 | **S19 / Owner-driven manual smoke + C-7 seed-then-mutable redesign + UX polish (12th + 13th simplify-pass)** *(2026-05-02)* | First owner-driven manual smoke session after the autonomous backlog drain. Six-commit chain: (1) **Wi-Fi caption non-breaking hyphen fix** *(`11789c3`)* — `Pause triggers` caption "Ignore Calendar / App / Wi-Fi / Schedule votes." was wrapping mid-word at the hyphen ("wi-/fi"). SwiftUI treats `-` as a soft break point — replaced with `\u{2011}` (U+2011 NON-BREAKING HYPHEN) so wrap falls on whitespace boundaries. UI-only, 0 test Δ. (2) **C-7 built-in QuickPreset seed-then-mutable migration** *(`7c97c60`)* — owner-reported in step 6 manual smoke: hard-coded `QuickPreset.allCases` enum cases (until 5 PM / 11 PM / midnight) waste popover space for users who never use them. Redesigned: `RecurringQuickPreset.builtinSeeds()` returns the three legacy rows with stable hard-coded UUIDs (replay-safe writes), every-day weekdays (`{1...7}`), matching target hours 17/23/0 (midnight uses hour 0 — `nextOccurrence` rolls forward). `seedBuiltinPresetsIfNeeded(in:)` migration handles three launch-time cases: fresh install (sentinel false + empty list → seed), v1.7 upgrader (sentinel false + non-empty → preserve, set sentinel only), returning user (sentinel true → guard returns immediately). New `SettingsKey.didSeedBuiltinPresets` is the **only** state distinguishing "never migrated" from "user emptied list". 538 → 547 tests (+9 RED→GREEN). (3) **Popover wiring — drop hard-coded ForEach, recurring only** *(`efb134e`)* — `MenuBarRoot.swift` drops `ForEach(QuickPreset.allCases)` and the separate built-in `VStack`. Recurring section now wraps in `if !activePresets.isEmpty` — owner-cleared state shows a clean popover (no empty divider, no zero-row VStack). `AppEnvironment.init` runs `seedBuiltinPresetsIfNeeded` BEFORE the recurring list read so the @Published mirror picks up seeds on first launch. `QuickPreset` + `QuickPresetRow` deprecated via doc-comment, retained for API stability + existing `nextOccurrence` / `minutes` regression tests. 547 tests still pass. (4) **12th simplify-pass** *(`c4c9894`)* on `7c97c60+efb134e` returned APPROVE — 0 CRIT/HIGH/1 MED/2 LOW, all addressed: MED-1 `seedBuiltinPresetsIfNeeded` doc said "Two cases" but §11 lists three (reworded to "Three launch-time cases"); LOW-1 alignment whitespace on builtinSeed*ID constants normalised; LOW-2 `testMigrationDoesNotResEEDAfterUserDeletedAllPresets` casing typo renamed. Bonus regression test pinned the third launch-time case (sentinel-true + non-empty → no-op). 547 → 548 tests (+1). (5) **Delete button in preset edit sheet** *(`c7d734c`)* — owner-reported: right-click Delete on the General list is too easy to miss; users tap a row to edit and never discover the context menu. Added bottom-leading destructive Delete button to `RecurringQuickPresetSheet`; only renders in edit mode (`onDelete != nil`). Confirmation dialog ("Delete <label>? This preset will be removed from the popover.") protects against accidental click on the already-`.destructive` button. Right-click Delete on list rows preserved unchanged. UI-only, 548 tests. (6) **13th simplify-pass** *(`992a161`)* returned APPROVE-WITH-NITS — 0 CRIT/HIGH/1 MED/2 LOW: MED-1 raw user label interpolated into confirm dialog title — added `confirmTitleLabelCap = 40` + `displayLabelForConfirm` computed var (trims whitespace, falls back to "this preset" for empty, ellipsis-truncates >40 chars); LOW-1 explicit init kept (memberwise-redundant nit deferred); LOW-2 `if onDelete != nil` style accepted. 548 tests still pass. **Patterns established**: U+2011 non-breaking hyphen for SwiftUI wrap-control on tight captions; stable hard-coded UUIDs for replay-safe seed migrations; sentinel-gated migration with three launch-time cases distinguishing "never ran" from "user emptied list"; bottom-leading destructive sheet button + confirmationDialog pattern for unrecoverable user-data actions. | `Sources/Core/{RecurringQuickPreset,SettingsStore,QuickPreset}.swift`, `Sources/UI/MenuBar/{MenuBarRoot,QuickPresetRow}.swift`, `Sources/UI/Settings/RecurringQuickPresetEditor.swift`, `Sources/App/AppEnvironment.swift`, `Tests/{RecurringQuickPresetTests,SettingsStoreTests}.swift`, `docs/design/08-c7-quick-presets-paths.md` §11 | 🟢 Done |
| 17 | **S18 / C-3 live polling + chart-colour prefs + C-7 recurring presets + 11th simplify-pass** *(2026-05-01)* | (1) **C-3 live polling shipped** *(`54f7d7a`)* — `TriggerCoordinator.recordActivity` now posts `Notification.Name.activityLogDidAppend` on every vote ON / vote OFF / user-explicit stop with active vote. `ActivityTab` listens via `.onReceive` and reloads the snapshot, debounced 300 ms via a `liveReloadTask` (cancel-on-new-event coalesces bursty trigger fires). Stop with no active vote suppresses the post (no spurious refresh); nil store suppresses too. Resolves "Live polling (refresh while tab open)" deferred item in 09-c3 §10. 494 → 499 tests (+5). (2) **C-3 user-customisable Charts colours shipped** *(`67b18b3`)* — new pure `ActivityChartPalette` (triggerOrder + defaultHex + `color(for:overrides:)` + encode/decode for SettingsStore round-trip). Defaults match v1.3 palette so a user who never touches the picker sees no visual change. New `Color(hex:)` and `Color.hexString` extensions for sRGB hex round-trip. New `SettingsKey.activityChartColors` stores `[String: String]` (triggerId → "#RRGGBB"); empty map removes the key. `AppEnvironment.activityChartColors` @Published mirror. ActivityTab gains "Chart colours" Section: 6 ColorPickers + Reset (only shown when at least one override is set). 499 → 514 tests (+15). (3) **C-7 per-day-of-week recurring presets shipped** *(`0693f3a`)* — owner can define custom popover presets like "Until 6 PM, Mon-Fri". New `RecurringQuickPreset` (Codable, Equatable, Identifiable, Sendable): id / label / targetHour / targetMinute / weekdays:Set<Int>. Pure helpers: `isActiveOn(date:)`, `nextOccurrence(after:)` (today + 7 days ahead, skipping inactive weekdays), `minutes(from:)` (clamped ≥1, mirrors built-in QuickPreset). New `SettingsKey.recurringQuickPresets` + `AppEnvironment.recurringQuickPresets` @Published mirror. MenuBarRoot popover renders user presets after the built-in QuickPresets, filtered by today's weekday. New "Custom presets" Section in General Settings tab: list with tap-to-edit + context-menu delete + "Add preset…" button → sheet (TextField label + hour/minute pickers + 7 weekday chips). Pure `RecurringPresetWeekday` helper (special-cases Weekdays / Weekends / Every day / Never). 514 → 537 tests (+23). (4) **11th simplify-pass** *(`f56e10e`)* on `54f7d7a+67b18b3+0693f3a` returned APPROVE-WITH-NITS (0 CRIT/HIGH, 1 MED, 4 LOW), all 5 actionable findings addressed: LOW-1 stale `HourlyAwakeChart.triggerDomain` doc/test name fixed; LOW-2 ActivityTab live-reload Task switched from `try?+guard` to `do/catch`; LOW-3 dropped dead `userInfo` payload on `.activityLogDidAppend`; LOW-4 `Color(hex:)` whitespace-tolerance latent defect fixed; MED-1 `nextOccurrence` doc tightened. 537 → **538 tests** (+1, new whitespace-tolerance pin). | `Sources/Core/{ActivityChartPalette,RecurringQuickPreset,SettingsStore}.swift`, `Sources/Triggers/TriggerCoordinator.swift`, `Sources/App/AppEnvironment.swift`, `Sources/UI/Settings/{ActivityTab,GeneralTab,RecurringQuickPresetEditor}.swift`, `Sources/UI/MenuBar/{MenuBarRoot,RecurringQuickPresetRow}.swift`, `Tests/{ActivityChartPaletteTests,RecurringQuickPresetTests,TriggerCoordinatorActivityLogTests,SettingsStoreTests}.swift` | 🟢 Done |
| 16 | **S17 / C-7 Path A + B1.2 J + 10th simplify-pass** *(2026-05-01)* | (1) **A — C-7 Quick presets shipped** *(`7252a0b`)* — owner picked Path A from 08-spec (smallest LOC, no FSM/persistence churn, accepts the "checkmark on Custom row" UX nit per §5). Path D retired since C-3 already shipped in v1.3 + v1.5. New pure `QuickPreset` enum (`.until5PM` / `.until11PM` / `.untilMidnight`) with `nextOccurrence(after:)` + `minutes(from:)` helpers; rolls to tomorrow when target has already passed; midnight encoded as `targetHour=24` → next day's `startOfDay`; `min(1)` clamp guarantees no 0-min activation at boundary. Popover gains a 4th Section (after the existing 7 duration presets + Custom row): 3 wall-clock target rows. New `QuickPresetRow` (distinct from `DurationPickerRow` so checkmark logic stays simple — preset rows never highlight; the active "Until X PM" session lights up the Custom row instead). 485 → 494 tests (+9). (2) **J — B1.2 disabled-state UX refinement** *(`86d2556`)* — `ShortcutRecorderField` now `.opacity(0.5) + .disabled(true)` when `coordinator.isEnabled == false` (the displayed glyph was misleading — looked live, wasn't). Reset button gates on isEnabled too + `.help()` tooltip showing the default glyph. Inline secondary-fg hint "Enable the shortcut above to record a different chord." replaces the silent grey-out. The intentionally-deferred items in 07-spec §1 (per-action chords, iCloud sync, false-negative chord-reserved indicator) remain out of v1.x scope per spec. (3) **10th simplify-pass** on the 2 commits returned APPROVE-WITH-NITS (0 CRIT/HIGH/MED, 3 LOW), 2 actionable items addressed in `bd46fb8`: LOW-1 misleading test comment tightened ('target == now to ms' was wrong fixture description); LOW-5 `ShortcutRecorderField` now cancels mid-recording via `.onChange(of: coordinator.isEnabled)` when toggle flips off (avoids "Press shortcut…" lingering behind dimmed widget). 494 tests still pass. **No new tests** for J — refinement on existing covered surface (B1.2 chord-management + recorder coverage from S12). | `Sources/Core/QuickPreset.swift` (NEW), `Sources/UI/MenuBar/{MenuBarRoot,QuickPresetRow}.swift`, `Sources/UI/Settings/ShortcutRecorderField.swift`, `Tests/QuickPresetTests.swift` (NEW) | 🟢 Done |
| 15 | **S16 / V2-06 polish + C-3 deferred E — display trigger I+G+H + multi-day chart + 9th simplify-pass** *(2026-05-01)* | (1) **I shipped** *(`dc13407`)* — `DebouncingDisplaySource` decorator collapses `didChangeScreenParametersNotification` bursts into one yield per 300ms window. Seq-counter pattern (each schedule bumps `pendingSeq` + captures it; in-flight Task yields only if its captured seq still equals `pendingSeq` at flush time) replaces a cancel-and-create approach that was racy under bursts. NSScreenSource is now wrapped by default in production. 468 → 471 tests (+3). (2) **G shipped** *(`6495697`)* — DisplaySource gains `isInClamshellMode: Bool`. Detection: NSScreen.screens has at least one external but no built-in (lid closed). Vote reason becomes `"Display: Studio Display (clamshell)"` for owner debugging via About → Status / Activity → Currently active. Vote semantics unchanged — both lid states still vote awake. 471 → 473 tests (+2). (3) **H shipped** *(`0dfdf73`)* — DisplaySource gains `attachedExternalDisplays: [DisplayInfo]` (UUID + name). NSScreenSource derives stable UUIDs via `CGDisplayCreateUUIDFromDisplayID` + `CFUUIDCreateString`. New `SettingsKey.externalDisplayWhitelist` JSON-encodes the UUID list. Trigger filters via `resolveVoteState`: empty whitelist preserves v1.2 (any external matches), non-empty narrows to listed UUIDs. Settings → Triggers → External Display gains a "Match only these displays" toggle list. 473 → 477 tests (+4). (4) **E shipped** *(`4e3c548`)* — New `DailyTotalsChart` Section in ActivityTab — bar chart of total awake minutes per day across the retention window. Today's bar darker so eye reads day-over-day pattern without overlay. `DailyTotal.compute` reuses `AwakeSegment.pair → merge → split` pipeline (parallel triggers don't double-count). 477 → 482 tests (+5). (5) **9th simplify-pass** on `dc13407+6495697+0dfdf73+4e3c548` returned APPROVE-WITH-NITS (0 CRIT, 1 HIGH, 2 MED, 3 LOW), all addressed in `b7a7c72`: HIGH-1 attached-list-name-priority regression test; MED-2 `encodeStringArray([], for:)` now `remove(key)` instead of writing `[]` blob (preserves "absent == default" invariant); MED-3 `DailyTotal.compute` switched to `Calendar.date(byAdding: .day)` (DST-correct); LOW-6 debouncer proxy test extended to cover clamshell + attached list. 482 → **485 tests** (+3). | `Sources/Triggers/ExternalDisplayTrigger.swift` (DebouncingDisplaySource + DisplayInfo + isInClamshellMode + attachedExternalDisplays + whitelist API + matchedDisplays/resolveVoteState), `Sources/Core/SettingsStore.swift` (externalDisplayWhitelist + encodeStringArray empty-clear), `Sources/UI/Settings/{TriggerConfigForms,ActivityTab}.swift`, `Tests/{DebouncingDisplaySourceTests,DailyTotalTests,ExternalDisplayTriggerTests,SettingsStoreTests}.swift` | 🟢 Done |
| 14 | **S15 / C-3 polish — Activity history B+C+D+F + 8th simplify-pass** *(2026-05-01)* | (1) **F shipped** *(`0b59e67`)* — `ActivityLogStore.retention` becomes mutable via `setRetention(_:)` (shrink GCs + flushes immediately, grow no-ops). New `SettingsKey.activityRetentionDays` + `AppEnvironment.activityRetentionDays` published mirror; ActivityTab gains a Stepper (range 1…90, default 14). 442 → 450 tests (+8). (2) **B shipped** *(`6a55c02`)* — `ActivityFilter` enum (.all / .only(triggerId)) gates the entry stream into both charts. Picker domain is dynamic — only triggers actually present in the current snapshot appear. Heatmap title + frame size now follow the user's retention setting (no more hard-coded 14-day frame). 450 → 455 tests (+5). (3) **C shipped** *(`86bc5b1`)* — `ActivityLogExporter` (CSV: ISO8601 timestamps for spreadsheets; JSON: same `secondsSince1970` Double coder as on-disk log so export round-trips losslessly). NSSavePanel side effect kept at the call site so `ExportButtons` stays a pure SwiftUI primitive. 455 → 462 tests (+7). (4) **D shipped** *(`0dfb9d2`)* — Click "Currently active" row → `SettingsRoute` flips tab to `.triggers` and `ScrollViewReader` scrolls to the matching `TriggerSection`. Same path also works as a deep link `latte://settings/triggers?focus=wifi`. `SettingsURLHandler.parseRoute(_:)` returns `(tab, focusedTriggerId)`; legacy `parse(_:)` kept as a thin tab-only wrapper. Already-open Settings window re-emits a `Notification.Name.settingsRequestFocusTrigger` so a second deep link still re-scrolls. 462 → 467 tests (+5). (5) **8th simplify-pass** on the 4 commits returned APPROVE-WITH-NITS (0 CRIT/HIGH, 2 MED, 5 LOW), all addressed in `4343d2a`: MED-1 reload on retention change (`.onChange` so a grow recovers older history without a tab bounce); MED-2 `isLoading=true` at reload entry so re-fetches show the spinner; LOW-1 `HourlyBucket` O(n²) noted for future (bounded 24×6); LOW-3 DST caveat documented on `splitByHourWithDate`; LOW-4 `parse(_:)` doc clarified; LOW-5 `86_400` literal collapsed into `ActivityLogStore.secondsPerDay`; LOW-7 picker label helper extracted to `ActivityFilter.label(for:)` so kebab-case `external-display` → "External Display" without `.capitalized`'s locale dependency. 467 → **468 tests**. Smoke unchanged at 22 (file-format + owner manual step 8 still cover the user-visible flows). | `Sources/Core/{ActivityLogStore,ActivityFilter,ActivityLogExporter,SettingsStore}.swift`, `Sources/App/{AppEnvironment,LatteApp}.swift`, `Sources/UI/Settings/{ActivityTab,SettingsRoot,SettingsTab,SettingsWindowController,TriggersTab}.swift`, `Tests/{ActivityLogStoreTests,ActivityFilterTests,ActivityLogExporterTests,AppEnvironmentTests,SettingsURLHandlerTests,SettingsStoreTests}.swift` | 🟢 Done |
| 13 | **S14 / C-3 — Activity history (ActivityLogStore + ActivityTab + 7th simplify-pass)** *(2026-04-30)* | Same-day continuation of S13. (1) **Design doc 09-c3-activity-history.md** authored decision-ready (§1-§11) — owner approved defaults: actor + JSON file persistence (NOT SwiftData), 14d ring buffer, 4th Settings tab, raw `TriggerVote.reason` strings stay out of the persisted log. (2) **C-3 Part 1** *(commit `e4a401c`)* — `ActivityLogEntry` Codable value-type (id/timestamp/triggerId/kind/reasonCode) + `ActivityLogStore` actor with lazy-load + atomic write + 14-day GC + corrupt-/missing-file safety + `LatteLog.activity` logger. `TriggerCoordinator` gains optional `activityStore` injection with hooks at `handleVote` (organic ON/OFF) and `stop(_:)` (user-explicit OFF, distinct `reasonCode: .userToggleOff`). 426 → 438 tests (+12: 7 store + 1 schema privacy + 4 integration). (3) **C-3 Part 2** *(commit `c4dac06`)* — `SettingsTab.activity` 4th case + `latte://settings/activity` deep link + `ActivityTab.swift` (Charts: 24h stacked bar per-trigger colour + 14d heatmap + Currently active list + empty/loading state, macOS 13 compatible — no `ContentUnavailableView`). `AppEnvironment` instantiates real store from `ApplicationSupportDirectory/Latte/`, nil-safe on sandbox/disk failure. `ActivityLogEntry.init` quantises `timestamp` to microseconds so `secondsSince1970` Double round-trip preserves equality. 438 → 439 tests. (4) **7th simplify-pass** on `e4a401c` + `c4dac06` returned APPROVE-WITH-NITS (0 CRIT/HIGH, 2 MED, 2 LOW), all addressed in `6ef4e45`: MED-1 `triggerDomain` `"externalDisplay"` → `"external-display"` (ExternalDisplayTrigger.id is hyphen-cased — Charts was using fallback colour); MED-2 `DailyHeatmapChart.compute` was passing cross-trigger entries to single-stream pairer producing phantom segments — now pairs per-trigger then merges via new `AwakeSegment.merge` (3 regression tests); LOW-1 `stoppedByCoordinator` doc-comment marks it reserved for a future code path; LOW-2 `defaultDirectory()` `catch` now logs via `LatteLog.activity.fault` before returning nil. 439 → **442 tests**. (5) **Smoke scenario 22** *(commit `7bfa1b7`)* — `22-activity-log.sh` mirrors 21 shape: verifies file-absence on fresh launch (lazy-load contract), opens Settings → Activity tab via deep link, captures window for owner review, asserts privacy contract via `jq` if any entry happened to be written. Smoke 21 → **22 scenarios**, all PASS. | `Sources/Core/{ActivityLogEntry,ActivityLogStore,Logging}.swift`, `Sources/Triggers/TriggerCoordinator.swift`, `Sources/App/AppEnvironment.swift`, `Sources/UI/Settings/{ActivityTab,SettingsRoot,SettingsTab,SettingsWindowController}.swift`, `Tests/{ActivityLogStoreTests,TriggerCoordinatorActivityLogTests,SettingsURLHandlerTests}.swift`, `.smoke/scenarios/22-activity-log.sh`, `docs/design/09-c3-activity-history.md` | 🟢 Done |
| 12 | **S13 — 5th + 6th simplify-pass cleanup + V2-13 TriggersTab extraction + B1.2 register-failure rollback** *(2026-04-30)* | Same-day continuation of S12. (1) **5th simplify-pass** on S12's 3 commits returned APPROVE-WITH-NITS (0 CRIT/HIGH, 2 MED, 2 LOW), all addressed in `f83a13a`: `ShortcutRecorderField.keyDown` no longer calls `endRecording()` after firing `onChord` (SwiftUI commit() drives the FSM exclusively — success → isRecording=false → updateNSView reconciles by calling endRecording on the NSView; validation failure leaves both layers in recording state); redundant `nsView.needsDisplay = true` removed from updateNSView (glyph didSet already triggers display invalidation); test `controlMaskAlias` private extension (single-use) inlined to `KeyChord.ctrlMask`. **Trigger §4.4.2 audit run alongside** — all 5 default + ScheduleTrigger FULL COMPLIANCE, no fixes needed. (2) **V2-13 TriggersTab extraction** *(commit `9fe4752`)* — TriggersTab.swift was 958 LOC (>800 ceiling). 6 forms (App+AppRow / WiFi / Calendar+CalendarPickerRow / Focus / Schedule+ScheduleEntryRow+WeekdayChip / ExternalDisplay) moved to sibling `Sources/UI/Settings/TriggerConfigForms.swift` (799 LOC). TriggersTab.swift now 161 LOC (TriggersTab + TriggerSection dispatcher only). Forms changed `private struct` → default-internal so TriggerSection.configBody can dispatch across files. xcodegen auto-discovers `Sources/**/` so no `project.yml` change. 423 tests still PASS. (3) **B1.2 register-failure rollback** *(commit `b53de75`)* — closes silent-failure gap from S12: when OS rejects new chord (`kEventHotKeyExistsErr` — held by another app or system), setChord previously persisted the new chord but no Carbon registration was active (user's existing shortcut died silently). New `ChordRegistrationError` public enum + `KeyboardShortcutCoordinator.@Published var registrationError`. setChord snapshots previousChord, attempts new registration, on failure rolls back chord/persistence + re-installs prior registration + populates registrationError. ShortcutRecorderField surfaces `registrationError` as inline red copy ("⌘1 is already used by another app or macOS — pick a different combination."); commit() leaves field in recording state so user can immediately try another chord. 423 → 426 tests (+3 rollback scenarios). (4) **6th simplify-pass** on S13's 3 commits returned APPROVE-WITH-NITS (0 CRIT/HIGH, 2 MED, 1 LOW), all addressed in this commit: ShortcutRecorderField.cancel() now calls `coordinator.clearRegistrationError()` (was leaving stale red copy after Esc/click-outside); double-rollback re-register failure now logs `LatteLog.shortcut.error` instead of silently failing; TriggerConfigForms top doc-comment notes the 6 forms are intentionally default-internal. (5) **Memory consolidation** — `anthropic-skills:consolidate-memory` skill ran: 17 memory files → 7 (S1-S7 deleted as captured in design docs + git log; S8a/8b/8c → `project_latte_v1_0.md` durable summary; S9_family/S10 → `project_latte_v1_1.md`; S11_S12 + S13 → `project_latte_v1_2.md`; NEW `user_owner.md` + `project_latte_status.md` durables). MEMORY.md 9396B → 1488B / 6 lines. | `Sources/UI/Settings/{ShortcutRecorderField,TriggerConfigForms,TriggersTab}.swift`, `Sources/Core/KeyboardShortcutCoordinator.swift`, `Tests/KeyboardShortcutCoordinatorTests.swift`, `docs/{v2-backlog,design/07-shortcut-recorder}.md`, memory consolidation | 🟢 Done |
| 11b | **S12 / B1.2 — Custom keyboard-shortcut recorder + 4th simplify-pass cleanup** *(2026-04-30)* | 5 commits same-day continuation of S11. (1) **4th simplify-pass** on S11's 5 commits returned APPROVE-WITH-NITS (1 MED, 2 LOW). MED resolved (`DisplaySource.changeStream` doc said "coalesces bursts" but adapter actually forwards raw — comment rewritten to reflect that the `lastVote == wantsAwake` guard does the dedup). LOW-A resolved (test was creating a 2nd iterator on a single-consumer AsyncStream — switched to reusing the existing iterator). LOW-B (TriggersTab.swift breaches 800-line ceiling; pre-existing) tracked as new backlog V2-13. NSScreenSource debounce future-polish recorded under V2-06 entry. (2) **02-architecture §4.4.2 single-consumer AsyncStream gotcha + isRunning gate pattern** codified for future trigger authors. (3) **B1.2 SHIPPED**: `KeyChord` Codable value type + `ReservedChord` blocklist (⌘Q/W/C/V/X/Tab/Space) + `SettingsStore.keyChord` round-trip + `HotKeyRegistrar.register(chord:handler:)` + `currentChord` (legacy `register(handler:)` kept as default-impl shim) + `KeyboardShortcutCoordinator.@Published chord` / `setChord` / `resetChord` + `SettingsKey.shortcutChord` (silent-default migration). `ShortcutRecorderField` SwiftUI/NSResponder wrapper (Spotlight-picker pattern; pure SwiftUI key capture isn't reliable on macOS 13). Settings → General gains a recorder row + Reset (disabled while on default). Inline validation (`hasRequiredModifier` + reserved-chord) keeps the FSM in `recording` until valid. 409 → **423 tests** (+14: 7 KeyChord + 7 coordinator chord-management). Smoke 20 → **21 scenarios** (new 21-shortcut-recorder.sh covers silent-default migration + Settings capture; chord-customisation interaction is owner manual smoke step 6). | `Sources/Core/{KeyChord,KeyboardShortcutCoordinator,SettingsStore}.swift`, `Sources/UI/Settings/{GeneralTab,ShortcutRecorderField}.swift`, `Sources/Triggers/ExternalDisplayTrigger.swift` (MED comment), `Tests/{KeyChordTests,KeyboardShortcutCoordinatorTests,SettingsStoreTests,ExternalDisplayTriggerTests}.swift`, `.smoke/scenarios/21-shortcut-recorder.sh`, `docs/{ROADMAP,v2-backlog,SESSION_HANDOFF,design/02-architecture,design/06-display-trigger,design/07-shortcut-recorder}.md` | 🟢 Done |
| 11a | **S11 / V2-06 — External display trigger + 3rd simplify-pass cleanup** *(2026-04-30)* | 5 commits in one session. (1) 3rd simplify-pass on S10.1+S10.1.1 returned APPROVE; single LOW (em-dash in `LatteLog.awake.info` string) resolved as ASCII-hyphen. (2) **V2-06 ExternalDisplayTrigger shipped** (per `docs/design/06-display-trigger.md`): `DisplaySource` protocol + `NSScreenSource` adapter (NSScreen.screens filtered through `CGDisplayIsBuiltin`, observing `didChangeScreenParametersNotification`) + `MockDisplaySource` + the 5th default trigger (Calendar/App/WiFi/Schedule/ExternalDisplay). Vote reason "Display: <localizedName ?? External Display>" mirrors S10 friendly format. **Single-consumer changeStream pattern**: observeTask installed once at first start() and gated through start/stop cycles via `isRunning` flag — `source.changeStream` is single-pass, so cancelling the task on stop would forfeit subsequent yields and break Coordinator restart-after-stop semantics. (3) Coordinator integration: `external-display` vote OR'd with other triggers; pause-all (C-9) gate suppresses display votes (parity with the other 4 triggers). (4) Settings → Triggers → External Display section with live status row binding `trigger.externalDisplayCount` + `firstExternalDisplayName`. (5) Smoke scenario 20 covers the no-monitor branch automatically (CI hosts have no external monitor → pmset must show no Latte assertion + Settings capture for owner review). 398 → **409 tests** (+11: 9 unit + 2 coordinator); smoke 19 → **20 scenarios**. | `Sources/{App/AppEnvironment,Core/{Logging,SettingsStore},Triggers/ExternalDisplayTrigger,UI/Settings/TriggersTab}.swift`, `Tests/{AppEnvironmentTests,ExternalDisplayTriggerTests,SettingsStoreTests,TriggerCoordinatorTests}.swift`, `.smoke/scenarios/20-external-display.sh`, `docs/{ROADMAP,v2-backlog,SESSION_HANDOFF,design/06-display-trigger}.md` | 🟢 Done |
| 9f | **S10.1 — second simplify-pass on S10 + plan-only specs for v1.2 (V2-06 + B1.2)** *(2026-04-30)* | (1) Code-reviewer agent ran a second simplify-pass scoped to the 4 S10 code commits — verdict APPROVE-WITH-NITS (0 CRIT/HIGH, 2 MED, 2 LOW). MED #1 resolved: `applyActivateOnLaunchIfEnabled` now logs a `LatteLog.awake.info` line when the activate is silently blocked by an input-boundary gate (`requireACForAwake` on battery), surfacing the otherwise-mute "toggle on, no assertion" failure mode. MED #2 resolved: `AppEnvironmentTests` overrides `setUp`/`tearDown` to defensively `AwakeManager.shared.deactivate()` each test, eliminating the "if test 3 crashes, tests 4+ inherit awake state" leak path; the 3 redundant inline `deactivate()` calls in the activate-at-launch tests are now dropped. LOW #1 resolved: `LatteApp.swift` comment clarified — branch is belt-and-suspenders, not redundant. LOW #2 resolved: `19-activate-on-launch.sh` poll-loop comment now explains 8s is cold-CI headroom rather than a synchronous-call requirement. (2) Cold-start cadence note added to SESSION_HANDOFF: `xcodebuild test` and `~/dev/smoke-harness/run.sh` MUST run serially — parallel runs surface a phantom rc=137 on `03-icon-states` because the test runner's failed launch (`LSMultipleInstancesProhibited`) propagates SIGKILL to the smoke scenario's running Latte instance. Verified during S10.1 cold-start. (3) Plan-only design docs landed for v1.2 features so the next code session can RED-GREEN immediately after owner UI smoke clears: `docs/design/06-display-trigger.md` (V2-06 external-display trigger, ~4h estimate, 8 unit + 2 integration tests, smoke 20 outline, 4 owner Qs) and `docs/design/07-shortcut-recorder.md` (B1.2 custom-chord recorder, ~5h estimate, 12 unit tests, smoke 21 outline, 4 owner Qs). 398/398 tests still pass; smoke 19/19 still pass; no state machine changes. | `Sources/App/{AppEnvironment,LatteApp}.swift`, `Tests/AppEnvironmentTests.swift`, `.smoke/scenarios/19-activate-on-launch.sh`, `docs/{SESSION_HANDOFF,design/06-display-trigger,design/07-shortcut-recorder}.md`, `ROADMAP.md` | 🟢 Done |
| 10 | **App Store Connect setup + Release archive** *(post-Dev Program)* | Create App Store Connect record, paste metadata from `docs/store/`, upload screenshots, archive Release build, distribute to App Store Connect (do NOT submit yet) | App Store Connect record draft + uploaded `.xcarchive` | ⚪ Pending (after 8.5) |
| 10 | **TestFlight beta launch** | Beta build, recruit ≥10 testers, multi-OS (macOS 13/14/15) coverage via beta | TestFlight build, beta feedback log | ⚪ Pending |
| 11 | **Submission + launch** | Address beta feedback, submit to App Store, launch on ProductHunt | App Store live, ProductHunt post | ⚪ Pending |

Status legend: 🟢 done · 🟡 in progress · 🔴 blocked · ⚪ pending

---

## Milestone gates

Decision points where progress halts until a gate is passed.

| Gate | Triggered after | Pass criteria | If fail |
|---|---|---|---|
| **G1: Design sign-off** | Session 2 | Owner reviews PRD + 02 + 03 + 04, accepts all open questions resolved | Iterate on specific doc; do not start code |
| **G2: Architecture validated** | Session 3 | First module compiles; trigger protocol can be mocked in tests | Refactor architecture; revisit 02 doc |
| **G3: MVP feature-complete** | Session 5 | All Phase 1.0 + 1.A + 1.C features tickable on Launch Criteria checklist (PRD §10) | Defer non-essential to Phase 1.5 |
| **G4: Build green** | Session 6 | Tests pass on macOS 14 + 15 CI, no power-assertion leak in 24-hr soak | Fix bugs; do not submit |
| **G5: Beta confidence** | Session 9 | Crash-free ≥99% over ≥2 weeks with ≥10 testers, ≤3 P2 bugs open | Extend beta; do not submit |
| **G6: App Store approved** | Session 10 | Apple review passes | Address rejection; resubmit |

---

## User-side blocking tasks

These cannot be done by the assistant; user must complete on their own time **between sessions**.

| Task | Required by | Estimated effort | Notes |
|---|---|---|---|
| Install Xcode 15+ from App Store | Before session 6 | 30 min download + install | Required for any build |
| Run `sudo xcode-select -s /Applications/Xcode.app/Contents/Developer` | Before session 6 | 1 min | Switch from CLT to Xcode |
| Install XcodeGen (`brew install xcodegen`) | Before session 6 | 1 min | For `project.yml` → `.xcodeproj` |
| Enroll in Apple Developer Program ($99/yr) | Before session 8 | 1~2 days approval | <https://developer.apple.com/programs/> |
| Decide bundle ID prefix (e.g., `com.yourname`) | Before session 8 | — | Replaces `com.example` |
| Design app icon (DIY, hire, or AI) | Before session 8 | Variable | 1024×1024 PNG; Asset Catalog auto-scales |
| (Optional) Register `latte.app` or similar domain | Before session 8 | 10 min | $10~30/yr; not required for launch |
| Set up GitHub Pages site for Privacy Policy + landing | Session 7~8 | 30 min | Template provided in session 8 |
| Recruit ≥10 TestFlight beta testers | Before session 9 | Variable | Twitter/Reddit/personal network |

---

## Context budget per session

Each session targets:
- **Concrete deliverables**: 1~3 files written or substantially edited
- **Review checkpoints**: ≤2 owner-decision moments
- **Wall time**: 1~3 hours (depending on complexity)

Avoiding "kitchen sink" sessions keeps each handoff clean.

---

## Risk-driven schedule adjustments

If risks fire (per PRD §9), the schedule changes:

- **R-01 fires** (Amphetamine adds calendar): compress sessions 3~5, ship Phase 1.A incomplete if needed
- **R-02 fires** (App Store rejects): insert ad-hoc session for rebranding/repositioning
- **R-06 fires** (burnout): pause for ≥1 week, then resume from last completed session

---

## How to read this roadmap in future sessions

When you (the developer) start a new session:

1. Read this file to remember the multi-session plan
2. Open [docs/SESSION_HANDOFF.md](docs/SESSION_HANDOFF.md) to see exactly where the previous session ended
3. The HANDOFF doc tells you: which file to open, what the next deliverable is, and any blocking decisions

The HANDOFF doc is **always overwritten** at the end of each session. The ROADMAP is the long-lived plan.

---

## Document version

| Version | Date | Notes |
|---|---|---|
| 1.50 | 2026-05-30 | **S50 — whole-app audit (Opus 4.8) + 5 correctness/privacy/App-Review fixes (681 → 691 tests)** (session on `claude/focused-hamilton-417bfc`, continuing S49's tip; same v1.9 line). Owner upgraded to Opus 4.8 and asked for a full app review before resuming the App Store push. Ran **4 parallel review agents** (Triggers / Core+iCloud-dark+CRDT / UI+lifecycle+Intents / security+privacy) over ~11k LOC: **0 CRITICAL**, binary structurally sound; iCloud dark code + CRDT + the H1 migration default all re-verified correct; entitlements/sandbox/secrets/`latte://` input-validation clean; `LATTE_ICLOUD_SYNC` OFF in both configs. Fixed every MEDIUM+ finding, TDD per fix, full gates between each (`git -C <worktree>` throughout). **T1 `a7a0ad4` (onboarding lockout, MEDIUM, most user-facing)**: the onboarding window had a `.closable` title-bar button but only the wizard's Skip/Finish called `markCompleted()` — closing with the X bypassed it, leaving `hasCompletedOnboarding` false and (since the menu-bar item is gated on that flag) a first-run user with NO UI for the session. Wired `OnboardingWindowController` as `NSWindowDelegate` (same pattern as `SettingsWindowController`); a user close now finalizes onboarding like Skip. `close()` uses `orderOut` (no `willClose`) so the programmatic path never double-fires; `markCompleted()` idempotent. +OnboardingWindowControllerTests. **T2 `e621139` (os_log PII, MEDIUM, "No Data Collected" integrity)**: `AwakeManager` logged `String(describing:)` of `AwakeState` at `privacy: .public`; for `.awakeTriggered`/`.coolingDown` the reflected form leaked every `TriggerVote.reason` — Wi-Fi SSID, calendar event title, running-app names — into the system log. Added `AwakeState: CustomStringConvertible` exposing only the static voting trigger IDs (e.g. "wifi"), never reasons; `String(describing:)` now uses it so the call site is fixed unchanged. +AwakeStateDescriptionTests. **T3 `42949cb` (post-stop spurious ON vote, MEDIUM, trap-#8-sensitive area)**: `reevaluateWatched`/`reemitCurrentVote` on Schedule/Calendar triggers enqueue a deferred `Task`; a `stop()` landing between enqueue and run let the stale poll emit a spurious ON vote. Routed both through a new `pollOnceIfRunning()` seam that re-checks `pollTask` at execution time (+`[weak self]`). Crucially the direct `pollOnce()` test/UI seam is unchanged — many tests call it without `start()`, so guarding `pollOnce` itself would have broken them (the obvious fix; caught before applying). +DeferredPollStopGuardTests (guarded seam no-ops while stopped; direct pollOnce on the same fixture still emits → proves the gate is the only suppressor). **T4 `676ec3b` (settings encode hardening, MEDIUM)**: `encodeStringArray` wrote `setData(try? encode(value))` → an encode failure passes nil → `setData` removes the key (silent data loss). Guard the encode, preserve the existing value + `.fault` log on failure, matching `setKeyChord`. Defensive — the throw branch is unreachable via the public `[String]` API, so no new repro test; existing SettingsStore contract tests cover the reachable path. **T5 `acd2394` (Wi-Fi auth, HIGH per security agent — known owner item)**: the trigger called `requestAlwaysAuthorization()` while Info.plist declares only `NSLocationWhenInUseUsageDescription` — an entitlement/description mismatch an App Reviewer can flag, and over-privileged. Switched to `requestWhenInUseAuthorization()` (macOS resolves both identically; `permissionStatus` already accepts `.authorized`/`.authorizedAlways`). Code-only; **OWNER device-verify remains**: confirm CoreWLAN `ssid()` still resolves under When-In-Use on the target macOS before the shipped build relies on the Wi-Fi trigger. **Verification**: 691/691 PASS (first-run green, no trap #8 stall on the final run; one run mid-session hit the documented stall and `run_tests.sh` auto-recovered on attempt 2) + default & flag-on builds **0 warnings** + doc-drift `--strict` clean + store-limits clean. **NO NEW patterns** (audit + targeted fixes); cumulative NEW S20→S47 ≈ 92 unchanged. **Deferred LOW backlog** (documented in SESSION_HANDOFF, intentionally NOT churned to keep verification error-free): signal-handler `Task{}` async-signal-safety (AwakeManager — works empirically on macOS, risky shutdown-path change); `IOPowerSource` `Unmanaged.passUnretained` (singleton, practically safe); **CoreWLANSource CLLocationManager↔delegate retain cycle — note the reviewer's `deinit{delegate=nil}` fix is WRONG (the cycle prevents deinit from ever running; correct fix needs an explicit teardown from WiFiTrigger, zero functional benefit for an app-lifetime object)**; AppTrigger/FocusTrigger missing `deinit` (app-lifetime, no functional consequence); activation-policy coordination across Demo+Settings windows; chart date POSIX-locale (i18n cosmetic); dead `?? presets[0]`; ActivityTab `@MainActor` annotation consistency. **Branch tip now S36…+S49 + T1…T5**; PR #1 still **OPEN, not merged** (owner call); pushed → auto-updates. **Next track unchanged = owner-driven App Store upload** (merge PR #1 · Small Business 15% · App ID · Team ID→`DEVELOPMENT_TEAM` · app record · screenshots 2880×1800 hard-blocker · ASC paste + $2.99 + privacy + age · archive/upload/submit) + the T5 Wi-Fi When-In-Use device-verify. |
| 1.49 | 2026-05-27 | **S49 — fixed the S48 audit findings at the doc level (the Claude-Code-doable autonomous work before the App Store push)** (session on `claude/focused-hamilton-417bfc`, continuing S48's wrap tip; same v1.9 line, no version bump, **no owner-visible behaviour change** — design docs + one lint-guard edit, zero Swift). Owner confirmed the originally-planned work was the **App Store push**, but most of that is owner-gated GUI (screenshots/ASC/signing), so the directive was **"일단 클로드코드 내에서 진행가능한 작업들을 먼저"** — clear the autonomous S48 audit fixes first. All H1 + M1–M3 + L1–L5 folded into the docs; **the code needed no change — it was already correct, the docs were behind.** Worked in the worktree; pure doc edits + one bash-guard edit → **no build/test/pty needed** (doc-drift `--strict` is the only gate, clean). **`git -C <worktree>` for all git ops** after S48 exposed the Bash cwd drifting back to the repo root (main/S35) mid-session and silently mis-targeting a `git push` (caught + re-pushed correctly with `-C`). **H1 (HIGH) — `806dea3`**: the data-loss trap — shipped v1.x never writes `schemaVersion`, so `04-data-model` §2.2 step1 / §4.1 / §6.2 saying "absent → fresh → no migration" (and RFC §3 telling the implementer to adopt §2.2 "steps 1–6 unchanged") would skip migrating every real upgrading user → total settings loss. The pre-built `SettingsMigration.currentSchemaVersion` already defaults absent → `1` (correct), so the fix **aligned the docs to the code**: §2.2 step1 / §4.1 / §6.2 now "absent → treat as 1 → migrate (no-op on a fresh install)"; RFC §3 migration bullet de-contradicted vs §11; RFC doc version 0.2 → 0.3. No code change. **M1–M3 + L1–L5 — `ac51d4a`**: activation-stage gaps folded into the RFC as the activation session's checklist (S8.5-gated, specified-not-implemented): **M1 §4 B-2** named the unresolved CloudKit **zone-retention** gap (local GC does NOT delete `CKRecord`s → unbounded zone + full-history re-download per sync; the "14-day GC bounds quota" claim was false for CloudKit) → recommended **delete-on-GC + upload-within-cutoff** (default bounded), with **append-only full history flagged as an explicit owner product choice** for the Phase-3 v2.1 lock; **M2 §4 B-2** dedup on the stored `CKRecord.recordName` (= the S43 `contentAddressedID`), never re-hash downloaded fields (CloudKit field precision ≠ the tested JSON round-trip → a divergent re-hash would duplicate history); **M3 §3/§6** pin the LWW timestamp to CloudKit server `modificationDate` not a device wall-clock (skew can let an older edit win; pure resolver unchanged — only which timestamp the driver feeds it); **L1 §2.2** the migration idempotency gate must live in the store `needsMigration` reads (UserDefaults `schemaVersion`) or gate on `migrationCompleted`, else migration re-runs every launch; **L2 §2.2** noted the type-mismatch key drop (reverts to default); **L3/L4 §7** a failed sync-in leaves the Mac with no active hotkey (prior chord unregistered first) + `apply` must be gated on `keyboardShortcutEnabled` by the caller; **L5** widened the `check_doc_drift.sh` §10 CloudKit-isolation guard to also catch `import CloudKit` (the true gating signal; only `CloudKitSyncEngine.swift` imports it and it's excluded → guard stays green) — the lone code change. **No NEW patterns** (doc session); cumulative NEW S20→S47 ≈ 92 unchanged. Tests **NOT re-run** (zero Swift change; last green 681 @ S47); doc-drift `--strict` clean incl. the widened guard. Findings + resolution recorded in `project_icloud_design_audit.md` (updated S48→S49) + `MEMORY.md` index. **Branch tip now S36…+S47 + S48 wrap + `806dea3` + `ac51d4a` + this S49 wrap**; PR #1 still **OPEN, not merged** (owner call); pushed → auto-updates. **The autonomous engineering backlog is exhausted again** — H1 fully closed; M2/M3/L1 + the M1 owner choice are now RFC activation requirements but their implementation is S8.5-gated (+ macOS-14 for Phase 2). **Next track = owner-driven App Store upload** (the originally-planned work): merge PR #1 · Small Business 15% · App ID · Team ID→`DEVELOPMENT_TEAM` · app record · screenshots 2880×1800 hard-blocker · ASC paste + $2.99 + privacy + age · archive/upload/submit; + WiFi WhenInUse device-verify. |
| 1.48 | 2026-05-27 | **S48 — thorough design-verification audit of the iCloud sync design (report-only; findings to memory)** (session on `claude/focused-hamilton-417bfc`, continuing S47's tip; same v1.9 line, no version bump, **no code/runtime/design-doc change** — audit + memory + this docs wrap only). Owner intent: **defer the originally-planned work one session** and instead spend this one *verifying all the iCloud sync **design** for errors* — the one body of work pre-built "dark" that **cannot** be validated against live CloudKit until S8.5, so a design-level correctness review is the only verification available before activation. Scope confirmed to **iCloud design (RFC §10 + the 5 S41–S43 dark resolvers + their tests + a cross-check vs `04-data-model`)**, output **report-only**, findings handling **memory only** (three `AskUserQuestion` confirms incl. the session-wrap). Audited (read-only, verified against actual files not memory): `docs/design/10` (440 lines) · `CloudSyncEngine`/`SettingsLWWResolver`/`SettingsMigration`/`ChordSyncResolver`/`ActivityLogMergeResolver` + all 5 test files (638 lines) · the depended-on types (`SettingsKey`/`SettingsStore`, `ActivityLogEntry`/`ActivityLogStore`, `KeyChord`/`HotKeyRegistrar`) · `04-data-model` §2.2/§3.2/§5/§6 · the §10 lint guard + `LATTE_ICLOUD_SYNC` wiring. **Found 1 HIGH, 3 MEDIUM, 5 LOW.** **🔴 H1 (HIGH — latent full-settings-loss doc/code contradiction)**: shipped v1.x **never writes `.schemaVersion`** (grep-confirmed: only read, default 1) → a real install has `exists(.schemaVersion)==false`. The *code* handles this correctly (`SettingsMigration.currentSchemaVersion` defaults absent→**1**→migrate; RFC §11 note documents the correction), **but** `04-data-model` §2.2 step1 (~L53) / §4.1 (~L118) / §6.2 (~L374) all still say **"absent → fresh install → no migration"**, and **RFC §3 instructs the implementer to "Adopt §2.2 as written … steps 1–6 unchanged"** — contradicting §11. Following §3 + the authoritative doc at activation misclassifies every real v1.x→v2.0 upgrade as fresh → migration skipped → **all trigger settings/presets/chord lost**. RFC §13's "resolved in S38" only fixed the §5 *enum-snapshot* staleness (that IS fixed) — NOT this; the two are conflated, which is the trap. **🟡 MEDIUM (activation-stage gaps; pure logic itself correct)**: **M1** CloudKit-side retention undefined → §4's "14-day GC bounds quota" is false (local GC never deletes remote records → unbounded zone growth + full-history re-download per sync; offline "resurrection" path undefined) → needs a CloudKit delete/tombstone strategy. **M2** dedup depends on `contentAddressedID` stability across the **CloudKit field** round-trip but only the JSON path is tested (if CloudKit stores `timestamp` at lower precision → recomputed id ≠ write-time id → duplicates) → mitigate by deduping on the stored `CKRecord.recordName` (already = the id). **M3** LWW `updatedAt` source unpinned (device wall-clock vs server `modificationDate`); skew can invert the winner → pin to server `modificationDate`. **🟢 LOW**: L1 migration idempotency-gate store ambiguity (`needsMigration` reads UserDefaults; §2.2 writes the bump to SwiftData; no `markMigrated`); L2 `readValue` silently drops a present-but-wrong-type key → reverts to default; L3 chord sync-in unregisters the prior chord before a failed re-register → device left with no hotkey + cue (by design, sharp edge); L4 `ChordSyncResolver.apply` ignores `keyboardShortcutEnabled`; L5 §10 lint greps only `CKContainer\|CKDatabase` (narrower than "all CloudKit in one adapter" — `CKRecord`/`import CloudKit` slip through). **✅ Verified GOOD**: micros quantization premise holds → content-addr id stable across JSON round-trip (tested); `pruned`↔`ActivityLogStore.gc` parity exact (both keep `>= cutoff`); CRDT laws + never-LWW hold and are tested (UUID-in-digest load-bearing for both dedup and non-collapse); `SettingsKey` is exactly **34 cases = compiler-exhaustive `valueType` switch → drift impossible**; CloudKit isolation real (kill-switch default-off, adapter `#if`-gated, zero real CK use outside the adapter); §5 enum-snapshot staleness genuinely fixed in S38 (distinct from H1). Findings recorded in **`project_icloud_design_audit.md`** (H1 full + M/L + line refs) + indexed in `MEMORY.md` as "S8.5 활성화 전 필독". **No NEW patterns** (audit session, no code); cumulative NEW S20→S47 ≈ 92 unchanged. Tests **NOT re-run** (zero code change; last green 681 at S47). **Branch tip now S36…+S47 + this S48 docs wrap**; PR #1 still **OPEN, not merged** (owner call); docs wrap auto-updates it. **The autonomous backlog REOPENED**: S47 declared it exhausted, but the audit surfaced concrete **doc-level (S8.5-independent) fixes** — **next session = fix the findings, H1 first** (correct `04-data-model` §2.2/§4.1/§6.2 + RFC §3 wording; add the M1–M3 design decisions to the RFC; L1–L5 clarifications). These make the *future* Phase-2 activation low-risk, which is the whole point of the dark pre-build. Owner-side App Store upload chain unchanged (merge PR #1 · Small Business 15% · App ID · Team ID→`DEVELOPMENT_TEAM` · app record · screenshots 2880×1800 hard-blocker · ASC paste + $2.99 + privacy + age · archive/upload/submit) + WiFi WhenInUse device-verify. |
| 1.47 | 2026-05-26 | **S47 — pty recovered → executed the S46-deferred autonomous backlog D→C→B** (session on `claude/focused-hamilton-417bfc`, continuing S46's tip; same v1.9 line, no version bump, **no owner-visible behaviour change**). At startup **trap #9 was CLEARED** (the machine was rebooted/relogged: `os.openpty` 8/8, 20 `/dev/ttys` nodes vs S46's 527 orphaned) → the entire S46-deferred backlog (B/C/D, all pty-gated) was unblocked. Owner confirmed **"자율 D→C→B"**. **Memory correction:** branch tip was `8fc4217` (S46 docs wrap), one commit past the `09e46ee` recorded in MEMORY/handoff. **D — baseline (681-green):** ran in the worktree, **681/681 PASS first run, no trap #8 stall** (`** TEST SUCCEEDED **`, 11.9s) → the honest gate S45 set, reconfirmed; nothing had changed since. **C — T7 perf nit (`8893a56`):** `ActivityLogMergeResolver.canonicallyOrdered` recomputed SHA-256 for *both* operands on every sort comparison (O(N log N) hashes); switched to **decorate-sort-undecorate** — hash each entry once (O(N)), sort the `(id, entry)` pairs, undecorate. The `(timestamp, content-addressed id)` order is byte-identical so the union merge's commutativity/associativity/idempotency contract is unchanged. Resolves the S46 pre-merge-review MEDIUM. Verified: 18 resolver tests + full 681 green, default + flag-on builds 0-warning, doc-drift clean. **B — trap #8 harness mitigation (`6e24e88`):** investigation found the two handoff-suggested levers were **exhausted or inapplicable** — the scheme is **already `parallelizable = "NO"`** (serialisation lever spent), and all 11 trigger test classes hold triggers as **local vars** (a `tearDown` can't reach them to cancel spawned tasks). S46 had confirmed there is **no product leak** left, so the correct B (per S46's own "harness mitigation, not leak-hunting" verdict) is to **codify the long-documented "re-run-on-FAILED" manual mitigation into a runner**: `scripts/run_tests.sh` classifies the captured log by the authoritative `** TEST SUCCEEDED/FAILED **` line (NOT a pipe exit code) into PASS/FAIL/STALL/PTY and **retries ONLY the zero-assertion stall** (trap #8). A real assertion failure, a nonzero failure summary, a build failure, OR pty exhaustion (trap #9 — reboot not retry) all **fail fast, never retried** → a genuine red is never masked. Pre-flight kills stale Latte + clears a stale `-resultBundlePath` before each attempt (so retries are clean); attempts bounded (default 3) well under the ~25 back-to-back runs that re-exhaust ptys. CI's Test step now calls it. **Verified**: 6-case classifier self-test (PASS/FAIL/STALL/PTY/build/empty — proving the safety-critical "real fail → no retry" direction via crafted-log fixtures); live happy-path (PASS attempt 1, exit 0); the `-resultBundlePath` retry-clean property (consecutive runs, no "already exists"); and **a live run that genuinely hit a trap #8 stall on attempt 1 and recovered on attempt 2** — empirical proof both that the residual stall is real/load-dependent (consistent with the S46 harness-level verdict) AND that the runner absorbs it. Diagnosed the "already exists" error (`xcodebuild: error: Existing file…`) classifies as **FAIL** (not retried), ruling out a misclassification masquerading as the stall. **S47 NEW patterns (3)**: (a) **decorate-sort-undecorate to hoist an expensive comparator key** — when a sort tie-break recomputes a costly key (SHA-256) per comparison, precompute it once per element (O(N) vs O(N log N)) and sort the decorated pairs; the order is identical so the algebraic contract holds. (b) **codify a documented manual flake-mitigation into a retry-runner that decides by the authoritative result line, not the pipe exit code** — retry ONLY the well-characterised no-assertion stall and **fail-fast on every real/build/pty failure** so a red is never masked; prove the dangerous direction (masking a real fail) impossible with crafted-log fixtures before trusting it. (c) **when the suggested fix levers are already pulled (serialisation) or structurally inapplicable (local-var triggers a teardown can't reach), the right move for a confirmed harness-level flake is harness infrastructure, not product/test churn.** Cumulative NEW S20→S47 ≈ 92. **Branch tip now S36…+S47** (D/C/B = `8893a56`, `6e24e88`, + this docs wrap); pushed → PR #1 auto-updates (still **not merged** — owner call). **Autonomous backlog now genuinely exhausted**: iCloud Phase 2/3 activation needs the live CloudKit container behind S8.5 (+ macOS-14 `@Model` for Phase 2); there is **no autonomous flag-flip** (S46 correction). **Owner-side queue = the App Store upload chain** (merge PR #1 · Small Business 15% · App ID `com.parkbyeongjun.latte` · Team ID→`DEVELOPMENT_TEAM` · app record · **screenshots `docs/store/screenshots/` empty = 2880×1800 hard-blocker** · ASC paste + $2.99 + privacy + age · archive/upload/submit) + WiFi WhenInUse device-verify. |
| 1.46 | 2026-05-25 | **S46 — opened the main-merge PR #1 + hardened the store-metadata verification gate + pre-merge review + Phase-1-dark re-verification** (session on `claude/focused-hamilton-417bfc`, continuing S45's tip; same v1.9 line, no version bump, **no Swift source change, no owner-visible behaviour change** — docs/CI/tooling only). Owner intent: build a task list + remaining-work list, recommend an order optimised for *incremental low-error verification* ("철저하게 차곡차곡 검증 오류 최소화"), execute autonomously using context fully, then wrap. Owner picked **"open the main-merge PR first."** **Constraint hit at startup: `xcodebuild test` was pty-blocked (trap #9, ACTIVE) — killing stale `testmanagerd` did NOT recover it** (the 527 `/dev/ttys*` nodes are orphaned device nodes the kernel reclaims only on reboot/relogin; only 1 live process held any ptys). So the plan was re-scoped to **pty-free / build-verifiable / read-only** work and test-dependent tasks were *deferred rather than written blind* (unverifiable test code would violate the session's own goal). **(1) PR #1 opened** (`claude/focused-hamilton-417bfc` → `main`, 42 commits S36–S45, 63 files +2557/−384; `https://github.com/SpesPark/latte/pull/1`) — **not merged** (shared-branch merge is an owner call; S46 commits stack + auto-update the PR). **(2) Store char-limit checker `09e46ee`**: `scripts/check_store_limits.sh` asserts every char-limited `docs/store/` field under Apple's cap (Unicode code points, trailing-NL stripped, Korean=1/char; reproduces S45 counts — description-en 3995/4000, promo-en 160/170, keywords-en 98/100), wired into `check_doc_drift.sh --strict` (CI gate) so over-cap copy fails CI not at submit — **codifies the S45 bug** (description-en silently ~690 over). Report-only + strict paths verified incl. a negative fixture; all 11 fields pass. **(3) Pre-merge review** (code-reviewer agent, read-only; posted to PR #1; **no code changed**) — **no merge-blockers.** Lone **HIGH = FALSE POSITIVE**: flagged `DebouncingDisplaySource`/`ExternalDisplayTrigger` plain `deinit` (vs `isolated deinit`) on a `@MainActor` class as a Swift-6 race, but the deinit only touches `observeTask: Task<Void,Never>?` + `AsyncStream.Continuation`, **both Sendable** → legal & race-free under Swift 6, **proven by the 0-warning strict build** (`isolated deinit`/SE-0371 is only for non-Sendable isolated state) → no action, do NOT "fix" it. One **MEDIUM deferred (→T7)**: `ActivityLogMergeResolver.canonicallyOrdered` recomputes SHA-256 per sort comparison (O(N log N) vs O(N)) — dark Phase-3 code, fix needs 18 resolver tests re-run (pty-blocked). **(4) Phase-1-dark re-verified + entry-point CORRECTED**: flag OFF by default (`project.yml:61` signs no-CloudKit `Latte.entitlements`), `Latte.icloud.entitlements` staged-inactive, container `iCloud.com.parkbyeongjun.latte` consistent (entitlement + `CloudKitSyncEngine.swift:22`), **both default & flag-on builds 0-warning**. **Correction: S45's "iCloud Phase 1 *activation* = autonomous" is WRONG** — `project.yml:54-60` + RFC §11 frame the flag-flip as **Phase 2** (S8.5 + provisioned container + switch `CODE_SIGN_ENTITLEMENTS` + macOS-14 `@Model`); **no autonomous activation step remains** (Phase 1 seam already shipped; a future session must NOT flip the flag — breaks signing without a container). **(5) Static Task-spawn inventory → trap #8 is harness-level**: every long-lived/looping `Task` in `Sources/` is stored + cancelled via deinit (WiFi/Schedule/Calendar `pollTask`, ExternalDisplay/Debouncing `observeTask`, coordinator `consumerTasks`); all others one-shot fire-and-forget (incl. `TriggerCoordinator.swift:222` post-append, `[weak self]`) → **no remaining uncancelled long-lived leak** → residual is a **load-dependent harness-level main-actor stall, not a product leak** (confirms S44); next reliability effort = harness mitigation (per-test teardown / lower concurrency), NOT leak-hunting. **S46 NEW patterns (3)**: (a) codify a manual char/limit audit into a CI script the moment it bites once — calibrate against a known prior result + verify the FAIL path with a negative fixture; (b) a `@MainActor` class with a plain `deinit` is NOT a Swift-6 race if the deinit only touches Sendable state (the 0-warning strict build is the proof — don't cargo-cult `isolated deinit`); (c) `xcodebuild build` needs no pty but `xcodebuild test` does — under trap #9 you can still verify builds + do read-only/doc/CI work, scope the session to that. Cumulative NEW S20→S46 ≈ 89. **pty exhaustion (trap #9) STILL ACTIVE at session end → reboot/relogin required before any `xcodebuild test`. All remaining autonomous work (trap #8 harness mitigation, T7 perf nit, final 681-green run) is pty-gated** → until reboot, remaining value is the owner-side App Store chain (merge PR #1 · Small Business 15% · App ID · Team ID→`DEVELOPMENT_TEAM` · app record · screenshots 2880×1800 hard-blocker · ASC paste + $2.99 + privacy + age · archive/upload/submit) + WiFi WhenInUse device-verify. |
| 1.45 | 2026-05-25 | **S45 — S44 verification gap CLOSED + branch pushed to origin + App Store metadata reconciled to the shipping 6-trigger feature set** (session on `claude/focused-hamilton-417bfc`, continuing S44's tip; same v1.9 line, no version bump, **no owner-visible behaviour change** — docs/metadata only). Owner intent: prep for App Store upload, then execute what's doable autonomously. **(1) Verification gap closed**: trap #9 (pty exhaustion) **self-recovered** on a fresh terminal login (`/dev/ttys*` nodes 527→32, `os.openpty` 8/8 OK, stale `testmanagerd` gone) — and the `sudo sysctl -w kern.tty.ptmx_max=2048` workaround is **REJECTED by macOS** (`Invalid argument`; 511 is at/above the kernel cap), so **reboot / letting the pty-holding processes exit is the reliable recovery, NOT the sysctl raise** (trap #9 corrected in `project_latte_status.md`). With ptys free, the pending **681-green full run passed on the FIRST run, 0 failures, no trap #8 stall**; default + `LATTE_ICLOUD_SYNC` flag-on builds both 0 warnings; doc-drift clean — **S44's only open verification item is done.** **(2) Branch pushed**: `git push -u origin claude/focused-hamilton-417bfc` → new remote branch (unpushed since S35); **not merged to main** (owner deferred the merge/PR). **(3) App Store metadata reconciliation** (`6dcf108`, 7 files in `docs/store/`): the listing copy was all written at the early v1.0 **3-/4-trigger** stage but the shipping binary (MARKETING_VERSION still **1.0.0** — the internal "v1.x" milestones never shipped) has **6 triggers** + Activity history + ⌘⇧L. Owner chose **1.0.0 as the public first release** describing the full current feature set. Fixes: `review-notes` dropped the **stale `network.client` entitlement** (removed S39, never in `Latte.entitlements` — a reviewer-visible mismatch), 4→6 triggers, removed the now-implemented per-calendar "planned" note; descriptions EN/KO add Focus/Schedule/External Display + Activity + shortcut, and **`description-en` was discovered already ~690 chars OVER the 4000 limit pre-existing** → fit to 3995 by dropping the decorative `━` bars + the redundant one-time-purchase section (KO restructured to match, 2421); whats-new 4→6 triggers + **5→6 accent tones** (whats-new was wrong; `CoffeeAccent` enum is 6: espresso/caramel/mocha/latte/matcha/noir); promo-en trimmed 184→160 (was over the 170 cap); all 11 store fields verified under Apple limits. **S45 NEW patterns (3)**: (a) audit store metadata char-limits + feature counts against the *binary/enums*, not the doc's self-label (description-en silently over-limit for sessions); (b) reviewer-facing entitlement lists drift — re-derive from `Latte.entitlements` at submit; (c) `kern.tty.ptmx_max` can't be raised past the kernel cap (`=2048` → Invalid argument) — pty exhaustion clears on reboot/process-exit. Cumulative NEW S20→S45 ≈ 86. **Owner deferred to next session**: main merge/PR, Apple account setup (Small Business Program 15%, App ID `com.parkbyeongjun.latte`, Team ID → `DEVELOPMENT_TEAM` is empty, app record), screenshots (`docs/store/screenshots/` empty — hard blocker, 2880×1800), ASC listing paste + pricing $2.99 + App Privacy + age rating, archive/upload/submit; WiFi WhenInUse downgrade (review-risk, device-verify). **Next autonomous**: iCloud Phase 1 activation prep or trap #8 harness-level reliability work (unchanged from S44). |
| 1.44 | 2026-05-25 | **S44 — trap #8 root-cause: a *second* trigger-task self-retain leak found + fixed (RED→GREEN); residual stall characterized as harness-level + pty-exhaustion discovery** (session on `claude/focused-hamilton-417bfc`, continuing S43's tip; same v1.9 line, no version bump, **no owner-visible behaviour change**). Owner context: **Apple Dev (S8.5) now handled** → autonomous backlog reopens, but owner chose **trap #8 reliability first** ("철저하게 차곡차곡 검증 오류 최소화" — a flaky suite makes every later iCloud-activation verification untrustworthy, so close the foundation first). **Approach**: evidence-first audit (전수 `Task`/`for await` 감사) over chasing the 12% flake, then TDD. **Root cause (the residual S41 missed)**: S41 closed the *coordinator→consumer→trigger* retention; a **second** path remained — **each trigger retains itself** via its own poll/observe task. `Task { [weak self] in guard let self else { return }; …while/for-await with sleep }` — the top-level `guard let self` binds a **strong** self for the whole closure scope **including the suspension**, so `trigger → pollTask → self → trigger` is an effective cycle: the trigger never deallocates, its `deinit` never runs, and the recurring poll/observe task lingers (accumulating across the suite). 5 sites: `Schedule`/`WiFi`/`Calendar` `pollTask` (and these three had **no deinit** at all) + `ExternalDisplayTrigger`/`DebouncingDisplaySource` `observeTask` (deinit cancels but the cycle blocked it from running). Control: `TriggerCoordinator` (146) is correct — its `guard let self` is **inside** the for-await body, not ahead of it. **TDD finding**: the first RED test gave a **false pass** — `await trigger.start()` only *enqueues* the task; if the `do`-block drops the trigger before the task runs its `guard let self`, it early-returns (nil) and no strong self is ever bound. Adding a **50 ms sleep after start()** (let the task bind self while still referenced) made the leak reproduce **deterministically** (weak-ref non-nil after the scope) — far more reliable than the 12% flake. **Fix (`a03efff`, 8 files +207/−16)**: break all 5 cycles — capture the `interval`/`source` by value and re-acquire `self` weakly each iteration so a strong self is **never** held across the suspension; add `isolated deinit { pollTask?.cancel() }` to the 3 polling triggers (the 2 ExternalDisplay classes already cancel). + 5 deterministic RED→GREEN leak regression tests (one per affected type, in the existing trigger test files — no new file, no xcodegen regen). **Verification**: 5 leak tests **RED (5/5 fail) → GREEN (5/5 pass, ~0.05 s each)**; default + flag-on (`LATTE_ICLOUD_SYNC`) builds **BUILD SUCCEEDED, 0 source warnings**; doc-drift --strict clean (Core SwiftUI-free + CloudKit-isolation green); catalog unchanged (172). Test count **676 → 681** (static; baseline 676 was clean-green + 5 new green + partial runs 0 assertion-failures up to 423 — **final single clean 681-green run pending pty recovery, see below**). **trap #8 NOT fully closed (honest result)**: an A/B under identical adversarial back-to-back runs showed **S43 baseline = 5/5 FAIL vs the fix = 3/5 FAIL** → the residual full-suite stall is **pre-existing and NOT caused by this change** (the fix is directionally better via reduced task accumulation), but the remaining stall is a **load-dependent, harness-level main-actor scheduling stall** that real-leak fixes (S41 + S44) reduce but don't eliminate. The captured log confirmed the mechanism: a random test hangs ~18–23 s → timeout → bundle restart → retry passes → overall verdict FAILED, **no assertion error**. Full elimination needs test-harness work (per-test teardown cancelling spawned tasks, or lower test concurrency) — a separate effort; **re-run-on-FAILED stays the mitigation**. **NEW cross-cutting trap #9 discovered**: ~25 back-to-back `xcodebuild test` + many `pkill -9` **exhausted ptys** (`kern.tty.ptmx_max`=511) → `Failed to install or launch the test runner. Pseudo Terminal Setup Error … Device not configured (errno 6)` — distinct from the stall (0 tests execute, no "Executed N" line; killing `testmanagerd` doesn't help). Fix: `sudo sysctl -w kern.tty.ptmx_max=2048` (reversible) or reboot; mitigation: cooldown between repeated full runs. **S44 NEW patterns (4)**: (a) **`guard let self` *ahead of* an `await` loop in a `[weak self]` Task is a retain cycle** — the strong binding spans the suspension; re-acquire weakly *inside* the loop (or capture the needed value), and cancel the task in `deinit`. (b) **RED-first catches a wrong/weak test, not just wrong code** — the false pass exposed that an unstructured Task must be given a suspension to *bind* its capture before you test the leak; without the post-start delay the test proved nothing. (c) **A/B a flaky metric against a stashed baseline before attributing it** (reaffirms S40 pattern f) — baseline 5/5-fail proved the residual stall pre-existing and saved a wrong "my fix regressed it" conclusion. (d) **Heavy test iteration is itself an environmental hazard** — back-to-back suites inflate the flake rate (load) *and* leak ptys; budget cooldowns and a hard cap on consecutive full runs. Cumulative NEW S20→S44 ≈ 83. **Owner-blocked / pending queue**: push `claude/focused-hamilton-417bfc` (now +S44), **final 681-green full-suite run after pty recovery (reboot or sysctl)**, S9 ASC, macOS-14 target decision (Phase 2 activation only), RFC Q4 container-id (non-gating), smoke 23 re-run, WiFi WhenInUse device-verify. **Next autonomous (S8.5 now handled)**: iCloud Phase 1 activation prep (flip `LATTE_ICLOUD_SYNC` + activate staged entitlement — but a signed build / live-container sync test is still owner/Xcode-signing-gated), or further trap #8 harness-level reliability work. |
| 1.43 | 2026-05-24 | **S43 — iCloud-sync Phase 3 pure logic pre-built dark (Domain B union merge, CloudKit-free)** (session on `claude/focused-hamilton-417bfc`, continuing S42's tip; same v1.9 line, no version bump, **no owner-visible behaviour change**). Owner reviewed the S42 handoff and chose **"Phase 3 선빌드 실행"** — the only remaining autonomous, CloudKit-free path the RFC flagged "confirm with owner before starting" (Phase 3 is v2.1 per §12 Q5, so pre-building ahead of v2.0 is a sequencing call). Same cadence as S40/S41/S42: verify the handoff, baseline, execute, verify maximally, wrap. **Handoff verified against the repo**: branch tip `a778a9c`, 36 ahead of origin/main, all 4 S42 SHAs present, main dir was on stale S35 — worked in the worktree. **Step 0 baseline**: fresh `xcodegen generate` → 658/658 PASS reconfirmed (the first full run hit trap #8 once; re-ran green — see below). **The single chunk** — **`ActivityLogMergeResolver`** (`Sources/Core`, docs/design/10 §4 B-2/§6/§10): the append-only **union** merge for the activity log, the structural counterpart to S42's `SettingsLWWResolver` (Domain A) — **never last-writer-wins** (§6: LWW on an event log silently destroys concurrent-day history). `merge(_:)` unions any number of device views, dedups by a stable **content-addressed id** (`contentAddressedID` = SHA-256 of the immutable tuple → a valid `CKRecord.recordName` with no transform at activation), and returns one canonical order (timestamp, then id) so the merge is **commutative, associative, idempotent** as plain array equality (device wake order can't matter; re-sync is a no-op). `pruned(_:now:retention:)` mirrors `ActivityLogStore.gc` exactly (drop strictly-older-than `now − retention`; default tracks `ActivityLogStore.defaultRetention` = 14 days) as a pure fn with `now` injected; `mergedAndPruned` = the full Domain B sync-in transform (union, then local GC on the merged set). **Inert pure value type, no `#if` gate, not `@MainActor`** — like S42 it carries no CloudKit symbols (CryptoKit is a system framework, not CloudKit), so the §10 isolation guard stays green; **no `ActivityLogEntry` model change** (so, unlike Phase 2, **no macOS-14 / `@Model` cost — macOS 13 holds for free**). **18 tests** covering every RFC §10 property: union/no-loss, dedup, idempotency, commutativity, associativity, the load-bearing **never-LWW** guard (two Macs logging the same instant with distinct ids → BOTH survive), content-addressed-id collision-freeness + a Codable round-trip stability check (the id must survive the on-disk `secondsSince1970` format or dedup breaks), canonical-ordering determinism, GC boundary, empty inputs, and `mergedAndPruned`. **Verification**: **658 → 676 PASS** (+18); default + flag-on (`LATTE_ICLOUD_SYNC`) builds **BUILD SUCCEEDED, 0 source warnings**; doc-drift --strict clean (Core SwiftUI-free + CloudKit-isolation both green); catalog unchanged (172). README 658→676; RFC §11 Phase-3 row updated + a "Phase 3 pure logic pre-built dark in S43" note added (records the CloudKit custom-zone wiring as the only S8.5-gated activation remainder). **Trap #8 confirmed still flaky (NOT this code)**: the first full `xcodebuild test` ended `** TEST FAILED **` with 0 assertion failures / no signal (the documented ~12% stall); the new suite passed 18/18 in isolation and the immediate re-run was 676/676 `** TEST SUCCEEDED **`. The S41 deinit-cancel fix reduced but did not fully eliminate the stall — re-run-on-FAILED remains the mitigation; trap #8 reopened as "mitigated, not closed" in memory. **S43 NEW patterns (3)**: (a) **a CRDT-style union merge becomes pure-testable by returning a canonical (deduped + total-ordered) form** — sorting the union by a stable key makes commutativity/associativity/idempotency hold as ordinary array `==`, so the algebraic laws are plain `XCTAssertEqual`s, not bespoke set comparisons; (b) **content-address over the *full* immutable tuple, not just the UUID** — including every field makes the id idempotent on true duplicates AND collision-free on genuine differences without trusting that UUIDs were assigned uniquely, and a Codable round-trip test pins the id to the on-disk format so sync dedup can't silently break; (c) **a "mitigated" flake is not a "closed" flake** — S41 marked trap #8 closed on 10/10 green, but it recurred on the first S43 full run; treat intermittent stalls as mitigated-until-root-caused and keep the re-run-on-FAILED ritual + a regression watch. Cumulative NEW S20→S43 ≈ 79. **Owner-blocked queue (autonomous backlog now genuinely exhausted)**: S8.5 Apple Dev, S9 ASC, **push `claude/focused-hamilton-417bfc` (now +S43; count via `git rev-list --count origin/main..HEAD`)**, macOS-14 target bump decision (Phase 2 activation only — Phase 3 needs no bump), RFC Q4 container-id (non-gating), smoke 23 re-run, WiFi WhenInUse downgrade device-verify. **Next autonomous: nothing left until S8.5** — all three iCloud phases' pure CloudKit-free logic is now pre-built; every remaining step (Phase 1 entitlement activation, Phase 2 `@Model`+macOS-14, Phase 2/3 live CloudKit zone wiring) needs the live container behind the Apple Developer Program. |
| 1.42 | 2026-05-24 | **S42 — iCloud-sync Phase 2 pure logic pre-built dark (CloudKit-free)** (session on `claude/focused-hamilton-417bfc`, continuing S41's tip; same v1.9 line, no version bump, **no owner-visible behaviour change**). Owner direction (same cadence as S40/S41): verify the handoff, list possible + remaining work, recommend an order, then execute autonomously chunk-by-chunk with maximal verification, continuing until context is well-used, then wrap. **Boundary context**: the unblocked autonomous backlog was declared exhausted at S41 (Phase 2+ is S8.5-gated). The one remaining autonomous path the RFC flagged "confirm with owner" was the **CloudKit-free pre-build of Phase 2's risk-bearing pure logic** (§10) — owner **confirmed** it, and **confirmed keeping the macOS 13 deployment target** (pure Codable snapshot, not a SwiftData `@Model` which would force macOS 14 and drop macOS 13 Macs — a product decision, not made silently). **Recommended + executed order**: Step 0 baseline (627/627 PASS reconfirmed) → Chunk 1 LWW resolver → Chunk 2 migration snapshot → Chunk 3 chord sync-in → wrap. All on the worktree (push = owner action; count via `git rev-list --count origin/main..HEAD`). **Chunk 1 — `SettingsLWWResolver` (`d3945b3`)**: Domain A (Settings) per-record last-writer-wins as a pure generic value type (docs/design/10 §6/§10) — strictly-later remote wins, exact tie keeps local (§3 conservative tie-break, re-toggle-recoverable). Generic over the Bool/Int/Double/String/Data trichotomy; named + scoped to make the §6 invariant structural (**the activity log never reaches LWW** — that is Phase 3's append-only union merge). 10 pure tests. **Chunk 2 — `SettingsMigration` (`b8b8cfc`)**: OQ-04 UserDefaults→v2 transform as **pure Codable, not SwiftData @Model** (keeps macOS 13). `snapshot(from:now:)` enumerates every *present* `SettingsKey` via a **compiler-enforced exhaustive `SettingsKey.valueType`** (34 cases — a new key without a classification fails the build = structural drift guard) into a `SettingsSnapshot` stamped `schemaVersion=2`; absent keys stay absent (preserves "never touched = default"); `schemaVersion` rides the top-level field, not a copied entry. Adds `SettingsStore.exists(_:)` (protocol + both conformers) so the copy distinguishes "never touched" from "set to default". **Correctness finding**: `needsMigration` defaults an absent `schemaVersion` to **1**, because the shipped v1.x app **never writes `schemaVersion`** — the §2.2 "absent → fresh install, skip" assumption would have *skipped migrating every real upgrading user*; corrected here + documented in the RFC. 15 pure tests incl. a full-`allCases` parity test (RFC §10) + JSON round-trip. **Chunk 3 — `ChordSyncResolver` (`fcc1b14`)**: §7 device-physical half of chord sync as pure `@MainActor` logic — a local (non-synced) override wins locally; a failed registration **keeps the synced value (never clobbers it)** and reports `showsDisabledCue` so the UI surfaces the shipped disabled-state hint (07-spec J, S17); **persists nothing** → structurally cannot rewrite the synced value. 6 pure tests with a failable `MockHotKeyRegistrar` (no Carbon, no iCloud). The local-only `shortcutChordDeviceOverride` key + its migration-exclusion are deferred to activation (adding it now would force a premature migration-exclusion change). **All three are inert pure types in `Sources/Core`, compiled + tested in the default build — no `#if` gate needed** (they carry no CloudKit symbols; §10 guard still passes). **Verification**: **627 → 658 PASS** (+10 LWW, +15 migration, +6 chord); default + flag-on builds 0 source warnings; doc-drift --strict clean (Core SwiftUI-free + CloudKit-isolation both green); catalog unchanged (172). README 627→658; RFC §11 row 2 + a "Phase 2 pure logic pre-built dark in S42" note added (records the macOS-14/`@Model` + CloudKit activation remainder). **S42 NEW patterns (4)**: (a) **pre-build a gated feature's risk-bearing logic as inert pure value types in the default build, not behind the `#if`** — they compile + unit-test with no flag-on step and carry none of the gated framework's symbols, so the lint guard stays green and there's no "dark code never compiled → rots" gap; (b) **a compiler-enforced exhaustive `switch` over an enum is a free drift guard** — classify every `SettingsKey` by storage type and a future un-classified key fails the build, stronger than any doc list (cf. 04-data-model's hand-maintained snapshots); (c) **a migration "fresh vs existing" probe must match what the *shipped* app actually wrote, not the design doc** — v1.x never wrote `schemaVersion`, so "absent = fresh" would skip all real upgrades; default the absent value to the *current* version instead; (d) **when "pure logic pre-build" meets a hidden platform-version cost (SwiftData ⇒ macOS 14), surface it as a product decision** — model the transform as plain Codable to hold the deployment target and defer the `@Model` write to activation, rather than silently bumping the floor. Cumulative NEW S20→S42 ≈ 76. **Owner-blocked queue (unchanged + now larger lead)**: S8.5 Apple Dev, S9 ASC, **push `claude/focused-hamilton-417bfc` (now +S42; count via `git rev-list --count origin/main..HEAD`)**, RFC Q4 container-id (non-gating), smoke 23 re-run, WiFi WhenInUse downgrade device-verify. **Next autonomous: genuinely exhausted until S8.5** — Phase 2 activation (macOS-14 bump + SwiftData `@Model` + live CloudKit wiring) and Phase 3 (Domain B union merge, v2.1) both need the live container. Phase 3's union-merge *is* a pure function (§4 B-2/§10) and **could be the next CloudKit-free pre-build if the owner wants more dark work** — confirm before starting (same gate as this session). |
| 1.41 | 2026-05-24 | **S41 — iCloud-sync Phase 1 (ships dark) + full-suite flake fix (trap #8 closed)** (session on `claude/focused-hamilton-417bfc`, continuing S40's tip; same v1.9 line, no version bump, **no owner-visible behaviour change**). Owner direction: list possible + remaining work, recommend an order, then execute autonomously chunk-by-chunk with maximal verification ("차곡차곡 쌓아 검증 오류 최소화"), continuing until context is well-used, then wrap. **Recommended + executed order**: Step 0 baseline → iCloud Phase 1 (5 sub-chunks) → flake fix → RFC staleness tidy → wrap. All on the worktree (push = owner action; count via `git rev-list --count origin/main..HEAD`). **Step 0 baseline**: build 0-warnings + 619/619 PASS confirmed (the S40 flake hit 2/3 on the first full runs — re-ran green per trap #8). **iCloud Phase 1 (docs/design/10 §11 row 1; 6 commits)** — the designated #1 autonomous item, now on S40's clean Swift 6 base; ships **dark** behind the `LATTE_ICLOUD_SYNC` compile-time kill-switch (off by default) so app behaviour is unchanged and the whole thing is pure-unit-testable with no Apple gate: `4fec07b` pre-req `fix(tests)` — `@MainActor SettingsRootLayoutTests` clears 2 Swift-6 autoclosure warnings that were PRE-EXISTING since S40 (its incremental build cached the file clean; a fresh xcodegen re-surfaced them — so S40's "test target 0 warnings" was an incremental-cache artifact; restores the genuine zero-source-warning baseline). `7937e39` **1a** `CloudSyncEngine` protocol seam + `MockCloudSyncEngine` (pure @MainActor lifecycle: isRunning/start/stop; §8 unavailable-account no-op; 4 tests). `bc8d8c1` **1b** wire into `AppEnvironment` dark — injected `cloudSync: (any CloudSyncEngine)?` (nil default) + `startCloudSyncIfNeeded()` boot hook in `applicationDidFinishLaunching` after bootTriggers (nil → `cloudSync?.start()` no-op); 3 wiring tests (nil-by-default, no-op-when-nil, injected-engine-started). `19a979e` **1c** `CloudKitSyncEngine` adapter skeleton wholly behind `#if LATTE_ICLOUD_SYNC` (default build never compiles/links CloudKit; account-availability gate only; container id `iCloud.com.parkbyeongjun.latte` lives here only) + a **CloudKit-isolation lint guard** in `check_doc_drift.sh` enforcing §10 (CKContainer/CKDatabase only in CloudKitSyncEngine.swift — caught its own doc-comment leak, reworded). `e90110e` **1d** stage **inactive** `Configuration/Latte.icloud.entitlements` (icloud-services=[CloudKit] + container-identifiers + network.client which S39 dropped and CloudKit re-needs); NOT referenced by any build setting → default signing untouched (Latte.entitlements, no CloudKit) → buildable pre-S8.5; project.yml documents the 2-step Phase 2 activation. `da5a755` **1e** RFC §11 "Phase 1 landed dark in S41" note + README 619→626. **Verified both flag states**: default build green + filtered tests pass with sync dark; a **flag-on build compiles the adapter clean under Swift 6** (closes the dark-code verification gap); doc-drift --strict clean (CloudKit guard passing). **Flake fix — trap #8 CLOSED (`1ae1eec`)**: confirmed the root cause by reading — `TriggerCoordinator.start(_:)` keeps a long-lived `for await trigger.voteStream` consumer task that strongly retains its trigger and is deliberately never cancelled on stop() (S8b), **and the isolated deinit didn't cancel it either** → a deallocated coordinator leaks the task as suspended main-actor work; across the suite's hundreds of coordinators these accumulated → the ~12% (this session 3/5) full-suite stall. Fix: the `isolated deinit` now cancels every consumer task — safe because the coordinator is already gone (no OFF→ON restart left to protect; the S8b path only runs while alive). RED→GREEN regression test (`testDeinitReleasesConsumerTaskAndTrigger`: weak-ref trigger released on coordinator dealloc — failed pre-fix/leaked, passes post-fix). **Validated 10/10 green full-suite runs post-fix** (was 3/5 flake pre-fix this session); all 21 TriggerCoordinatorTests incl. OFF→ON restart stay green. README 626→627. **RFC staleness tidy (`15e6dbd`)**: RFC §3/§13 flagged 04-data-model §5 SettingsKey snapshot as stale + deferred — but S38 already refreshed it (+authoritative `allCases` pointer); marked resolved. **Tests 619 → 627 PASS** (+7 CloudSync seam/wiring, +1 flake regression; SettingsRootLayout fix added 0); build 0 warnings (default + flag-on); doc-drift clean; catalog unchanged (172). **S41 NEW patterns (5)**: (a) **A dark compile-time-gated feature is verifiable in BOTH states without its runtime gate** — default build proves "nothing changed"; a one-off flag-on `xcodebuild build` proves the `#if`-walled code still compiles (CloudKit links without the entitlement — that's a sign/runtime concern, not compile), closing the "never-compiled dark code rots" gap. (b) **Stage an entitlement as an unreferenced file, not an active key** — adding CloudKit keys to the live entitlements would break personal-signing pre-S8.5; a separate inactive `*.icloud.entitlements` + a documented 2-step switch keeps the default build green while the artifact is ready. (c) **A token-grep lint guard will catch its own documentation** — the §10 CKContainer/CKDatabase guard fired on the protocol's own doc comment; keep the literal tokens out of every file except the adapter (reword docs to "CloudKit's container/database APIs"). (d) **An "intentionally never cancelled" task still needs deinit cancellation** — the S8b "don't cancel on stop()" rule protects the *live* restart path; it never implied "don't cancel when the owner object is being destroyed", and that gap was the trap #8 leak. (e) **A fresh xcodegen/clean build is the honest warning audit** — incremental builds cache unchanged files clean and silently hide their warnings (S40's "0 warnings" missed 2); regenerate before claiming a zero-warning baseline. Cumulative NEW S20→S41 ≈ 72. **Owner-blocked queue**: S8.5 Apple Dev, S9 ASC, **push `claude/focused-hamilton-417bfc` (now +S41; count via `git rev-list --count origin/main..HEAD`)**, RFC Q4 container-id (accept at Phase 2 kickoff, non-gating), smoke 23 re-run, WiFi WhenInUse downgrade device-verify. **Next autonomous: thin — iCloud Phase 2+ is S8.5-gated (live CloudKit); the substantive unblocked backlog is exhausted at this boundary.** |
| 1.40 | 2026-05-24 | **S40 — Swift 6 strict-concurrency migration (S39's deferred Step 4)** (session on `claude/focused-hamilton-417bfc`; same v1.9 line, no version bump, **no owner-visible behaviour change**). Owner chose the Swift 6 migration *before* iCloud Phase 1 so Phase 1 lands on a clean concurrency base. Approach: flip `project.yml` `SWIFT_VERSION` 5.10 → **6.0** and fix everything that surfaces. **App-side (13 S39-flagged warnings → 0):** Category A = `isolated deinit` (SE-0371) on the 4 nonisolated-deinit sites (`AwakeManager.powerObservation`, `NSScreenSource.observer`, `TriggerCoordinator` ×2 observer tokens) — teardown runs on the main actor; objects are released on main in practice so it's synchronous, zero behaviour change. Category B = only `FocusTrigger.observe` needed an edit (moved the `self.isFocusActive` read *inside* the `Task { @MainActor }` hop, out of the `@Sendable` KVO block); the `AppTrigger` onLaunch/onTerminate + FocusTrigger onChange *captures* the S39 audit flagged **resolved automatically** — **SE-0434 makes `@MainActor` closures `Sendable`** in Swift 6. Category C = the 2 ObjectiveC `@preconcurrency` hints cleared as a side-effect of fixing deinit isolation. **Test-target (flipping the whole project surfaced test-only issues; fixed for a genuinely clean base):** `@unchecked Sendable` class box for counters/clocks captured in `@Sendable` closures (OnboardingStateTests, TriggerCoordinatorActivityLogTests, CalendarTriggerTests — reapplies the existing ScheduleTriggerTests.Clock pattern); **fresh-iterator-inside-Task ×10** replacing "capture an outer non-Sendable `AsyncStream.Iterator` into a `@Sendable` Task" (all 10 are nil-probe tests → semantically identical, the pattern several siblings already used); `MainActor.assumeIsolated` in `setUp`/`tearDown` (AppEnvironmentTests, AwakeIntentsTests — nonisolated XCTestCase overrides run on main); `@MainActor` on RetentionPickerTests; `var c`→`let c` (RecurringQuickPresetTests). **Result: app + test target build with 0 warnings / 0 errors under Swift 6; 619/619 PASS** (count unchanged — fixed test code, added no tests). Doc-drift clean; catalog unchanged (172). PRD §8.1 + README + memory toolchain note updated (Swift 6 mode; Xcode 26.4.1→26.5 / Swift 6.3.2 drift corrected). Only residual build warning = a toolchain `ld` note (test bundle targets macOS 13, Xcode 26 XCTest dylib built for 14) — pre-existing, unrelated. **IMPORTANT finding — a PRE-EXISTING full-suite test flake, NOT caused by S40**: the full `xcodebuild test` sweep intermittently (~12%) hangs — a *random* async test (test82 / ReevaluateWatched / ActivityLog, different each run) stalls the main actor ~18s → "Restarting after unexpected exit, crash, or test timeout" → TEST FAILED, with **no assertion failure and no signal** (environmental/scheduling stall). Classified rigorously: the hung test is **0/15 in isolation**, and the **S39 baseline (5.10, changes stashed) reproduces it 3/25 (~12%)** stalling at different tests — **Swift-version-independent**. Same family as the documented "phantom rc=137" flake; suspected mechanism is the uncancelled `TriggerCoordinator` consumer Tasks accumulating (~600) across the suite. Logged as cross-cutting trap #8 in memory; mitigation = re-run on FAILED; real fix = a separate session. **S40 NEW patterns (6)**: (a) `isolated deinit` (SE-0371, Swift 6.1+) is the clean fix for "`@MainActor` class touches a non-Sendable isolated stored prop in deinit"; (b) SE-0434 — `@MainActor` closures are Sendable in Swift 6, so the only residual `@Sendable`-capture fix is moving main-actor property *reads* inside the actor hop; (c) fresh-iterator-inside-Task beats capturing an outer `AsyncStream.Iterator` into a `@Sendable` Task (identical for nil-probes); (d) `@unchecked Sendable` class box for test counters/clocks in `@Sendable` closures; (e) `MainActor.assumeIsolated` for `@MainActor` test classes' nonisolated `setUp`/`tearDown`; (f) **characterize a flaky failure before "fixing" it** — isolate the test (0/15) AND diff against the unmodified baseline (3/25) before attributing it to your change (saved a wrong fix; the flake was pre-existing). Cumulative NEW S20→S40 ≈ 67. **Owner-blocked queue**: S8.5 Apple Dev, S9 ASC, push `claude/focused-hamilton-417bfc` (now +S40), RFC Q4 container-id (non-gating), smoke 23 re-run, WiFi WhenInUse downgrade device-verify. **Next autonomous: iCloud Phase 1 (now on a clean Swift 6 base) — or the pre-existing full-suite flake (#2) for CI reliability.** |
| 1.39 | 2026-05-22 | **S39 — whole-app audit + audit-driven stabilization (Steps 1-3 of the recommended order)** (session on top of S38; same v1.9 release line; **first code changes since S20**). Owner asked for a thorough whole-app verification before starting iCloud Phase 1. Ran a 5-stream parallel audit (code-quality, security, concurrency, architecture/test-gaps, compiler-warnings) then self-verified the top findings by direct file reads before acting. **Verdict: 0 CRITICAL; codebase healthy.** All work on `claude/focused-hamilton-417bfc` (push = owner action; count via `git rev-list --count origin/main..HEAD`). **Step 1 stabilization (6 commits):** `d4c7007` drop unused `network.client` entitlement (no network code in Sources/; aligns the manifest with the No-Data-Collected claim); `423e331` stop trapping SIGABRT (it suppressed the OS crash report + spawned a Task from an already-faulting runtime; the kernel releases the IOPMAssertion on exit regardless; SIGINT/SIGTERM kept); `888b2f3` post `.activityLogDidAppend` only after the append `await` commits (TriggerCoordinator's comment claimed actor-FIFO ordering guaranteed it, but the unstructured Task only worked via the consumer's 300ms debounce) + contract test; `f749818` snapshot power-source observers before fan-out (a callback that cancels its observation mutated the dict mid-iterate) + cancel-during-callback test; `7d6297a` guard WiFi `requestAccess` against the continuation overwrite-leak (a concurrent second call stranded the first coroutine forever); `5446104` drop the dead ExportButtons tombstone comment. **Step 2 test-gap fills (2 commits):** `ecd5ba9` AppIntents tests (Toggle/Start/Stop `perform()` incl. the 1 & 1440-minute bounds — the only UI-independent entry point, previously 0 tests); `bbc4448` Calendar overlapping-event OFF-suppression test (guards the `&& activeIDs.isEmpty` branch — back-to-back meetings must not release the Mac mid-meeting). **Step 3 architecture prep for iCloud (2 commits):** `21db227` moved `ActivityChartPalette` Color rendering out of `Sources/Core` (it was Core's *only* `import SwiftUI`, violating 02-arch §3.2 and blocking the OQ-04 SwiftData lift; pure data/JSON stays in Core, the `Color` extension + `color(for:)` move to a UI-layer file — same module, behaviour unchanged) + `d2693e2` a CI guard in `check_doc_drift.sh --strict` so Core can't silently re-import SwiftUI. **Tests 611 → 619 PASS** (+8: 5 AppIntents, 1 Calendar overlap, 1 PowerSource fan-out, 1 TriggerCoordinator ordering); doc-drift clean (now also asserts Core is SwiftUI-free); catalog unchanged (172 keys). **Export-claim correction**: memory v1.3.1 + this doc's §16 changelog recorded CSV/JSON export as shipped, but git shows it was added S15 (`86bc5b1`) and **removed S24 (`0e13546`, non-functional)** — corrected here; no exporter exists in current code (so, per the security stream, there is *no* path-traversal export surface to worry about). README test/smoke counts refreshed (610→619, 22→23). **Deliberately deferred** (each deserves a dedicated full-budget session — rushing risks regressions in delicate code): **Step 4** = Swift 6 strict-concurrency migration (13 warnings concentrated in nonisolated-deinit teardown + `@Sendable` closure capture, exactly the concurrency code prior sessions carefully tuned); **Step 5** = iCloud Phase 1 (the major deliverable). **S39 NEW patterns (4)**: (a) **For a whole-app audit, fan out non-overlapping split-role agents then self-verify the top findings by direct read** — an agent's summary is its *intent*, not ground truth; re-reading caught that "no export code" was a genuinely *removed* feature (not an oversight) and downgraded a "HIGH silent-data-loss" to LOW once the `[String]`-encode-can't-fail reality was confirmed. (b) **A permission/entitlement change that can't be unit-verified is owner-device-smoke-gated, not an autonomous edit** — applied the safe WiFi continuation-guard, deferred the `requestAlways→WhenInUse` downgrade (CoreWLAN `ssid()` resolution under When-In-Use needs a real Mac), per mock/production-parity. (c) **Stop at a clean, fully-committed, fully-green boundary rather than start a large risky item with too little budget to finish it** — a half-done concurrency refactor is worse than a clean handoff; "use context fully" ≠ "begin work you can't land cleanly". (d) **post-after-await converts an *accidental* ordering guarantee into a real one** — leaning on a downstream debounce to mask an unstructured-Task race is the kind of invariant a future eager consumer silently breaks. Cumulative NEW S20→S39 ≈ 61. **Owner-blocked queue**: S8.5 Apple Dev, S9 ASC, push `claude/focused-hamilton-417bfc`, RFC Q4 container-id (non-gating), smoke 23 re-run, WiFi WhenInUse downgrade device-verify. **Next autonomous: iCloud Phase 1 — or the Swift 6 migration first if preferred.** |
| 1.38 | 2026-05-18 | **S38 — §13 SettingsKey doc-tidy + RFC §12 owner decisions locked (Phase 1 unblocked)** (autonomous session on top of S37; same v1.9 release line; **zero code**, docs-only, 611 unchanged). Owner question — "ADP 등 외부 작업이 선행되지 않아도 진행할 수 있는 작업은 뭐야?" — surfaced that the S37 handoff had mis-bucketed RFC §12 with the Apple blocks; §12 is owner-*decision*-gated, **not** owner-*Apple*-gated, and the owner was present. Ran the two genuinely ADP-independent items back to back. 2 commits on `claude/focused-hamilton-417bfc` (push = owner action; count via `git rev-list --count origin/main..HEAD`): `2b276bf` `docs(design): sync 04-data-model §5 SettingsKey snapshot with source (24 → 34 cases)` — the §5 enum snapshot predated **10** shipped keys (RFC §13 estimated "~7"): coffeeAccent, hasSeededAppDefaults, externalDisplay{Enabled,Whitelist}, keyboardShortcutEnabled, shortcutChord, activityRetentionDays, activityChartColors, recurringQuickPresets, didSeedBuiltinPresets. Brought to exact parity with `Sources/Core/SettingsStore.swift` (34 cases incl. `, Sendable`) **+ a new authoritative-source pointer** making the snapshot an explicit curated reader-aid with `SettingsKey.allCases` as single source of truth — drift-proofing per the RFC's own §10/§13 principle. Closes the doc-tidy RFC §13 deliberately deferred (RFC §13 §4 trigger-table drift left out of scope, as RFC scoped). `b61015b` `docs(design): record RFC §12 owner decisions — Phase 1 unblocked` — owner answered the 3 code-gating questions: **Q1 = B-2** (activity log → CloudKit custom-zone append-only union merge; Phase 3 in scope), **Q2 = approved** (§5.1 privacy sign-off; "No Data Collected" preserved), **Q5 = v2.0 = Phase 1+2 / Phase 3 = v2.1**. §12 rewritten as a decision log (Q3 resolved-by-Q5; **Q4 container-id open + explicitly non-gating**); RFC header 0.1→0.2, intro blockquote, §11 Phase-0 row, §14 summary + 07-spec §1 + 09-c3 §14 cross-refs all flipped "owner decision pending" → "decisions locked 2026-05-18". **Gate effect**: iCloud-sync **Phase 1 is now an actionable autonomous session** (dark `CloudSyncEngine` seam + mock + entitlement file behind compile-time kill-switch; zero behaviour change; 100% pure-unit-testable per RFC §10; needs **no ADP** — entitlement file only, container stubbable until S8.5). Phase ≥ 2 (live CloudKit) stays S8.5-gated. **Tests 611/611 PASS unchanged**; doc-drift clean; catalog unchanged (not touched). **S38 NEW patterns (2)**: (a) **A "separate doc-tidy, not gating" deferral inside an RFC is itself a zero-gate autonomous unit — execute it, don't re-defer**; fix a stale inlined snapshot by adding an authoritative-source pointer so it can't silently re-drift, not just by refreshing the copy. (b) **"Owner-decision-gated" ≠ "blocked" when the owner is in-session** — always separate owner-*decision* gates from owner-*Apple* gates when triaging; conflating them hid that the largest remaining autonomous item (iCloud Phase 1) was 3 answers away from actionable. Cumulative NEW S20→S38 ≈ 57. **Owner-blocked queue**: S8.5 Apple Dev, S9 ASC, push `claude/focused-hamilton-417bfc` (tip = S36+S37+S38; supersedes standalone S36 branch), RFC Q4 container-id (non-gating), smoke 23 re-run. **Next autonomous: iCloud Phase 1.** |
| 1.37 | 2026-05-18 | **S37 — iCloud-sync joint-design RFC + inherited-baseline & catalog verification** (autonomous session on top of S36; same v1.9 release line; **zero code**). Owner direction: "철저하게 차곡차곡 쌓아가서 검증할 때 오류를 최소화" — stack autonomous work with maximal verification safety, ride out context, then wrap. Worktree branch reset onto S36 tip (`4309cd3`) so S37 chains continuously and the owner pushes **one** branch. **Step 0 baseline verified**: `pkill` pre-flight → 611/611 PASS, `xcodegen generate` (fresh worktree had no .xcodeproj), doc-drift clean, catalog 172 keys + ru 4 CLDR forms — all inherited claims confirmed by measurement. S37 commits on `claude/focused-hamilton-417bfc` (2 substantive + docs-wrap commits; exact count via `git rev-list --count origin/main..HEAD` — a frozen integer here would be invalidated by the wrap commit that writes it): `9598eba` `docs(design): C-3/B1.2/Settings iCloud sync joint-design RFC` — new [`docs/design/10-c3-icloud-sync-rfc.md`](docs/design/10-c3-icloud-sync-rfc.md) resolving the iCloud-sync deferral from 09-c3 §14 + 07-spec §1. Core design: **one CloudKit entitlement/migration event** (the OQ-04 v2.0 UserDefaults→SwiftData migration, 04-data-model §2.2) covering **two independent domains** — Settings (last-writer-wins; includes the B1.2 chord with per-device best-effort registration + optional non-synced local override) and Activity log (recommended B-2: CloudKit custom-zone **append-only union merge**, never LWW, immutable content-addressed entries, local 14-day GC on the merged set; B-1 device-local fallback documented). Privacy analysis: "No Data Collected" **preserved** (private DB only, no developer-side processing, the S14 structured-`reasonCode` discipline already excludes all PII). `CloudSyncEngine` protocol seam mirrors the existing `SettingsStore`/`HotKeyRegistrar`/`DisplaySource` mock pattern → merge/LWW/migration/chord-fallback 100% pure-unit-testable without CloudKit; only final two-Mac confirmation owner-gated. Ships dark behind a compile-time kill-switch. Blocks on S8.5. 09-c3 §14 + 07-spec §1 now cross-reference the RFC. `35104b8` `docs(design): RFC precision pass` — consistency audit vs the docs it extends; 2 precision fixes, no design change (§8 fallback pinned to 04-data-model §2.2 "deleted in v2.1" wording; §1 reconciles with v2-backlog "Re-evaluate post-v1.0 ship" gate + PRD §7.4 no-telemetry permanence). **Verification, no change**: full catalog sweep = 172 keys × 10 target locales = **1720 cells, zero gaps** (plural-aware); LanguageStep defer-and-watch item **empirically CLOSED** (2 strings, full 11-locale coverage, `testEveryEntryCoversAllPhaseHLanguages()` already guards the invariant — no duplicate test added); RFC↔docs consistency audit found **no contradictions**. **Tests 611/611 PASS unchanged** (docs-only); doc-drift clean; catalog unchanged. **S37 NEW patterns (4)**: (a) **RFC-first is the correct autonomous move for a "solo ship risks migration churn" deferral** — zero code = zero regression = it *protects* the verification-safety the deferral guarded. (b) **CloudKit *private* DB preserves Apple's "No Data Collected"** — user's own private DB + no developer-side processing ≠ developer collection; the S14 reasonCode boundary carries over for free. (c) **An append-only log must never ride a last-writer-wins sync path** — the two-domain split is a structural data-loss guard, not a preference. (d) **"Re-evaluate post-X" in a Won't-do list is a gate, not a refusal** — an RFC against it is the gate firing; say so explicitly. **Autonomous backlog now terminal at the RFC boundary** — next step (Phase 1 code) requires owner answering RFC §12 Q1/Q2/Q5. **Owner-blocked queue**: S8.5 Apple Dev, S9 ASC, push `claude/focused-hamilton-417bfc` (branch tip = S36×2 + S37 docs/RFC commits; supersedes the standalone S36 branch), RFC §12 decisions. |
| 1.36 | 2026-05-17 | **S36 — Russian CLDR four-form plurals + S35-handoff drift fixes** (new session on top of S35; same v1.9 release line). Cold-start review found S35's plural migration shipped Russian with only `one`+`other` and a literal "1" in the `one` slot ("1 минута") — CLDR-incorrect because n=21/31/101 are also Russian `one` but resolved via `other` ("%lld минут" → "21 минут"; correct is "21 минута"). 2 commits on branch `claude/suspicious-kowalevski-4ffca8` (push = owner action): `2fa0668` `feat(i18n): Russian CLDR four-form plurals (one/few/many/other)` — the 3 plural keys (`%lld minutes`, `%lld hours`, `Currently: %lld external displays`) filled with the complete CLDR four-form set, every form keeping `%lld` so the count renders for 21/31/… too; `other` carries the decimal form per CLDR (never hit by `%lld` integers but Apple requires the form). New `testRussianPluralKeysHaveAllFourCLDRForms` locks in all four forms + the `%lld` placeholder, guarding the two-form/literal-1 regression. TRANSLATIONS.md reframed so Russian is the worked reference; Polish/Arabic/Czech remain community-PR targets (not yet in shipped 11-locale knownRegions). Plus a `docs:` commit for SESSION_HANDOFF drift: (1) S35's "Push origin/main — owner action, 4 commits ready" was stale (already merged + pushed; HEAD = origin/main = `9fcb9d7`) — corrected; (2) cold-start verification block was missing the mandatory `pkill -9 -f "Latte.app"` pre-flight that `feedback_smoke_iteration.md §5` says belongs there — added (this session re-hit the exact trap: a leftover Release Latte instance + `LSMultipleInstancesProhibited` made `xcodebuild test` fail "Could not launch LatteTests"; not a code regression — `pkill` cleared it, then 610/610 → 611/611). **Tests 610 → 611 PASS** (+1 Russian CLDR test); **doc-drift clean**; catalog still 172 keys × 11 langs (no key count change — form expansion only). **S36 NEW patterns (3)**: (a) **CLDR `one` is not "n == 1"** — Slavic `one` includes 21/31/41/101…; a literal-"1" plural form is a latent i18n bug for any language where `one` is a modular class, not the singleton. Always use the `%lld` placeholder in every plural form. (b) **`other` is the decimal-fallback form, mandatory but often dead** — for `%lld`-integer keys no integer ever routes to `other` in languages with full one/few/many, yet Apple requires it; populate it with the decimal-appropriate form (Russian: the `few`-shaped "минуты") rather than copying `many`. (c) **Worktree-vs-main edit split footgun** — absolute paths under the repo root resolve to the *main* checkout, not the active `.claude/worktrees/<name>` worktree; mixed edits landed across two branches. Always target the worktree path explicitly for session work; verify with `git -C <worktree> status` before committing. **Owner-blocked queue (unchanged)**: S8.5 Apple Dev (Day 16), S9 ASC. Remaining autonomous backlog still owner-tooling-blocked or multi-session-design (C-3 iCloud RFC). |
| 1.35 | 2026-05-16 | **S35 — autonomous infrastructure round: CI doc-drift + xcstrings plurals + smoke pre-flight + docs** (same-day continuation of S34; same v1.9 release line). Owner direction: "철저하게 차곡차곡 쌓아가서 검증할 때 오류를 최소화" — pick the SESSION_HANDOFF "Next-session entry points" 3-5, chunk-verify, ride out remaining context. 4 commits: `2a6579f` CI doc-drift wiring (new `doc-drift` job in `.github/workflows/ci.yml` running `scripts/check_doc_drift.sh --strict` on every push + PR; ubuntu-latest runner, +30s wall-time at no macOS-minute cost; catches the S33 README/URL/catalog drift class before merge instead of relying on humans noticing across sessions) + `ba8a053` xcstrings plurals migration (3 flat-key pairs → 3 variations.plural keys: `1 minute`+`%d minutes` → `%lld minutes`; `1 hour`+`%d hours` → `%lld hours`; `Currently: 1 external display`+`Currently: %lld external displays` → `Currently: %lld external displays`. Catalog 175 → 172 keys. `scripts/migrate_plurals.py` idempotent migration tool committed as reference. `AwakeDuration.label` + `TriggerConfigForms.statusRow` simplify — `m == 1` / `h == 1` / `count == 1` ternaries removed; Swift `String(localized:)` inflates plural automatically when Int interpolates. `LocalizationCatalogTests.extractValue(from:)` helper added so coverage tests accept stringUnit-flat OR variations-plural shapes uniformly) + `b97de70` smoke pre-flight (new `.smoke/scenarios/00-bundle-integrity.sh` runs first via 00- prefix; verifies 11 .lproj + Assets.car + Info.plist via `~/dev/smoke-harness/lib/assert_bundle_resources.sh` cross-project helper; ~50ms file-system inspection, no app launch; closes the smoke-layer gap when tests are skipped during owner manual triage) + `3940098` TRANSLATIONS.md plural variations section (community-PR contract for native speakers of Russian/Polish/Arabic/Czech to add missing one/few/many/other forms; explicit "other-only is correct" for ko/ja/zh languages). **Tests 610/610 PASS** (8s); **doc-drift clean**; **bundle integrity pre-flight verified** against fresh Release .app (happy path + bad-count + missing-bundle exit codes 0/1/3). **S35 NEW patterns (6)**: (a) **GitHub Actions parallel job on ubuntu-latest for static checks** — separating macOS-Xcode work from bash/python/awk work keeps PR check budget cheap while expanding coverage. (b) **xcstrings variations.plural with mixed-form language strategy** — en/de/es/fr/it/pt-BR/ru get `one` + `other`; ko/ja/zh-Hans/zh-Hant get `other`-only (no grammatical plural — CLDR-aligned). The catalog ships with two forms per multi-form language as a starting point; Russian/Polish/Arabic native speakers can extend to one/few/many/other via community PR (TRANSLATIONS.md flow). (c) **Swift `String(localized: "\(intVar) X")` auto-inflates plural variations** — interpolating an Int triggers `%lld X` key lookup, then `variations.plural` form selection by host locale's CLDR rule. The call-site ternary (`x == 1 ? "1 X" : "\(x) Xs"`) becomes one line; the catalog owns the form-selection logic. (d) **`extractValue(from:) helper for catalog-coverage tests** — coverage tests that walked only `stringUnit.value` paths break the moment any key adopts `variations.plural`; a single helper that prefers `variations.plural.other` (always present per CLDR) and falls back to other forms keeps the coverage invariant intact across both shapes. (e) **Smoke pre-flight scenario via filename prefix** — `00-` prefix in `.smoke/scenarios/` runs first under alphabetical iteration; the scenario does file-system inspection only (no app launch), so it's a fast guard (~50ms) for "is the bundle even well-formed before we burn 6 min on UI scenarios". (f) **Cross-project helper in owner-local tree + per-project scenario in repo** — the S28 split pattern reapplied: `~/dev/smoke-harness/lib/assert_bundle_resources.sh` (owner-local, serves all Apple projects) + `.smoke/scenarios/00-bundle-integrity.sh` (repo, Latte-specific count). Future Apple projects can adopt the helper with their own count without owning harness changes. **Owner-blocked queue (unchanged)**: S8.5 Apple Dev (Day 15), S9 ASC. Phase I owner-visible surfaces complete since S34; v1.x autonomous backlog now functionally exhausted (remaining items either owner-tooling-blocked or require multi-session design — see SESSION_HANDOFF v2 long-tail). |
| 1.34 | 2026-05-16 | **S34 — Phase I Chunks 2-4 (MenuBar + Settings + Activity + About) + S33-lesson safety net** (2026-05-16, post-S33 follow-up; same v1.9 release line). Owner direction: "차곡차곡 검증 오류 최소화" — pick highest-value autonomous work, chunk-verify, run as deep as context allows. 6 commits: `a169030` BundleIntegrityTests (3 new XCTests guarding S33 P0 class — bundle has 11 .lproj + Assets.car + ko resolves "Awake" → "깨어 있음" — Bundle.main = host Latte.app inside macOS unit test; defensive walk-up fallback) + `f07ef55` Phase I Chunk 2 MenuBar surface (21 keys × 10 langs = 210 cells; HeaderView statusTitle/Subtitle + AwakeDuration.label `String(localized:)` wrap; SwiftUI auto-localised literals in MenuBarRoot + CustomDurationRow + AwakeDurationTests locale-agnostic fix) + `807a75c` Phase I Chunk 3a TriggerConfigForms (25 new × 10 = 250 cells, 19 short labels reused from S31/32; filterDescription refactor to inline ternary; external-display "Currently 1/N display(s)" split into two cases) + `888f054` Phase I Chunk 3b ActivityTab (9 new × 10 = 90 cells, 12 reused; RetentionPicker.label wrap) + `1ebd257` Phase I Chunk 4 Settings/About leftovers (12 new × 10 = 120 cells; SettingsWindowController title wrap; DemoCupWindowController title intentionally verbatim because it's a CGWindowListCopyWindowInfo lookup token) + `1ce33b7` check_doc_drift.sh + carryover URL/test-count fixes (README 607 → 610; validate_pages.sh bj-park → spespark hint text). **Catalog grew 108 → 175 keys (+67 new, +12 S31/32 reuse) × 10 non-en langs = 1750 cells**. Tests 607 → 610 (BundleIntegrityTests adds 3). Smoke 22/22 (unchanged, gated by smoke harness). Owner-visible: ko users now see fully Korean MenuBar popover + all 5 trigger config forms + Activity tab + About tab + Settings window title. **S34 NEW patterns (6)**: (a) BundleIntegrityTest pattern — `Bundle.main` resolves to host app inside macOS hosted unit test (`Latte.app/Contents/PlugIns/LatteTests.xctest` layout); test asserts bundle has all 11 .lproj + Assets.car + spot-checks one ko translation actually resolves; runs every test cycle. (b) SwiftUI `Text(condition ? "literal" : "literal")` ternary resolves as LocalizedStringKey when both branches are literals — eliminate helper returning String; inline ternary keeps single localization mechanism. (c) Plural-aware string design — instead of one key with embedded ternary like `"Currently: \(n) external \(n == 1 ? "display" : "displays")"`, split into two `String(localized:)` cases (1-form + plural-form) so each language can pick its own plural shape cleanly. (d) Window-title verbatim invariant — `NSWindow.title` that's used as a CGWindowListCopyWindowInfo lookup token MUST NOT be localized; flag in code comment + skip in i18n sweep. (e) S31/32 catalog reuse — short labels like "From"/"To"/"Mode"/"Add"/"Watched calendars" recur across multiple Settings tabs; catalog scan + skip-if-present is mandatory pre-merge step to avoid duplicate-cell debt. (f) check_doc_drift.sh as periodic guard — test-count claim in README vs static `func test…` count; stale URLs scoped to *current surfaces* (README/project.yml/scripts/.github) while ROADMAP/SESSION_HANDOFF preserve historical references as audit trail; catalog vs project.yml knownRegions cross-check. **Phase I now functionally complete across all owner-visible surfaces**. **Owner-blocked queue (unchanged)**: S8.5 Apple Dev Program (Day 15 of wait), S9 ASC metadata. |
| 1.33 | 2026-05-15 | **S33 — community PR groundwork + Phase I Chunk 1 (Onboarding body i18n)** (2026-05-15, same-day continuation of S32). 2 commits: `da56770` README/i18n community PR groundwork (TRANSLATIONS.md contributor guide + .github/ISSUE_TEMPLATE/translation_improvement.md + README Internationalization section + bj-park.github.io → spespark.github.io drift fix + 586 → 607 test count drift fix) + `13c7ad4` Phase I Chunk 1 (Onboarding body 22 keys × 10 langs = 220 cells; xcstrings 86 → 108 keys, 940 → 1188 cells; OnboardingView.swift `String(localized:)` wrap of `doneSummary` + `description(for:)` helpers, %@-format key for name interpolation). **Build verified** (xcodebuild build SUCCESS + xcodebuild build-for-testing SUCCESS); **tests not run** due to persistent macOS Pseudo Terminal Setup Error (CoreSimulator 1051.50/1051.54 framework drift; survives `simctl shutdown all` + DerivedData clear; expected to recover after reboot). Test count 607 → 607 expected (no test added; existing `testEveryEntryCoversAllPhaseHLanguages` enforces 10-lang coverage of new keys). Smoke not run (same PTY blocker). **S33 NEW patterns (4)**: (a) TRANSLATIONS.md as community-PR contract — explicit "machine-assisted, awaiting native review" quality bar per language; two contribution paths (issue template + direct PR); test contract documented so contributors know what guardrails exist. (b) `String(localized:)` `\(arg)` interpolation auto-folds to `%@`-format key in xcstrings — Swift converts `\(names)` to `%@` placeholder, catalog key reads `"…based on: %@."`, runtime substitutes at format time. (c) **PTY blocker discovery**: parallel swift-test in another project (`MacSuiteUI`) holds `testmanagerd` PTY; even after the other test exits, the CoreSimulator framework-version drift (Xcode update mid-session?) leaves PTY in a bad state — `xcrun simctl shutdown all` repairs CoreSimulator state but does NOT unblock PTY allocation; reboot is the reliable fix. (d) Owner principle override: "차곡차곡 검증" gates further code commits when tests can't run; pivoted to non-test-dependent doc/meta work (ROADMAP rows, SESSION_HANDOFF, memory wrap) to use remaining context safely. **Owner-blocked queue narrows further**: S8.5 Apple Dev (Day 13/14), S9 ASC. Phase I Chunks 2-4 (MenuBar / ActivityTab / status notifications) deferred to S34 post-reboot. |
| 1.32 | 2026-05-15 | **S32 — i18n cluster close-out: Phases D + E + H + ko refinements** (same-day continuation of S31). 3 commits: `95e4a6c` Phase D (Onboarding LanguageStep — 11 native-name button grid prepended as first wizard step; `LanguagePreference.apply` writes AppleLanguages immediately) + Phase E (Settings → General Language Picker with "Restart Latte to apply." caption); `1284c9e` Phase H bulk translation (86 keys × 9 langs = 774 cells for ja/zh-Hans/zh-Hant/es/de/fr/pt-BR/it/ru via Python dict-script in `/tmp/`, Apple platform terminology per locale) + 3 ko refinements (`Enable` 사용 → 활성화, `Reset` 기본값 → 초기화, `Watched calendars` 감시 중인 → 관찰 중인); `71b55a4` SESSION_HANDOFF wrap. Tests 594 → 607 (+12 LanguagePreferenceTests + 1 testEveryEntryCoversAllPhaseHLanguages). Smoke 22/22. xcstrings 86 keys × 11 langs = all 940 cells populated (22KB → 79KB). Onboarding flow: language → welcome → pickTriggers → done. **S32 patterns NEW (5)**: UserDefaults(suiteName:) chains through NSGlobalDomain for system keys (split `current(in:)` + pure `resolve(rawValues:)`); region-tagged language fall-through with prefix folding (ko-KR → ko, pt-PT → pt-BR); Step enum prepend pattern for wizard insertion (5 surgical touchpoints); bulk LLM translation as hand-writable Python dict-script (774-cell dict, JSON-merge); per-language gap-report test (`gaps: [Lang: [Key]]` accumulator → assert empty → actionable diff). |
| 1.31 | 2026-05-15 | **S31 — i18n P1 anchor: xcstrings infrastructure + Settings ko translations** (Phase A+B+C+F). 3 commits: `e1a56a7` xcstrings infra (project.yml knownRegions = 11 langs, SWIFT_EMIT_LOC_STRINGS=YES, LOCALIZATION_PREFERS_STRING_CATALOGS=YES; Resources/Localizable.xcstrings = 86 keys en source + ko translations; AssertionStatusFormatter + CoffeeAccent.shortDescription `String(localized:)` wraps) + `7842b60` SESSION_HANDOFF + memory wrap + `6c6f955` chore: latte-resume.sh one-command cold-start ritual (status check + i18n catalog summary auto-surface). Tests 587 → 594 (+7 LocalizationCatalogTests: sourceLanguage=en, version=1.0, every-key-has-ko, ko != en source, String(localized:) resolves en, formatter strings non-empty). Smoke 22/22. Owner direction: "11-language ambitious i18n with LLM-assisted ship + community PR for review". P1 anchor = ko (developer-native); Phase H bulk translation deferred pending owner ko review. |
| 1.30 | 2026-05-15 | **S30 — URL drift fix + matcha theme + i18n proposal** (cluster, owner-driven). Tail commit `3a30e61` (.matcha CoffeeAccent — green pivot, 6-tone picker; `afa1980` SESSION_HANDOFF wrap) + earlier commits in cluster: URL drift bj-park → spespark fixes across docs/v2-backlog/ROADMAP/site README; .matcha 6-tone CoffeeAccent (Light beige, Warm amber, Caramel, Deep brown, Mid-tone brown, Green tea/Espresso); i18n P1 proposal drafted (owner asked Y on "translate accent shortDescription"). Tests 587/587 unchanged through URL fixes; +pattern coverage assertion for .matcha. Cluster set up the conditions for S31 anchor work (CoffeeAccent.shortDescription wrap landed in S31). |
| 1.29 | 2026-05-15 | **S29 — S8d Pages unlock + V2-22 GitHub remote** (infrastructure unlock, no Latte source touched). Owner decisions resolved: username `SpesPark` (post-availability-probe; `Spes`/`bj-park` both taken), `gh` CLI path with browser OAuth (Google SSO), HTTPS+token (no SSH key). `gh repo create SpesPark/latte --public --source=. --push` from main repo + `git push -u origin gh-pages` from staging worktree + Pages auto-enabled by gh-pages branch push (409 already enabled). Live: https://spespark.github.io/latte/ + /privacy.html (200, validated). V2-22 closed as side effect. Tests 587/587 unchanged. Smoke 22/22 unchanged. |
| 1.28 | 2026-05-05 | **S28 — smoke harness rebuild gate + feature-presence assertion** (cross-project infrastructure close-out for S27 stale-binary discovery). 2 commits in this repo (`beae984` config-yml, `7d10ef4` scenarios) + harness changes in owner-local `~/dev/smoke-harness/` (run.sh / lib/assert_binary_type.sh / README). Tests 587/587 unchanged. Smoke 22/22 PASS on fresh binary. Step C (S8d) deferred — owner decisions (GitHub username / gh vs web / SSH key) pending. |
| 1.27 | 2026-05-05 | **S27 v2 — stale-binary discovery** (post-wrap). v2 docs commit + S22~S27 P-issue re-verification queue + smoke harness rebuild gap acknowledgement. Tail commit `5bddc8e`. |
| 0.1 | 2026-04-25 | Initial draft (session 1) |
| 0.2 | 2026-04-25 | Session 2 complete; design phase closed; sessions 1–2 marked done, session 3 marked next |
| 0.3 | 2026-04-25 | Session 3 complete; Phase 1.0 code + tests landed; sessions 1–3 done, session 4 marked next |
| 0.4 | 2026-04-26 | Session 4 complete; Phase 1.A trigger real wiring + Source DI pattern + tests; sessions 1–4 done, session 5 marked next |
| 0.5 | 2026-04-26 | Session 5 complete; Phase 1.C design polish — Canvas+TimelineView coffee cup, Liquid Glass card, menu bar icon variants, Settings typography, TriggersTab vote indicator, `05-icon-spec.md`; sessions 1–5 done, session 6 marked next |
| 0.6 | 2026-04-26 | Session 6 complete; first green Xcode build (Xcode 26.4.1) + all 178 tests passing; fixes: `static let` for AppIntent properties, `(1, 1440)` literal for `@Parameter` range, `NSApp.sendAction` fallback for `openSettings` (macOS 13 compat), first-OFF silence semantic for WiFi/Focus triggers (consistent with AppTrigger); sessions 1–6 done, session 7 marked next |
| 0.7 | 2026-04-26 | Session 6 (extended) — post-build smoke surfaced gaps; fixed: `SettingsWindowController` (NSHostingController-based; `Settings { }` scene + `SettingsLink` don't wire reliably for LSUIElement menu-bar apps), espresso-brown accent via `NSColor` dynamic provider (Canvas doesn't resolve `Color.accentColor` / `Color.primary` against current appearance), `Theme.Colors.cupStroke` explicit `NSColor.labelColor`-based dynamic, `DurationPickerRow` hover state + caramel accent bar + dimmed Turn-off, divider opacity 0.5, **custom duration α** (`CustomDurationRow` — inline 1–1440-min Stepper + Start). All 178 tests still pass. |
| 1.2 | 2026-04-27 | **Session 8b smoke fix-first iteration** (4 commits on top of `a4a3ce9`). Owner ran the new build through the §S8b smoke checklist; each round surfaced a P1 that was fixed before moving on. Tail commit `31389cd`. **Round 1 (`aa42e90`)** — owner reported filled+outline icon styles indistinguishable + Calendar trigger silent failure; first fix collapsed all 3 awake variants onto one steam glyph + added `requestPermissionIfNeeded()` to TriggersTab Toggle ON path (Settings was the only enable path that bypassed the permission check, leaving CalendarTrigger.pollOnce silent). **Round 2 (`45e73fc`)** — owner pushed back: "filled and outline shouldn't share the awake glyph either, each style needs its own steam variant"; per-style paired SF Symbols re-introduced. **Real root cause discovered** for the OFF→ON Toggle dead-cup bug: `TriggerCoordinator.stop()` was cancelling the consumer Task that iterated `trigger.voteStream`, which terminates the underlying AsyncStream's storage via the iterator's task-cancellation handler — even though `continuation.finish()` is never called (the S7.9/7.11 fix). Future yields after a `start()` were silently dropped. Fix: keep consumer Tasks alive across the trigger's registered lifetime; `stop()` only halts emissions and synthesises the user-explicit vote-OFF. Plus `MockTrigger.stop` no longer calls `continuation.finish()` so tests now exercise the same lifecycle. `testToggleOffOnCycleReActivatesCup` + `testRepeatedToggleCyclesAllReActivate` regression guards added — both fail on `aa42e90`/earlier, pass here. **Round 3 (`acea09a`)** — owner reported popover rendering alongside the onboarding wizard at first launch (boot logic was inside the popover's `.task`, fired when SwiftUI materialised the content view). Moved boot to `LatteAppDelegate` (`@NSApplicationDelegateAdaptor`); `MenuBarExtra(isInserted:)` bound to a `@Published menuBarVisible` flag synced to `OnboardingState.hasCompletedOnboarding` via Combine sink, so the menu-bar icon hides while the wizard is up. Added structured info logs to `FocusTrigger.evaluate()` for owner-side diagnosis. **Round 4 (`31389cd`)** — owner shared the diagnostic logs: `currentlyActive=false isEnabled=true permission=granted configured=true` repeating across multiple Toggle cycles, even with system Focus mode active. Confirms macOS sandboxed apps need `com.apple.developer.usernotifications.communication` entitlement to read `INFocusStatusCenter.focusStatus.isFocused` reliably (the 19:32 DoNotDisturb log entry was the smoking gun). Decision: defer Focus trigger from v1.0 ship — `AppEnvironment.registerDefaultTriggers` no longer registers `FocusTrigger`; onboarding wizard and Settings → Triggers automatically hide it (both iterate `coordinator.triggers`). Tracked as **V2-03b** in `docs/v2-backlog.md` with the action checklist for re-enabling after Apple Dev Program. Same commit also fixes V2-02 for WiFi+Calendar — config form commits now immediately call `trigger.evaluate()`/`trigger.pollOnce()` so add/remove of SSIDs and lead/trail/picker changes reflect within one render pass instead of waiting up to 30s/60s for the next poll. 284 → 288 tests, all pass. **V2-30 NEW**: cross-project smoke harness scoped for S8c (owner planning 10+ macOS apps; harness amortises after 2). |
| 1.1 | 2026-04-27 | **Session 8b — v1.0 scope expansion** (owner approved "ship aggressively" after S8b research). 4 commits on top of `87d1eab`: `05f8c2d` V2-01 menu-bar awake visualization (per-style paired SF Symbol; LatteApp observes `manager.isAwake` so MenuBarExtra re-renders on transitions; closes a 15-year category-standard UX gap, KYA #57) + V2-10 `accentAwake` cleanup; `6109859` Launch at Login (`SMAppService.mainApp` wrapper + `LaunchAtLoginCoordinator` ObservableObject + GeneralTab toggle; reconciles stale settings cache to live service status on init); `7decb84` V2-04 EKCalendar list picker (`CalendarSummary` value-type projection of EKCalendar id/title/color/source-title; `CalendarSource.availableCalendars()` protocol method; multi-select picker UI with "Select all" / "Use all calendars" affordances; empty selection preserves v1 default behavior, no migration); `2da1f3d` first-run onboarding wizard (3-step picker with welcome → trigger cards → confirm; `OnboardingState` ObservableObject backed by `SettingsKey.firstRunCompleted`; `OnboardingWindowController` mirrors `SettingsWindowController` LSUIElement pattern). 263 → 284 tests, all pass. v2-backlog.md updated (V2-01/04/10 marked shipped; v1.1 ship order reshuffled). PRD §1 v0.3 → v0.4 documents the 5 additions. Description-en/ko + V2-11 owner guide added. |
| 1.0 | 2026-04-27 | **Session 8a complete — code & metadata prep for App Store** (no Dev Program required). Row 8 split into 8a (code & metadata, this commit, Done) / 8b (owner pre-flight smoke, Next) / 8.5 (Apple Developer Program enrollment, owner-only HARD gate). Bundle ID changed from `com.example.latte` placeholder to default `com.parkbyeongjun.latte` across 6 source/config sites + 4 design docs; `MARKETING_VERSION` bumped to `1.0.0`; project folder renamed `Caffeinated-Clone/` → `Latte/`. New asset trees: `docs/site/` (GitHub Pages landing + Privacy Policy, dark-mode-aware HTML, deployable to `https://bj-park.github.io/latte/`) and `docs/store/` (14 metadata text files for App Store Connect paste, en + ko localized; screenshot capture guide; reviewer notes). New `docs/v2-backlog.md` consolidates 7 pre-existing deferred items (V2-01..V2-12) plus 3 new owner-revisit items captured during S8 (V2-20 bundle ID prefix, V2-21 git author, V2-22 GitHub username/repo). 263/263 tests still passing; Release config build verified clean. `02-architecture.md` v0.16 → v1.0 (design freeze gate). |
| 0.16 | 2026-04-27 | Session 7.11 — **Stream-lifecycle parity for Calendar/WiFi/Focus triggers + first AppIcon raster set**. Latent bug surfaced during the post-S7.10 review: the S7.9 stream-lifecycle fix (don't call `continuation.finish()` in `stop()` so Toggle OFF→ON cycles work) was applied to `AppTrigger` only. `CalendarTrigger.stop()`, `WiFiTrigger.stop()`, and `FocusTrigger.stop()` still finished their continuations — meaning if the owner ever toggled any of those three OFF and back ON, the stream would be permanently closed and future yields silently dropped, with the cup never re-activating. Owner is currently using App trigger so the bug stayed latent, but it was clearly a regression risk for any future smoke pass that touches the other triggers. Fix: removed `continuation.finish()` from all three `stop()` methods, identical to the AppTrigger fix; added the same explanatory comment in each. Existing tests in `Calendar/WiFi/FocusTriggerTests` that read "stream finishes after stop" were updated to verify "no further votes are yielded after stop" via a bounded-timeout `Task.cancel()` probe (matches the AppTrigger test pattern). 261 → 263 tests, all pass. Coverage stays PASS (CalendarTrigger 98.79%, WiFiTrigger 96.61%, FocusTrigger 97.98%, AppTrigger 98.62%, AwakeManager 94.26%, all gated files ≥ 80%). **Owner-supplied app icon**: a 1254×1254 master ("Latte icon.png") was provided. Generated the 10 macOS AppIcon raster sizes (16/32/64/128/256/512/1024 covering 1x/2x of 16/32/128/256/512) into `Resources/Assets.xcassets/AppIcon.appiconset/`; updated `Contents.json` with the matching `filename` keys per Apple's asset catalog format. Master and a 1024×1024 marketing variant filed under `docs/design/assets/icon-master-1254.png` + `icon-1024.png` for future App Store / press use; original repo-root file moved out so we don't ship a stray PNG. F-1.C.04 (App icon) per `docs/design/05-icon-spec.md` is now ready as far as the asset bundle goes — Icon Composer / dark-tinted variants remain optional follow-ups for owner. **This commit closes the S7-family iteration** (S7 → S7.11, eight sub-sessions across two days, 178 → 263 tests, 16 incremental ROADMAP version bumps). The remaining S8 work is owner-side: Apple Dev Program enrollment, bundle ID prefix decision, folder rename `Caffeinated-Clone/` → `Latte/`, and one more re-smoke against the S7.11 build to confirm the bug list is empty before App Store submission. |
| 0.15 | 2026-04-26 | Session 7.10 — **Per-trigger grace replaces blanket 60 s cool-down (state machine surgery + UX win)**. Owner re-smoke against S7.9 confirmed Toggle OFF and watched-list edits *do* propagate through the system, but the cup stayed activated for 60 seconds because the state machine always entered `.coolingDown(60s)` on the last vote-off — and `.coolingDown` is `isAwake = true`. To owner that read as "비활성 안됨." Cross-app analysis (Amphetamine, Owly, KeepingYouAwake, Caffeinated, Theine) showed no leading sleep-prevention app uses a hidden cool-down; competitors transition immediately. Combined with the fact that Latte's `.asleep` only releases the IOPMAssertion (macOS still respects its own 5–15 min idle timeout before actually sleeping), the cool-down was largely working invisibly underneath the OS timer with no perceptible benefit. **Fix**: replace the blanket cool-down with **per-trigger grace**. `Trigger` protocol gains `var graceSecondsAfterOff: TimeInterval { get }` (default-impl extension returns 0). `TriggerVote` gains `graceSecondsAfterOff: TimeInterval` (Sendable, default 0). `AwakeInput.triggerVoteOff` extends from `(id: String)` to `(id: String, graceSeconds: TimeInterval)`. `AwakeStateMachine.step` for `(.awakeTriggered, .triggerVoteOff(id, grace))`: when `newVotes.isEmpty`, fork on `grace == 0` → `.asleep` + `.releaseAssertion` (no timer) vs `grace > 0` → existing `.coolingDown` path. `AwakeManager.receiveTriggerVote` extracts grace from vote when forwarding. `TriggerCoordinator.stop` (Toggle OFF path) synthesizes its vote-OFF with grace=0 explicitly — user-explicit actions always bypass the trigger's declared grace. `AppTrigger.reevaluateWatched` does the same on user-driven list edit. `AppTrigger.handleTerminate` (organic) emits with `self.graceSecondsAfterOff` so triggers can opt into a grace period if they ever override the default. **v1: all 4 triggers stay at grace = 0**, so every OFF (Toggle, list edit, app-terminate, calendar-end, WiFi-leave, Focus-off) releases the assertion immediately. Cool-down infrastructure is preserved for future opt-in (e.g., production telemetry surfaces a real WiFi-blip flicker → override `WiFiTrigger.graceSecondsAfterOff: 30`, no state machine surgery needed). 7 test sites updated: §8 worked-example tests for cool-down behavior now pass `graceSecondsAfterOff: 30` explicitly; new tests cover both forks (`testAwakeTriggered_triggerVoteOff_lastVote_grace0_goesToAsleepImmediately` + `testAwakeTriggered_triggerVoteOff_lastVote_gracePositive_goesToCoolingDown`) plus AwakeManager-level integration of `receiveTriggerVote` dispatching grace correctly. The S7.9 owner-scenario integration tests in `TriggerCoordinatorTests` were tightened from "coolingDown OR asleep" to strict "asleep". 255 → 261 tests, all pass. Coverage gate still PASS (AwakeManager 94.26%, AppTrigger 98.62%, TriggerCoordinator 92.96%). `03-state-machine.md` v0.2 documents the new design with full revised rationale; `02-architecture.md` v0.15 §13 captures the architectural surgery. |
| 0.14 | 2026-04-26 | Session 7.9 — **Trigger lifecycle fix: Toggle OFF + watched-list edit (fix-first after S7.8 smoke)**. Owner ran the S7.8 build and confirmed positive cases work (enable → cup activates) but flagged that negative cases were broken: Toggle OFF and remove-app-from-watched-list both left the cup activated indefinitely. Two structural bugs fixed at once. **(A) Toggle OFF only persisted the flag** — `TriggerSection`'s `Toggle.onChange` set `trigger.isEnabled = false` but did nothing to the live trigger. The `WorkspaceObservation` stayed alive, the consumer Task in `TriggerCoordinator` kept forwarding stale ON votes, `awakeManager.pendingVotes` retained the entry, `manager.isAwake` stayed true. Fix: pass `coordinator: TriggerCoordinator` into `TriggerSection`; the Toggle's `onChange` now spawns a `Task { @MainActor in … }` that calls `coordinator.start(trigger)` on ON or `coordinator.stop(trigger.id)` on OFF — and `coordinator.stop` already synthesizes a vote-OFF that flips state out of `.awakeTriggered`. Universal across all 4 triggers (Calendar / App / WiFi / Focus). **(B) AppTrigger captured `watched` once at start()** and froze it into the `onLaunch:` / `onTerminate:` closures, so removing an app from the watched list left the trigger using a stale set: `matchingRunning` retained the un-watched app, no vote-OFF emitted, cup stayed on. Fix: `AppTrigger.watchedSet` is now an instance var (set in `start()`, cleared in `stop()`); `handleLaunch/handleTerminate` simplified to read it; new public `AppTrigger.reevaluateWatched()` reads the latest `settings.appTriggerBundleIDs`, recomputes `matchingRunning`, and emits vote ON/OFF/re-emit-ON-with-fresh-reason on transition. `AppTriggerConfigForm.commit(_:)` calls it after writing settings, so adds/removes propagate within one render pass. **Stream lifecycle hardening**: removed `continuation.finish()` from `AppTrigger.stop()` — finishing the AsyncStream permanently silenced the trigger across any future Toggle OFF → ON cycle. Stream now stays open for the trigger's lifetime; the coordinator cancels its consumer Task on stop, recreates one on start, and the trigger keeps yielding votes from a single long-lived stream. New regression test `testRestartAfterStopReEmitsInitialSnapshot`. 6 new tests total (5 reevaluateWatched + 1 restart-after-stop), `testStopCancelsObservation` comment refreshed for the lifecycle change. 249 → 255 tests, all pass. Coverage gate still PASS (AppTrigger 98.06% — slight drift from 98.89% reflects the new reevaluate branches; well above 80% floor). The same "watched-list mutation doesn't reach the live trigger" pattern likely affects WiFi/Calendar/Focus too — deferred to S7.10 if owner re-smoke surfaces it; the universal Toggle fix already covers "disable this trigger entirely" for all 4. |
| 0.13 | 2026-04-26 | Session 7.8 — **App trigger pickable filter + installed-only seed (fix-first after S7.7 smoke)**. Owner re-smoke against S7.7 surfaced 3 connected complaints: (1) curated defaults pre-populate apps the owner doesn't have installed (creating an unfamiliar list with broken-looking icons), (2) "Add from running apps" Menu items show names without icons, (3) the same Menu lists every running process (system daemons, menu-bar utilities, Latte itself). Fixed at the root rather than cosmetically: `WorkspaceSource` extended with `pickableRunningBundleIDs: [String]` (real impl filters `.regular` activation policy + excludes self via `Bundle.main.bundleIdentifier`) and `isInstalled(_:) -> Bool` (real impl checks `NSWorkspace.urlForApplication(withBundleIdentifier:)`). New `AppTriggerDefaults.installedDefaults(in:)` filters the 6 curated IDs to those installed on the user's machine. New `SettingsKey.hasSeededAppDefaults` Bool flag persists "we've seeded once." `AppTrigger.init` runs `seedInstalledDefaultsIfNeeded()` — once per install — that writes installed-only curated defaults to the watched list and sets the flag (idempotent across re-launches; respects users who explicitly cleared their list). The legacy fallback in `SettingsStore.appTriggerBundleIDs` getter (return all 6 curated IDs when raw is empty) is **removed** — empty now means watch nothing, and seeding is the one place curated defaults populate. UI: TriggersTab Menu uses `pickableRunningBundleIDs`, items render via `Label { Text(name) } icon: { Image(nsImage:) }` with SF Symbol category fallback (`video.fill` for video-call apps, `bubble.left.and.bubble.right.fill` for chat apps); `AppRow` uses the same SF Symbol fallback; empty-state copy refreshed to invite users to add their own apps. New `AppTriggerDefaults.symbolHint(for:)` exposes the category mapping. 14 new tests (symbol hint × 2, isInstalled × 3, installedDefaults × 2, first-launch seed × 4, pickable × 3); `testRequiredKeysExist` extended with the new SettingsKey. 235 → 249 tests, all pass. Coverage gate: AppTrigger 98.89% (up from 98.68% in S7.7); all gated files ≥80%. |
| 0.12 | 2026-04-26 | Session 7.7 — **App trigger UX friendly names (fix-first after S7.6 smoke)**. Owner reported S76-DEF-01: App trigger row showed raw bundle IDs (`us.zoom.xos`) and the trigger had no purpose copy, making it opaque to non-technical users. `WorkspaceSource` protocol gains `displayInfo(for:) -> AppDisplayInfo?` returning a Sendable struct of `displayName` + optional PNG-encoded icon data. `NSWorkspaceSource.displayInfo(for:)` resolves in priority order: **running app** (`NSRunningApplication.localizedName`/`icon`, downsampled to 64pt) → **installed bundle** (`NSWorkspace.urlForApplication(...)` + `Bundle.localizedInfoDictionary`/`infoDictionary` + `NSWorkspace.icon(forFile:)`) → **curated default** (`AppTriggerDefaults.displayName(for:)` table covering Zoom/Microsoft Teams/Webex/Discord/Slack/Google Meet) → `nil`. `MockWorkspaceSource.displayInfoLookup` stubbable for tests; falls through to curated table. `AppTrigger.displayInfo(for:)` is a thin passthrough so SwiftUI never touches the source directly. `AppTriggerConfigForm` rewritten: new `AppRow` view (real icon + display name body line + bundle ID muted caption when distinct), purpose-explaining caption at the top, "Add from running apps" Menu items now use display names (sorted case-insensitively), manual bundle-ID input collapsed under a `DisclosureGroup` labelled "Advanced — add by bundle ID" closed by default (label is non-interactive caption per S7.6 lesson, so DisclosureGroup is safe here). 6 new tests — `AppTriggerDefaults.displayName(for:)` curated mapping + unknown-ID nil, `MockWorkspaceSource.displayInfo` curated fallback + override priority + nil-for-unmapped, `AppTrigger.displayInfo` passthrough. 229 → 235 tests; gate still PASS (AppTrigger 98.68% with adapter+inner-closures excluded). Zero persistence/architecture changes — friendly resolution is purely a render-time concern. |
| 0.11 | 2026-04-26 | Session 7.6 — **TriggersTab UX rewrite (fix-first after S7.5 smoke)**. Owner reported subtitle/Toggle state mismatch ("켜져 있을 때 disabled로 표시되는") + general "복잡하고 직관적이지 않음." Root cause: DisclosureGroup label held both the row HStack and the Toggle, so SwiftUI hit-testing fired the disclosure expand and the Toggle on overlapping click areas; meanwhile `subtitle` read `trigger.isEnabled` (SettingsStore) while the Toggle UI showed `@State var isOn`, so any onChange miss left the two visibly out of sync. Plus the `if newValue { isExpanded = true }` side-effect inside onChange added a second mutation in the same transaction. Rewritten as **Section per trigger** (`Form { ForEach { TriggerSection } }`): each trigger is its own `Section`, with a single standalone `Toggle("Enable")` row (no click-target collision possible), and the per-trigger config form rendered inline below the toggle when `isOn`. Section header carries icon + name + voting dot; section footer carries the live "Voting awake — …" reason or permission-status hint. `subtitle` helper deleted; the Toggle itself is the source of truth for enabled state. `AppTriggerConfigForm` "Add from running apps" was a nested DisclosureGroup — replaced with a `Menu` showing the candidate list as standard macOS dropdown. `WiFi` mode picker upgraded from `.radioGroup` to `.segmented` for clearer two-state choice. `id: \.offset` → `id: \.id` on the outer ForEach for explicit identity. Zero protocol/architecture changes; SwiftUI-only restructure. 229/229 tests still pass; coverage gate still PASS. |
| 0.10 | 2026-04-26 | Session 7.5 — **Phase 1.5.A: per-trigger config UI**. The S5 deferred backlog item lands. `Sources/UI/Settings/TriggersTab` is rewritten around `DisclosureGroup`; tapping a trigger row reveals a per-trigger configuration form: **App** (bundle-ID list + remove + manual add + "Add from running apps" picker fed by `AppTrigger.runningBundleIDs` passthrough), **Wi-Fi** (SSID list + remove + manual add + "Add current network" using `WiFiTrigger.currentSSID` + on/inverse mode picker), **Calendar** (lead/trail steppers 0–15 min + exclude-all-day toggle; the EventKit calendar picker itself is intentionally deferred to a future Phase 1.5.B since it needs live `EKCalendar` listing). **Focus** shows an explanatory paragraph since `INFocusStatusCenter` does not expose stable per-Focus IDs to third parties in v1. New public passthroughs `AppTrigger.runningBundleIDs` + `WiFiTrigger.currentSSID` (both delegate to their `*Source` adapters; tests added). 229/229 pass; coverage gate still PASS (`Triggers/AppTrigger` 98.5%, `Triggers/WiFiTrigger` 96.6%). No architecture-level changes — pure UI + 2 small public-API additions on existing types. |
| 0.9 | 2026-04-26 | Session 7 complete — **coverage + QA gate**. Coverage baseline (`xcodebuild -enableCodeCoverage YES`) + per-file analysis revealed 4 files under 80% (`SettingsStore` 75.2%, `PowerAssertion` 29.6%, `WiFiTrigger` 60.2%, `CalendarTrigger` 52.0%) once live-system adapters were excluded. Closed the gaps: `SettingsStore` 100% (added `double` round-trip + 5 type-mismatch fallback tests), `PowerAssertion` 87.3% (new `Tests/PowerAssertionTests.swift` exercising real `IOPMAssertion*` APIs — promoted off the adapter exemption list since IOKit power assertions need no entitlement), `WiFiTrigger` 96.5% (start/stop lifecycle, `requestPermissionIfNeeded`, isEnabled setter, permissionStatus getter), `CalendarTrigger` 98.8% (start/stop, isEnabled/permissionStatus, typed settings setters). 227/227 tests pass, all gated files ≥80%. New `docs/QA_LOG.md` consolidates the coverage snapshot, owner-side smoke checklist, adapter exemption rationale, and macOS 13/14/15 matrix deferral to TestFlight. |
| 0.8 | 2026-04-26 | Session 6 (extended again) — **coffee tone customization**. New `CoffeeAccent` enum with 5 hand-tuned presets (Espresso / Caramel / Mocha / Latte / Noir), each carrying light + dark sRGB pairs resolved via `NSColor(name:dynamicProvider:)`. Persisted under new `SettingsKey.coffeeAccent`, mirrored on `AppEnvironment.@Published var coffeeAccent`. Settings → General → Appearance gains a Coffee tone Picker with color-dot previews, plus a small `CoffeeCupView` preview at the top of the Appearance section so the user sees the new tone live without leaving the window (the menu-bar popover closes when Settings takes focus). Five views consume `environment.coffeeAccent.color` (`HeaderView`/`CoffeeCupView` liquid, `DurationPickerRow`/`CustomDurationRow` accent bar + checkmark, `GeneralTab` status dot, `TriggersTab` voting indicator, `AboutTab` cup). 17 new tests (12 `CoffeeAccentTests` + 5 `AppEnvironmentTests` for accent ⇄ store mirror); 195/195 pass. |
