https://<github-username>.github.io/<repo>/

Notes:
- Replace placeholder with deployed GitHub Pages URL (or custom domain if owner buys one).
- Optional in App Store Connect, but recommended — gives reviewers (and users) a one-click context page.
